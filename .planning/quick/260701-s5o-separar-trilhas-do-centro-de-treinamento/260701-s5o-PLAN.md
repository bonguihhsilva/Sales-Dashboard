---
phase: 260701-s5o-separar-trilhas-do-centro-de-treinamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/vendedor/treinamentos/categorias.ts
  - src/app/vendedor/treinamentos/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vendedor vê as 14 trilhas do Centro de Treinamentos agrupadas em 3 seções visuais: Vendas e Atendimento, Skin Care, Perfumes"
    - "Cada seção tem um título/header visível acima do grupo de cards correspondente"
    - "Cards de trilha mantêm exatamente o mesmo visual (TrilhaCard.tsx intocado)"
    - "Contadores do topo (trilhas, módulos, XP, concluídas) continuam corretos e somando todas as trilhas"
    - "Nenhuma migração de banco foi criada — categorização é 100% front-end"
  artifacts:
    - path: "src/app/vendedor/treinamentos/categorias.ts"
      provides: "Mapa estático titulo da trilha -> categoria + helper de agrupamento"
      exports: ["CATEGORIA_ORDEM", "categorizarTrilhas"]
    - path: "src/app/vendedor/treinamentos/page.tsx"
      provides: "Renderiza grid agrupado por categoria com header de seção"
      contains: "categorizarTrilhas"
  key_links:
    - from: "src/app/vendedor/treinamentos/page.tsx"
      to: "src/app/vendedor/treinamentos/categorias.ts"
      via: "import { categorizarTrilhas } from './categorias'"
      pattern: "categorizarTrilhas\\(trilhas\\)"
---

<objective>
Separar as 14 trilhas do Centro de Treinamentos (`/vendedor/treinamentos`) em 3 categorias visuais — "Vendas e Atendimento", "Skin Care", "Perfumes" — usando um mapeamento estático nome-da-trilha → categoria no front-end. Sem migração de banco, sem novo campo em `trilhas`.

Purpose: Facilitar navegação do vendedor conforme o catálogo de treinamentos cresce (hoje 14 trilhas, 62 módulos, cobrindo vendas, skincare e perfumaria).
Output: `src/app/vendedor/treinamentos/categorias.ts` (mapa + helper de agrupamento) e `page.tsx` atualizado renderizando 3 seções com header, mantendo `TrilhaCard.tsx` e o restante do layout intocados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Página e componentes existentes (não modificar TrilhaCard.tsx)
@src/app/vendedor/treinamentos/page.tsx
@src/app/vendedor/treinamentos/TrilhaCard.tsx
@src/lib/lms/theme.ts
@src/lib/lms/queries.ts
</context>

<interfaces>
<!-- CatalogoTrilha, de src/lib/lms/queries.ts — o tipo que page.tsx recebe de getCatalogo() -->
```typescript
export interface CatalogoTrilha {
  id: string
  titulo: string
  descricao: string | null
  icon: string | null
  cor: string | null
  is_global: boolean
  ordem: number
  moduloCount: number
  totalXp: number
  progressoPct: number
}
```

<!-- Não há campo de categoria em trilhas. Coluna publico_alvo existe no banco mas com valores
inconsistentes ('vendas' | 'skincare' | 'Vendedores' | NULL) e NÃO é selecionada por getCatalogo().
Categorização deve ser feita 100% no front-end via mapa titulo -> categoria. -->

<!-- Tokens de tema disponíveis em src/lib/lms/theme.ts (import { LMS as C }) -->
```typescript
export const LMS = {
  deep: '#0C0C0E', elevated: '#1C1C22', surface2: '#252530',
  border: 'rgba(255,255,255,0.06)', borderStrong: 'rgba(255,255,255,0.12)',
  text: '#F2F1F4', muted: '#7A7682',
  gold: '#C9933A', amber: '#F5A742', blue: '#3B82F6', green: '#22C55E',
  pink: '#E91E8C', // já documentado como "trilha de skincare"
} as const
```

<!-- Títulos exatos das 14 trilhas seedadas (confirmados via grep nas migrations) -->
```
Vendas e Atendimento:
  Fundamentos de Vendas
  Processo de Venda
  Fundamentos de Vendas Consultivas
  Negociação e Objeções
  Excelência em Atendimento ao Cliente
  Estratégia e Liderança
  Atacado B2B
  Vendas Online e Atendimento Web
  Estratégia Profissional

Skin Care:
  Fundamentos do K-Beauty
  Conhecendo a Pele
  Ingredientes Ativos
  Rotina e Marcas

Perfumes:
  Perfumaria de Alto Nível
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar mapa de categorias e helper de agrupamento</name>
  <files>src/app/vendedor/treinamentos/categorias.ts</files>
  <action>
    Criar novo arquivo `src/app/vendedor/treinamentos/categorias.ts` exportando:

    1. `CATEGORIA_ORDEM: readonly string[]` — array com as 3 categorias na ordem de exibição: `['Vendas e Atendimento', 'Skin Care', 'Perfumes']`.

    2. Um `Record<string, string>` privado (`TRILHA_CATEGORIA`) mapeando titulo exato da trilha -> nome da categoria, usando os 14 títulos listados no bloco `<interfaces>` acima (comparação exata de string, sem normalização de acentos/case — os títulos vêm fixos do seed).

    3. `categorizarTrilhas<T extends { titulo: string }>(trilhas: T[]): { categoria: string; trilhas: T[] }[]` — função que:
       - Agrupa as trilhas recebidas pelo mapa `TRILHA_CATEGORIA`
       - Se uma trilha não tiver categoria mapeada (titulo desconhecido — ex: trilha nova ainda não catalogada), cai em categoria fallback `'Outros'` — não deve lançar erro nem excluir a trilha do grid
       - Retorna array de grupos na ordem de `CATEGORIA_ORDEM` (e `'Outros'` por último, apenas se houver alguma trilha nesse grupo)
       - Preserva a ordem relativa das trilhas dentro de cada grupo (a ordem já vem de `ordem` via `getCatalogo`, que faz `.order('ordem', { ascending: true })` — não reordenar)
       - Grupos vazios (0 trilhas) não aparecem no resultado

    Usar TypeScript genérico para não acoplar ao tipo `CatalogoTrilha` importado (evita import circular/desnecessário) — `T extends { titulo: string }` é suficiente.
  </action>
  <verify>
    <automated>cd C:\Projetos\da-silva-dashboard; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Arquivo `categorias.ts` existe, exporta `CATEGORIA_ORDEM` e `categorizarTrilhas`, compila sem erros de tipo. Chamar `categorizarTrilhas` com um array simulando as 14 trilhas produz 3 grupos nas categorias corretas, sem trilhas perdidas ou duplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Renderizar grid agrupado por categoria em page.tsx</name>
  <files>src/app/vendedor/treinamentos/page.tsx</files>
  <action>
    Editar `src/app/vendedor/treinamentos/page.tsx`:

    1. Importar `categorizarTrilhas` de `./categorias`.

    2. Após buscar `trilhas` (linha `const trilhas = await getCatalogo(user.id)`), calcular `const grupos = categorizarTrilhas(trilhas)`. Manter os cálculos de `totalModulos`, `totalXP`, `concluidas` exatamente como estão hoje (somam sobre `trilhas`, não sobre `grupos` — não alterar essa lógica).

    3. Substituir o bloco atual de "Grid de trilhas" (o `<div style={{ display: 'grid', ...}}>` único que mapeia `trilhas.map(...)`) por uma renderização que itera `grupos` e, para cada grupo, renderiza:
       - Um header de seção acima do grid: título da categoria (`grupo.categoria`) em `fontFamily: 'Syne, sans-serif'`, `fontWeight: 700`, tamanho um pouco menor que o título da página (ex: `1.0625rem`–`1.125rem`), cor `C.text`, com contagem de trilhas ao lado em `DM Mono` + `C.muted` (ex: "Vendas e Atendimento · 9 trilhas"). Adicionar `marginBottom` (~0.875rem) entre o header e o grid de cards do grupo, e `marginTop`/`marginBottom` (~2rem) entre seções consecutivas.
       - O mesmo grid de cards já existente (`display: grid`, `gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))'`, `gap: '1.25rem'`) contendo `grupo.trilhas.map(trilha => <TrilhaCard key={trilha.id} trilha={trilha} />)` — reaproveitar exatamente o mesmo estilo de grid que já existe hoje, sem alterar `TrilhaCard.tsx`.

    4. Manter o estado vazio (`trilhas.length === 0`) exatamente como está — só entra no novo bloco de grupos quando há trilhas.

    5. Não alterar a "Top bar" (título da página, contadores de XP/concluídas) nem nenhum outro trecho do arquivo além do bloco do grid.
  </action>
  <verify>
    <automated>cd C:\Projetos\da-silva-dashboard; npx tsc --noEmit -p tsconfig.json; npm run build</automated>
  </verify>
  <done>`npm run build` passa sem erros. Página `/vendedor/treinamentos` renderiza 3 seções com header (Vendas e Atendimento, Skin Care, Perfumes) cada uma com seu grid de `TrilhaCard`, na ordem correta, mantendo contadores do topo e demais elementos intactos. Nenhum arquivo de migration foi criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Mudança puramente de apresentação (agrupamento visual client-side de dados já autorizados/carregados via RLS existente). Nenhum novo trust boundary introduzido — `getCatalogo` já filtra por `ativa = true` e RLS de `trilhas` permanece inalterada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260701s5o-01 | Information Disclosure | categorias.ts (mapa estático) | accept | Mapa contém apenas títulos de trilhas já públicas para o vendedor autenticado (mesmos dados já retornados por `getCatalogo`); nenhuma informação nova exposta. |
</threat_model>

<verification>
1. `npm run build` sem erros de tipo ou lint bloqueante.
2. Visualmente (via checkpoint humano fora deste plano, se necessário): acessar `/vendedor/treinamentos` logado como vendedor e confirmar as 3 seções com headers, cards intactos, contadores do topo corretos.
3. Confirmar via `git status` que nenhum arquivo em `supabase/migrations/` foi criado ou modificado por esta tarefa.
</verification>

<success_criteria>
- As 14 trilhas aparecem agrupadas em exatamente 3 seções visuais com headers: "Vendas e Atendimento" (9 trilhas), "Skin Care" (4 trilhas), "Perfumes" (1 trilha).
- `TrilhaCard.tsx` não foi modificado.
- Nenhuma migração de banco foi criada.
- `npm run build` passa.
</success_criteria>

<output>
After completion, create `.planning/quick/260701-s5o-separar-trilhas-do-centro-de-treinamento/260701-s5o-SUMMARY.md`
</output>
