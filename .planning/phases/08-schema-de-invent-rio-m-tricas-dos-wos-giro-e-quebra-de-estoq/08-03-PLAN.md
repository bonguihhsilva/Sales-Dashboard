---
phase: 08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/20260720000005_product_costs_seed_and_upsert.sql
  - src/app/api/admin/upload-catalog/route.ts
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "product_costs é semeada a partir do products do período mais recente por tenant, sem sobrescrever custo já existente"
    - "Existe uma RPC upsert_product_costs que respeita a precedência: source='manual' nunca é sobrescrito por 'catalog'"
    - "upload-catalog passa a popular product_costs além de products, de forma aditiva"
    - "products não é alterada (D-04)"
  artifacts:
    - path: "supabase/migrations/20260720000005_product_costs_seed_and_upsert.sql"
      provides: "seed INSERT...SELECT + função upsert_product_costs"
      contains: "upsert_product_costs"
    - path: "src/app/api/admin/upload-catalog/route.ts"
      provides: "segundo destino de escrita (product_costs) via RPC"
      contains: "upsert_product_costs"
  key_links:
    - from: "upload-catalog/route.ts"
      to: "product_costs"
      via: "adminDb.rpc('upsert_product_costs', ...)"
      pattern: "upsert_product_costs"
---

<objective>
Popular `product_costs` (D-03): (a) um seed de bootstrap a partir do `products` do período mais recente por tenant, e (b) estender a rota `upload-catalog` para também gravar em `product_costs` a cada upload de catálogo — respeitando a regra de precedência `source='manual'` > `catalog`/`api`. Esta é a ÚNICA mudança de código de aplicação da fase, e é aditiva.

Purpose: sem isso, `product_costs` nasce vazia e todas as métricas monetárias (`stock_value`, `turnover`) são NULL para todos os produtos até o próximo upload (R-04). A precedência é implementada em SQL (RPC) porque o upsert do supabase-js não sabe fazer `DO UPDATE ... WHERE source <> 'manual'`.
Output: 1 migration (seed + RPC) + 1 edição em upload-catalog/route.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-CONTEXT.md
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md
@src/app/api/admin/upload-catalog/route.ts
@supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql

<interfaces>
- products (tenant_id, period_id, product_code, name, cost_price numeric) — fonte do custo
- periods (id, tenant_id, year, month) — período mais recente = maior (year, month)
- product_costs (tenant_id, product_code, unit_cost, name, source, updated_at) — PK (tenant_id, product_code)
- upload-catalog/route.ts: já monta `rows: ProductRow[]` e faz `adminDb.from('products').upsert(rows, { onConflict: 'tenant_id,period_id,product_code' })`. Cada row tem { tenant_id, period_id, product_code, name, cost_price, sale_price, margin_pct }.
- createAdminClient() (service-role) já importado como adminDb.

Padrão de RPC plpgsql com jsonb do projeto: ver ingest_sales_records em 20260714000001 (jsonb_array_elements, GET DIAGNOSTICS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration seed product_costs + RPC upsert_product_costs (D-03)</name>
  <files>supabase/migrations/20260720000005_product_costs_seed_and_upsert.sql</files>
  <read_first>
    - 08-CONTEXT.md D-03, D-04
    - 08-RESEARCH.md "Seed inicial de product_costs (D-03)" (INSERT...SELECT verbatim) e "Assumptions Log" A5 (usar year/month, não MAX(id))
    - 08-DIAGNOSTICS.md seção R-04 (quantos SKUs o seed cobre)
    - supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql (padrão plpgsql jsonb)
  </read_first>
  <action>
    Criar `supabase/migrations/20260720000005_product_costs_seed_and_upsert.sql` com duas partes.

    Parte 1 — função `upsert_product_costs` que implementa a precedência D-03 em SQL (`DO UPDATE ... WHERE product_costs.source <> 'manual'`):

    ```sql
    -- Phase 08: população de product_costs (D-03).

    -- ── 1. upsert_product_costs: destino de escrita do upload-catalog ──────────
    -- Precedência D-03: manual nunca é sobrescrito por catalog; catalog sobrescreve
    -- catalog e api. SECURITY INVOKER (default): chamada só via service-role.
    CREATE OR REPLACE FUNCTION upsert_product_costs(p_tenant_id uuid, p_rows jsonb)
    RETURNS integer
    LANGUAGE plpgsql
    SET search_path = public
    AS $$
    DECLARE
      v_count integer;
    BEGIN
      INSERT INTO product_costs (tenant_id, product_code, unit_cost, name, source, updated_at)
      SELECT
        p_tenant_id,
        elem->>'product_code',
        NULLIF(elem->>'unit_cost', '')::numeric,
        elem->>'name',
        'catalog',
        now()
      FROM jsonb_array_elements(p_rows) AS elem
      WHERE elem->>'product_code' IS NOT NULL AND elem->>'product_code' <> ''
      ON CONFLICT (tenant_id, product_code) DO UPDATE
        SET unit_cost  = EXCLUDED.unit_cost,
            name       = EXCLUDED.name,
            source     = 'catalog',
            updated_at = now()
        WHERE product_costs.source <> 'manual';
      GET DIAGNOSTICS v_count = ROW_COUNT;
      RETURN v_count;
    END;
    $$;

    REVOKE ALL ON FUNCTION public.upsert_product_costs(uuid, jsonb) FROM anon, authenticated, public;
    GRANT EXECUTE ON FUNCTION public.upsert_product_costs(uuid, jsonb) TO service_role;
    ```

    Parte 2 — seed de bootstrap a partir do período mais recente (verbatim do RESEARCH, mapeando `products.cost_price` → `product_costs.unit_cost`), com `ON CONFLICT DO NOTHING` (nunca reverte um custo já populado):

    ```sql
    -- ── 2. Seed de bootstrap a partir do products do período mais recente ──────
    -- DO NOTHING: se um tenant já populou product_costs por upload real, o seed
    -- não reverte nada. Não é backfill de períodos antigos (D-03).
    WITH latest_period AS (
      SELECT DISTINCT ON (tenant_id) tenant_id, id AS period_id
      FROM periods
      ORDER BY tenant_id, year DESC, month DESC
    )
    INSERT INTO product_costs (tenant_id, product_code, unit_cost, name, source, updated_at)
    SELECT
      p.tenant_id,
      p.product_code,
      p.cost_price,
      p.name,
      'catalog',
      now()
    FROM products p
    JOIN latest_period lp
      ON lp.tenant_id = p.tenant_id AND lp.period_id = p.period_id
    ON CONFLICT (tenant_id, product_code) DO NOTHING;
    ```

    NÃO alterar `products` de forma alguma (D-04).
  </action>
  <acceptance_criteria>
    - Arquivo contém `CREATE OR REPLACE FUNCTION upsert_product_costs(p_tenant_id uuid, p_rows jsonb)` com `WHERE product_costs.source <> 'manual'` no DO UPDATE.
    - Contém `SET search_path = public` e `GRANT EXECUTE ... TO service_role`.
    - Contém o seed `INSERT INTO product_costs ... ON CONFLICT (tenant_id, product_code) DO NOTHING` usando `ORDER BY tenant_id, year DESC, month DESC`.
    - NÃO contém `ALTER TABLE products`.
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000005_product_costs_seed_and_upsert.sql"; grep -q "CREATE OR REPLACE FUNCTION upsert_product_costs" "$f" && grep -q "product_costs.source <> 'manual'" "$f" && grep -q "SET search_path = public" "$f" && grep -q "ON CONFLICT (tenant_id, product_code) DO NOTHING" "$f" && grep -q "year DESC, month DESC" "$f" && ! grep -q "ALTER TABLE products" "$f" && echo OK</automated>
  </verify>
  <done>Migration cria upsert_product_costs (com precedência manual) e semeia product_costs do período mais recente; products intocada.</done>
</task>

<task type="auto">
  <name>Task 2: upload-catalog grava em product_costs via RPC (D-03)</name>
  <files>src/app/api/admin/upload-catalog/route.ts</files>
  <read_first>
    - src/app/api/admin/upload-catalog/route.ts (linhas 78-84: onde products é upsertada — inserir o segundo destino logo após)
    - 08-CONTEXT.md D-03 (precedência) e "Integration Points"
  </read_first>
  <action>
    Editar `src/app/api/admin/upload-catalog/route.ts`. Logo APÓS o bloco que faz `adminDb.from('products').upsert(rows, ...)` e checa `error` (linhas ~78-82), adicionar a chamada aditiva à RPC `upsert_product_costs`, montando o array jsonb a partir das mesmas `rows` (mapeando `cost_price` → `unit_cost`). A falha do upsert de custo NÃO deve derrubar o upload de catálogo (que já teve sucesso em products) — logar mas retornar sucesso do products. Manter o retorno atual `{ imported: rows.length }`, adicionando `costs_updated`:

    ```ts
    // Segundo destino (D-03, Fase 08): custo vigente em product_costs.
    // Aditivo — precedência (manual não sobrescrito) é resolvida na RPC.
    const costRows = rows.map(r => ({
      product_code: r.product_code,
      unit_cost: r.cost_price,
      name: r.name,
    }))
    const { data: costsUpdated, error: costErr } = await adminDb.rpc('upsert_product_costs', {
      p_tenant_id: profile.tenant_id,
      p_rows: costRows,
    })
    if (costErr) {
      // Não derruba o upload: products já foi gravado com sucesso.
      console.error('upsert_product_costs falhou:', costErr.message)
    }

    return NextResponse.json({ imported: rows.length, costs_updated: costsUpdated ?? 0 })
    ```

    Remover o `return NextResponse.json({ imported: rows.length })` anterior (substituído pelo novo return acima). Não alterar mais nada na rota (auth, rate limit, parsers permanecem idênticos). Rodar `npm run lint` e `npm run build` para confirmar TypeScript strict.

    Nota de tipos: `adminDb` (createAdminClient) é usado hoje sem generic `Database`, então `.rpc('upsert_product_costs', ...)` compila mesmo sem regenerar `src/types/supabase.ts`. Se o cliente for tipado e o build reclamar de RPC desconhecida, regenerar tipos é tarefa pós-push (Plano 07) — neste caso, usar `adminDb.rpc('upsert_product_costs' as never, {...} as never)` NÃO é aceitável; em vez disso confirmar que createAdminClient não passa generic Database (é o caso hoje).
  </action>
  <acceptance_criteria>
    - `upload-catalog/route.ts` contém `adminDb.rpc('upsert_product_costs'` e monta `costRows` com `unit_cost: r.cost_price`.
    - O retorno de sucesso inclui `costs_updated`.
    - `npm run build` passa (TypeScript strict).
    - Auth/rate-limit/parsers inalterados (o diff toca apenas o bloco após o upsert de products).
  </acceptance_criteria>
  <verify>
    <automated>f="src/app/api/admin/upload-catalog/route.ts"; grep -q "upsert_product_costs" "$f" && grep -q "costs_updated" "$f" && grep -q "unit_cost: r.cost_price" "$f" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>upload-catalog grava product_costs via RPC de forma aditiva; build passa; nenhuma regressão no fluxo de products.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload de catálogo (adm/gerente/super_admin) → product_costs | Escrita de custo cruza a fronteira; deve ser escopada ao tenant do caller |
| RPC upsert_product_costs → product_costs | Executável só por service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | upsert sobrescrevendo custo manual | mitigate | `DO UPDATE ... WHERE source <> 'manual'` — precedência D-03 garantida no SQL, não no cliente. |
| T-08-10 | Elevation of Privilege | RPC chamável por authenticated | mitigate | REVOKE EXECUTE FROM anon, authenticated, public + GRANT só a service_role; `p_tenant_id` explícito vindo de profile.tenant_id validado na rota. |
| T-08-11 | Spoofing | upload gravando custo em tenant alheio | mitigate | Rota já valida `period.tenant_id === profile.tenant_id`; RPC recebe `p_tenant_id = profile.tenant_id`. |
</threat_model>

<verification>
- Migration cria RPC com precedência manual e seed idempotente (DO NOTHING).
- upload-catalog grava product_costs aditivamente; build TypeScript passa.
- products não alterada.
</verification>

<success_criteria>
- product_costs deixa de nascer vazia (seed) e passa a ser mantida por upload (D-03).
- Precedência manual > catalog implementada e testável.
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-03-SUMMARY.md`
</output>
