---
phase: 08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260720000001_product_costs_and_inventory_settings.sql
  - supabase/migrations/20260720000002_inventory_indexes.sql
autonomous: true
requirements: [D-01, D-02, D-05, D-06, D-22]
must_haves:
  truths:
    - "Tabela product_costs existe com PK (tenant_id, product_code) e RLS tenant_isolation ativa"
    - "Tabela inventory_settings existe com todos os thresholds e DEFAULTs de D-05, RLS ativa"
    - "Os 4 índices de D-22 existem para suportar as views das waves seguintes"
  artifacts:
    - path: "supabase/migrations/20260720000001_product_costs_and_inventory_settings.sql"
      provides: "product_costs + inventory_settings + RLS"
      contains: "CREATE TABLE IF NOT EXISTS product_costs"
    - path: "supabase/migrations/20260720000002_inventory_indexes.sql"
      provides: "4 índices D-22"
      contains: "idx_stock_snapshots_tenant_product_date"
  key_links:
    - from: "product_inventory_metrics (wave 3)"
      to: "stock_snapshots (tenant_id, product_code, snapshot_date DESC)"
      via: "DISTINCT ON usa o índice idx_stock_snapshots_tenant_product_date"
      pattern: "idx_stock_snapshots_tenant_product_date"
---

<objective>
Criar as duas tabelas novas da fase (`product_costs` D-01/D-02, `inventory_settings` D-05/D-06) com RLS `tenant_isolation`, e os 4 índices obrigatórios de D-22. Estas são as fundações que TODAS as views/RPCs das waves seguintes consomem — por isso são wave 1, e os índices precisam existir antes das views (Pitfall 6 do RESEARCH).

Purpose: `product_costs` fornece o custo vigente por produto (base de `stock_value`/`turnover`); `inventory_settings` fornece thresholds configuráveis por tenant com defaults no schema (D-06: ausência de linha não quebra nada); os índices evitam que as views degradem linearmente com o histórico.
Output: 2 migrations SQL. NÃO aplicadas ao banco (aplicação é o Plano 07 [BLOCKING]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-CONTEXT.md
@supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql
@supabase/migrations/20260626171308_profit_commission.sql
@supabase/migrations/20260603000022_perf_rls_initplan_wrap_auth_calls.sql

<interfaces>
Padrão de RLS tenant_isolation do projeto (verbatim de products/stock_snapshots), com wrapper de initplan D-02:
```sql
ALTER TABLE <tabela> ENABLE ROW LEVEL SECURITY;
CREATE POLICY "tenant_isolation" ON <tabela>
  USING (tenant_id IN (SELECT tenant_id FROM profiles WHERE id = (SELECT auth.uid())));
```
Índices existentes hoje (NÃO recriar, NÃO remover):
- stock_snapshots: `stock_snapshots_tenant_date (tenant_id, snapshot_date)` — cobre janela mas NÃO agrupamento por produto
- sales_records: `idx_sales_records_tenant_id (tenant_id)` + UNIQUE `uq_sales_records_tenant_period_ref (tenant_id, period_id, order_ref)`
- sale_items: `sale_items_period_vendor (tenant_id, period_id, vendor_id)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration product_costs + inventory_settings + RLS (D-01, D-02, D-05, D-06)</name>
  <files>supabase/migrations/20260720000001_product_costs_and_inventory_settings.sql</files>
  <read_first>
    - 08-CONTEXT.md D-01, D-02, D-05, D-06 (DDL verbatim das duas tabelas)
    - supabase/migrations/20260626171308_profit_commission.sql (padrão tenant_isolation em products, linhas 19-24)
  </read_first>
  <action>
    Criar `supabase/migrations/20260720000001_product_costs_and_inventory_settings.sql` com o DDL EXATO de D-01 e D-05, comentado em português com separadores `-- ── N. titulo ───`:

    ```sql
    -- Phase 08: custo vigente por produto (product_costs) + thresholds de inventário (inventory_settings)

    -- ── 1. product_costs (D-01/D-02) ───────────────────────────────────────────
    -- Custo VIGENTE por produto, sem period_id (não é histórico — products cobre isso).
    -- unit_cost nullable de propósito (D-16/D-18: custo desconhecido ≠ zero).
    CREATE TABLE IF NOT EXISTS product_costs (
      tenant_id     uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
      product_code  text NOT NULL,
      unit_cost     numeric(12,2),
      name          text,
      source        text NOT NULL DEFAULT 'catalog',   -- 'catalog' | 'manual' | 'api'
      updated_at    timestamptz NOT NULL DEFAULT now(),
      PRIMARY KEY (tenant_id, product_code)
    );

    ALTER TABLE product_costs ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "tenant_isolation" ON product_costs
      USING (tenant_id IN (
        SELECT tenant_id FROM profiles WHERE id = (SELECT auth.uid())
      ));

    -- ── 2. inventory_settings (D-05/D-06) ──────────────────────────────────────
    -- Uma linha por tenant. Ausência de linha NÃO quebra nada: as views usam
    -- LEFT JOIN + COALESCE(coluna, <default literal>) — um tenant sem linha
    -- recebe exatamente estes defaults.
    CREATE TABLE IF NOT EXISTS inventory_settings (
      tenant_id            uuid PRIMARY KEY REFERENCES tenants(id) ON DELETE CASCADE,
      sales_window_days    integer NOT NULL DEFAULT 30,
      long_window_days     integer NOT NULL DEFAULT 90,
      fast_mover_dos_max   numeric NOT NULL DEFAULT 15,
      slow_mover_dos_min   numeric NOT NULL DEFAULT 120,
      dead_stock_days      integer NOT NULL DEFAULT 90,
      stale_snapshot_days  integer NOT NULL DEFAULT 7,
      abc_cut_a            numeric NOT NULL DEFAULT 0.80,
      abc_cut_b            numeric NOT NULL DEFAULT 0.95,
      updated_at           timestamptz NOT NULL DEFAULT now()
    );

    ALTER TABLE inventory_settings ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "tenant_isolation" ON inventory_settings
      USING (tenant_id IN (
        SELECT tenant_id FROM profiles WHERE id = (SELECT auth.uid())
      ));
    ```

    NÃO inserir nenhuma linha em inventory_settings (D-06: defaults vêm do COALESCE nas views; a linha só é criada por SQL quando um tenant quiser divergir). NÃO tocar em `products` (D-04).
  </action>
  <acceptance_criteria>
    - Arquivo contém `CREATE TABLE IF NOT EXISTS product_costs` com `PRIMARY KEY (tenant_id, product_code)`.
    - Arquivo contém `CREATE TABLE IF NOT EXISTS inventory_settings` com `tenant_id uuid PRIMARY KEY` e os 8 thresholds com DEFAULTs de D-05.
    - Contém `ENABLE ROW LEVEL SECURITY` duas vezes e `tenant_isolation` duas vezes com `(SELECT auth.uid())`.
    - grep: os 8 nomes de coluna de D-05 (`sales_window_days`, `long_window_days`, `fast_mover_dos_max`, `slow_mover_dos_min`, `dead_stock_days`, `stale_snapshot_days`, `abc_cut_a`, `abc_cut_b`) todos presentes.
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000001_product_costs_and_inventory_settings.sql"; grep -q "PRIMARY KEY (tenant_id, product_code)" "$f" && grep -q "tenant_id uuid PRIMARY KEY" "$f" && grep -q "(SELECT auth.uid())" "$f" && for c in sales_window_days long_window_days fast_mover_dos_max slow_mover_dos_min dead_stock_days stale_snapshot_days abc_cut_a abc_cut_b; do grep -q "$c" "$f" || { echo "MISSING $c"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Migration cria product_costs e inventory_settings com RLS tenant_isolation e defaults D-05; products intocada.</done>
</task>

<task type="auto">
  <name>Task 2: Migration dos 4 índices de D-22</name>
  <files>supabase/migrations/20260720000002_inventory_indexes.sql</files>
  <read_first>
    - 08-CONTEXT.md D-22 (lista dos 4 índices)
    - 08-RESEARCH.md "Índices (D-22) — sintaxe e verificação de duplicata" (comentários sobre quais já existem)
  </read_first>
  <action>
    Criar `supabase/migrations/20260720000002_inventory_indexes.sql` com os 4 índices EXATOS do RESEARCH (todos `IF NOT EXISTS`, seguros contra duplicata). NÃO remover nenhum índice existente:

    ```sql
    -- Phase 08: índices de suporte às métricas de inventário (D-22).
    -- Ordem: esta migration roda ANTES das views (Pitfall 6) para evitar full scan
    -- na primeira execução das views em produção.

    -- 1. Resolve "snapshot mais recente por produto" (DISTINCT ON) e janelas por produto.
    --    O existente stock_snapshots_tenant_date (tenant_id, snapshot_date) NÃO cobre
    --    o agrupamento por product_code.
    CREATE INDEX IF NOT EXISTS idx_stock_snapshots_tenant_product_date
      ON stock_snapshots (tenant_id, product_code, snapshot_date DESC);

    -- 2. Chave da junção D-07 (sale_items.order_id = sales_records.order_ref).
    --    Inexistente hoje: o UNIQUE (tenant_id, period_id, order_ref) não cobre
    --    filtro por (tenant_id, order_ref) sem period_id.
    CREATE INDEX IF NOT EXISTS idx_sales_records_tenant_order_ref
      ON sales_records (tenant_id, order_ref);

    -- 3. Filtro de janela de data. idx_sales_records_tenant_id existe mas só cobre
    --    tenant_id; (tenant_id, sale_date) é mais específico e não duplica.
    CREATE INDEX IF NOT EXISTS idx_sales_records_tenant_sale_date
      ON sales_records (tenant_id, sale_date);

    -- 4. Agregação por produto. Existentes cobrem (tenant_id, period_id, vendor_id)
    --    e (period_id) — nenhum cobre (tenant_id, product_code).
    CREATE INDEX IF NOT EXISTS idx_sale_items_tenant_product
      ON sale_items (tenant_id, product_code);
    ```
  </action>
  <acceptance_criteria>
    - Arquivo contém os 4 nomes de índice: `idx_stock_snapshots_tenant_product_date`, `idx_sales_records_tenant_order_ref`, `idx_sales_records_tenant_sale_date`, `idx_sale_items_tenant_product`.
    - Todos os 4 usam `CREATE INDEX IF NOT EXISTS`.
    - O índice de snapshots usa `snapshot_date DESC`.
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000002_inventory_indexes.sql"; n=$(grep -c "CREATE INDEX IF NOT EXISTS" "$f"); [ "$n" -eq 4 ] && grep -q "snapshot_date DESC" "$f" && grep -q "idx_sales_records_tenant_order_ref" "$f" && grep -q "idx_sale_items_tenant_product" "$f" && echo OK</automated>
  </verify>
  <done>4 índices de D-22 criados via IF NOT EXISTS; nenhum índice existente removido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente authenticated → product_costs / inventory_settings | Toda leitura direta por sessão authenticated deve ser bloqueada exceto pela própria linha do tenant (RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | product_costs (custo é dado sensível) | mitigate | RLS `tenant_isolation` no CREATE TABLE — um tenant nunca vê custo de outro. Leitura da Fase 09 via service-role com `.eq('tenant_id',...)` explícito. |
| T-08-04 | Elevation of Privilege | policy RLS mal formada | mitigate | Usar o wrapper `(SELECT auth.uid())` (initplan D-02), idêntico a products/stock_snapshots — padrão auditado do projeto. |
| T-08-05 | Tampering | inventory_settings alterado por tenant errado | mitigate | RLS restringe USING a linhas do próprio tenant; PK impede linha duplicada por tenant. |
</threat_model>

<verification>
- Ambas as migrations existem em supabase/migrations/ com timestamps 20260720000001 e 20260720000002.
- product_costs e inventory_settings têm RLS ativa e policy tenant_isolation com `(SELECT auth.uid())`.
- 4 índices D-22 declarados IF NOT EXISTS.
- Nenhuma alteração em products (D-04).
</verification>

<success_criteria>
- Fundação de schema pronta para as views (wave 2+) consumirem.
- Ordering respeitado: índices e tabelas antes das views.
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-01-SUMMARY.md`
</output>
