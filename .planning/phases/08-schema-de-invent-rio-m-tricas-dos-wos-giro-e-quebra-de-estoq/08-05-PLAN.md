---
phase: 08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq
plan: 05
type: execute
wave: 4
depends_on: [02, 04]
files_modified:
  - supabase/migrations/20260720000008_inventory_rpcs.sql
autonomous: true
requirements: [D-20, D-21]
must_haves:
  truths:
    - "product_stock_history(p_tenant_id, p_product_code, p_days) retorna uma linha por dia do intervalo, incluindo dias sem snapshot com quantity NULL"
    - "inventory_summary(p_tenant_id) retorna os 8 KPIs de topo do contrato numa única linha"
    - "Ambas as RPCs são SECURITY INVOKER, recebem p_tenant_id explícito, têm search_path fixo e EXECUTE só para service_role"
  artifacts:
    - path: "supabase/migrations/20260720000008_inventory_rpcs.sql"
      provides: "RPCs product_stock_history e inventory_summary"
      contains: "CREATE OR REPLACE FUNCTION product_stock_history"
  key_links:
    - from: "inventory_summary"
      to: "product_inventory_metrics"
      via: "agregação FILTER sobre a view por p_tenant_id"
      pattern: "FROM product_inventory_metrics"
---

<objective>
Criar as duas RPCs da fase: `product_stock_history` (série temporal de um produto para sparkline, incluindo dias sem snapshot) e `inventory_summary` (os 8 KPIs de topo da tela `/dashboard/compras`). RPCs, não views, porque precisam de parâmetro variável (`p_product_code`, `p_days`) ou retornam um agregado escalar do tenant (D-20).

Purpose: fecham o contrato de saída para a Fase 09. `product_stock_history` alimenta o gráfico de um produto; `inventory_summary` alimenta os KPI cards.
Output: 1 migration SQL. NÃO aplicada ao banco.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-CONTEXT.md
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-RESEARCH.md
@supabase/migrations/20260601000019_p0_security_closeout.sql

<interfaces>
Contrato RPCs:
- product_stock_history(p_tenant_id uuid, p_product_code text, p_days integer DEFAULT 90) RETURNS TABLE (day date, quantity numeric, qty_sold numeric)
- inventory_summary(p_tenant_id uuid) RETURNS TABLE (total_skus integer, total_stock_value numeric, skus_sem_custo integer, skus_em_quebra integer, skus_parados integer, skus_stale integer, dos_medio numeric, turnover_medio numeric)

Padrão do projeto (store_daily_evolution_multi, vendor_evolution): LANGUAGE sql, SEM SECURITY DEFINER (default INVOKER), SET search_path = public, p_tenant_id explícito. Chamadas só via createAdminClient() (service-role bypassa RLS).
Fontes: stock_snapshots, product_daily_sales (Plano 02), product_inventory_metrics (Plano 04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RPCs product_stock_history + inventory_summary (D-20, D-21)</name>
  <files>supabase/migrations/20260720000008_inventory_rpcs.sql</files>
  <read_first>
    - 08-CONTEXT.md contrato de RPCs (assinaturas e RETURNS TABLE exatos) e D-20/D-21
    - 08-RESEARCH.md "Assinatura de RPC com p_tenant_id" (product_stock_history verbatim com generate_series) e "Don't Hand-Roll" (generate_series LEFT JOIN)
  </read_first>
  <action>
    Criar `supabase/migrations/20260720000008_inventory_rpcs.sql` com as duas funções. `product_stock_history` usa `generate_series` como base com LEFT JOIN (dias sem snapshot → quantity NULL, Pitfall 5). `inventory_summary` agrega `product_inventory_metrics` por tenant. Ambas com `SET search_path = public` e EXECUTE restrito. Copiar:

    ```sql
    -- Phase 08: RPCs de inventário (D-20). SECURITY INVOKER (default), p_tenant_id explícito.

    -- ── 1. product_stock_history: série temporal de um produto ─────────────────
    -- Uma linha por dia do intervalo, incluindo dias sem snapshot (quantity NULL)
    -- para o gráfico não mentir sobre continuidade (Pitfall 5).
    CREATE OR REPLACE FUNCTION product_stock_history(
      p_tenant_id    uuid,
      p_product_code text,
      p_days         integer DEFAULT 90
    )
    RETURNS TABLE(day date, quantity numeric, qty_sold numeric)
    LANGUAGE sql
    SET search_path = public
    AS $$
      SELECT
        d.day,
        s.quantity,
        COALESCE(ds.qty_sold, 0) AS qty_sold
      FROM generate_series(CURRENT_DATE - p_days, CURRENT_DATE, interval '1 day')::date AS d(day)
      LEFT JOIN stock_snapshots s
        ON s.tenant_id = p_tenant_id
       AND s.product_code = p_product_code
       AND s.snapshot_date = d.day
      LEFT JOIN product_daily_sales ds
        ON ds.tenant_id = p_tenant_id
       AND ds.product_code = p_product_code
       AND ds.sale_date = d.day
      ORDER BY d.day;
    $$;

    REVOKE ALL ON FUNCTION public.product_stock_history(uuid, text, integer) FROM anon, authenticated, public;
    GRANT EXECUTE ON FUNCTION public.product_stock_history(uuid, text, integer) TO service_role;

    -- ── 2. inventory_summary: KPIs de topo da tela /dashboard/compras ──────────
    CREATE OR REPLACE FUNCTION inventory_summary(p_tenant_id uuid)
    RETURNS TABLE(
      total_skus         integer,
      total_stock_value  numeric,
      skus_sem_custo     integer,
      skus_em_quebra     integer,
      skus_parados       integer,
      skus_stale         integer,
      dos_medio          numeric,
      turnover_medio     numeric
    )
    LANGUAGE sql
    SET search_path = public
    AS $$
      SELECT
        count(*)::integer                                        AS total_skus,
        sum(stock_value)                                         AS total_stock_value,
        count(*) FILTER (WHERE NOT has_cost)::integer            AS skus_sem_custo,
        count(*) FILTER (WHERE is_stockout_now)::integer         AS skus_em_quebra,
        count(*) FILTER (WHERE movement_class = 'dead')::integer AS skus_parados,
        count(*) FILTER (WHERE stock_status = 'stale')::integer  AS skus_stale,
        round(avg(dos), 1)                                       AS dos_medio,
        round(avg(turnover), 2)                                  AS turnover_medio
      FROM product_inventory_metrics
      WHERE tenant_id = p_tenant_id;
    $$;

    REVOKE ALL ON FUNCTION public.inventory_summary(uuid) FROM anon, authenticated, public;
    GRANT EXECUTE ON FUNCTION public.inventory_summary(uuid) TO service_role;
    ```

    Notas: `avg(dos)`/`avg(turnover)` ignoram NULLs automaticamente (média só dos produtos com métrica calculável — correto). `skus_em_quebra` conta `is_stockout_now = true` (NULL de 'unknown' não conta). NÃO adicionar SECURITY DEFINER.
  </action>
  <acceptance_criteria>
    - Arquivo contém `CREATE OR REPLACE FUNCTION product_stock_history(` com `RETURNS TABLE(day date, quantity numeric, qty_sold numeric)` e `generate_series`.
    - Contém `CREATE OR REPLACE FUNCTION inventory_summary(p_tenant_id uuid)` com os 8 nomes de coluna do contrato.
    - Ambas contêm `SET search_path = public` e NÃO contêm `SECURITY DEFINER`.
    - Ambas têm `REVOKE ALL ON FUNCTION ... FROM anon, authenticated, public` e `GRANT EXECUTE ... TO service_role`.
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000008_inventory_rpcs.sql"; grep -q "CREATE OR REPLACE FUNCTION product_stock_history(" "$f" && grep -q "RETURNS TABLE(day date, quantity numeric, qty_sold numeric)" "$f" && grep -q "generate_series(CURRENT_DATE - p_days" "$f" && grep -q "CREATE OR REPLACE FUNCTION inventory_summary(p_tenant_id uuid)" "$f" && ! grep -q "SECURITY DEFINER" "$f" && n=$(grep -c "SET search_path = public" "$f"); [ "$n" -eq 2 ] && for c in total_skus total_stock_value skus_sem_custo skus_em_quebra skus_parados skus_stale dos_medio turnover_medio; do grep -q "$c" "$f" || { echo "MISSING $c"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Ambas as RPCs criadas conforme contrato, SECURITY INVOKER, search_path fixo, EXECUTE só para service_role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamador da RPC → dados de inventário | RPC recebe p_tenant_id explícito; nenhuma inferência de tenant pela sessão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Elevation of Privilege | RPC exposta via /rest/v1/rpc a authenticated | mitigate | REVOKE EXECUTE FROM anon, authenticated, public + GRANT só a service_role. SECURITY INVOKER (não DEFINER) — sem escalonamento. |
| T-08-16 | Information Disclosure | IDOR cross-tenant via p_tenant_id | mitigate | Chamada só via service-role no Server Component, que passa `profile.tenant_id` autorizado; RPC filtra `WHERE tenant_id = p_tenant_id`. Padrão idêntico a store_daily_evolution_multi. |
| T-08-17 | Tampering | search_path mutável | mitigate | `SET search_path = public` em ambas (hardening P0-4). |
</threat_model>

<verification>
- product_stock_history retorna série contínua com dias sem snapshot (quantity NULL).
- inventory_summary retorna os 8 KPIs do contrato numa linha.
- Ambas SECURITY INVOKER, search_path fixo, EXECUTE restrito a service_role.
</verification>

<success_criteria>
- Contrato de saída da Fase 08 completo (2 tabelas + 4 views + 2 RPCs).
- Fase 09 pode ser executada contra estas assinaturas estáveis.
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-05-SUMMARY.md`
</output>
