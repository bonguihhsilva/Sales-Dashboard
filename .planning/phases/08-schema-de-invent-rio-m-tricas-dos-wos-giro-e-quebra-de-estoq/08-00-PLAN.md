---
phase: 08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md
autonomous: true
requirements: [D-03, D-07, D-08]
must_haves:
  truths:
    - "A taxa de sale_items com order_ref órfão (sem sales_records correspondente) está medida por tenant e documentada"
    - "O número de SKUs que ficam sem custo no dia 1 do seed está dimensionado"
    - "Existe uma decisão explícita registrada: excluir órfãos (D-08 como está) OU implementar fallback"
  artifacts:
    - path: ".planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md"
      provides: "Resultado dos diagnósticos R-01 (órfãos) e R-04 (cobertura do seed) + decisão"
      contains: "orphan_pct"
  key_links: []
---

<objective>
Rodar os diagnósticos read-only (R-01 órfãos de `order_ref`, R-04 cobertura do seed de `product_costs`) contra o banco de produção ANTES de escrever qualquer view. O resultado decide se `product_daily_sales` (Plano 02) pode excluir órfãos silenciosamente (D-08 como está) ou precisa de fallback de data.

Purpose: `product_daily_sales` junta `sale_items.order_id = sales_records.order_ref`. Se uma fração relevante de `sale_items` não casar, a série de venda diária subestima a venda e infla DoS/turnover para TODOS os produtos, silenciosamente (R-01). Esta é a única forma de detectar isso antes de construir a fase inteira em cima da junção.
Output: `08-DIAGNOSTICS.md` com as taxas medidas e uma decisão explícita.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-CONTEXT.md
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-RESEARCH.md

<interfaces>
Schema real confirmado (leitura direta das migrations):
- sale_items (tenant_id uuid, period_id int, vendor_id text, order_id text, product_code text, qty numeric, unit_price numeric, total_price numeric, unit_cost numeric, total_profit numeric) — UNIQUE(tenant_id, period_id, order_id, product_code)
- sales_records (tenant_id uuid, period_id int, ..., sale_date date, valor numeric, quantity numeric, order_ref text) — UNIQUE(tenant_id, period_id, order_ref)
- products (tenant_id uuid, period_id int, product_code text, name text, cost_price numeric, sale_price numeric, ...) — UNIQUE(tenant_id, period_id, product_code)
- periods (id int, tenant_id uuid, year int, month int, label text)
Junção canônica D-07: sale_items.order_id = sales_records.order_ref DENTRO do mesmo tenant_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medir taxa de órfãos order_ref (R-01) por tenant</name>
  <files>.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md</files>
  <read_first>
    - 08-RESEARCH.md seção "Diagnóstico de órfãos order_ref (R-01)" e "Open Questions" #1
    - 08-CONTEXT.md D-07, D-08, R-01
  </read_first>
  <action>
    Rodar via Supabase MCP `execute_sql` (SOMENTE LEITURA — nenhuma escrita, nenhuma migration) a query de diagnóstico verbatim do 08-RESEARCH.md:

    ```sql
    SELECT
      si.tenant_id,
      count(*) AS total_sale_items,
      count(*) FILTER (WHERE sr.order_ref IS NULL) AS orphan_items,
      round(
        100.0 * count(*) FILTER (WHERE sr.order_ref IS NULL) / NULLIF(count(*), 0),
        2
      ) AS orphan_pct
    FROM sale_items si
    LEFT JOIN sales_records sr
      ON sr.tenant_id = si.tenant_id
     AND sr.order_ref  = si.order_id
    GROUP BY si.tenant_id
    ORDER BY orphan_pct DESC;
    ```

    Também medir fan-out potencial (order_ref duplicado em sales_records, que inflaria a venda no JOIN):
    ```sql
    SELECT tenant_id, count(*) AS dup_order_refs
    FROM (
      SELECT tenant_id, order_ref, count(DISTINCT sale_date) AS n
      FROM sales_records
      GROUP BY tenant_id, order_ref
      HAVING count(DISTINCT sale_date) > 1
    ) x
    GROUP BY tenant_id;
    ```

    Criar `08-DIAGNOSTICS.md` registrando:
    - Tabela com `orphan_pct` por tenant.
    - Resultado do fan-out.
    - DECISÃO EXPLÍCITA seguindo o critério do 08-RESEARCH Open Questions #1: se `orphan_pct < 1%` → seguir D-08 como está (INNER JOIN exclui órfãos; Plano 02 usa `DISTINCT ON` em sales_records para eliminar fan-out). Se `orphan_pct >= 1%` → registrar como BLOCKER e propor uma das opções do R-01 (a: fallback de data via `periods` do item; b: aceitar e expor lacuna numa métrica de saúde) para o dono decidir.
  </action>
  <acceptance_criteria>
    - `08-DIAGNOSTICS.md` existe e contém a string `orphan_pct` com valores numéricos por tenant.
    - Contém uma linha começando com `DECISÃO:` declarando "excluir órfãos" ou "fallback necessário".
    - grep confirma: `grep -c "orphan_pct" 08-DIAGNOSTICS.md` retorna >= 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "orphan_pct" ".planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md" && grep -q "DECISÃO:" ".planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md" && echo OK</automated>
  </verify>
  <done>Taxa de órfãos medida por tenant, fan-out verificado, decisão sobre D-08 registrada.</done>
</task>

<task type="auto">
  <name>Task 2: Dimensionar cobertura do seed de product_costs (R-04)</name>
  <files>.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md</files>
  <read_first>
    - 08-RESEARCH.md "Open Questions" #3 e "Seed inicial de product_costs (D-03)"
    - 08-CONTEXT.md D-03, R-04
  </read_first>
  <action>
    Rodar via Supabase MCP `execute_sql` (SOMENTE LEITURA) a comparação de cobertura do seed — quantos SKUs o período mais recente cobre vs. o histórico completo:

    ```sql
    WITH latest_period AS (
      SELECT DISTINCT ON (tenant_id) tenant_id, id AS period_id
      FROM periods
      ORDER BY tenant_id, year DESC, month DESC
    )
    SELECT
      p.tenant_id,
      count(DISTINCT p.product_code) FILTER (WHERE p.period_id = lp.period_id) AS skus_periodo_recente,
      count(DISTINCT p.product_code)                                            AS skus_historico_total,
      count(DISTINCT p.product_code) FILTER (WHERE p.cost_price IS NULL AND p.period_id = lp.period_id) AS skus_recente_sem_custo
    FROM products p
    JOIN latest_period lp ON lp.tenant_id = p.tenant_id
    GROUP BY p.tenant_id;
    ```

    Também quantos product_code vendidos (em sale_items) na janela longa NÃO existem no período mais recente de products (ficariam sem custo no dia 1):
    ```sql
    WITH latest_period AS (
      SELECT DISTINCT ON (tenant_id) tenant_id, id AS period_id
      FROM periods ORDER BY tenant_id, year DESC, month DESC
    )
    SELECT si.tenant_id, count(DISTINCT si.product_code) AS vendidos_sem_catalogo_recente
    FROM sale_items si
    LEFT JOIN products p
      ON p.tenant_id = si.tenant_id
     AND p.product_code = si.product_code
     AND p.period_id = (SELECT period_id FROM latest_period lp WHERE lp.tenant_id = si.tenant_id)
    WHERE p.product_code IS NULL
    GROUP BY si.tenant_id;
    ```

    Adicionar ao `08-DIAGNOSTICS.md` uma seção "R-04 — Cobertura do seed" com os números e a conclusão: o seed do período mais recente é suficiente para o MVP (métricas monetárias NULL para o gap são comportamento correto por D-18), OU o dono deve subir um catálogo atualizado antes de confiar em `stock_value`/`turnover`.
  </action>
  <acceptance_criteria>
    - `08-DIAGNOSTICS.md` contém a seção "R-04" com `skus_periodo_recente` e `vendidos_sem_catalogo_recente`.
    - grep confirma presença de `R-04` e `skus_periodo_recente`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "R-04" ".planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md" && grep -q "skus_periodo_recente" ".planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md" && echo OK</automated>
  </verify>
  <done>Cobertura do seed dimensionada; conclusão sobre suficiência do seed registrada em 08-DIAGNOSTICS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agente executor → banco de produção | Todas as queries desta fase são SELECT read-only; nenhuma escrita cruza a fronteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Diagnóstico via MCP execute_sql | mitigate | Somente `SELECT` — nenhum `INSERT/UPDATE/DELETE/DDL`. Revisão manual da query antes de rodar. |
| T-08-02 | Information Disclosure | 08-DIAGNOSTICS.md commitado | accept | Só agrega contagens/percentuais por tenant_id (uuid); nenhum dado de cliente/vendedor ou PII. |
</threat_model>

<verification>
- `08-DIAGNOSTICS.md` existe com resultados de R-01 e R-04.
- Decisão sobre exclusão de órfãos (D-08) registrada explicitamente.
- Nenhuma escrita ocorreu no banco.
</verification>

<success_criteria>
- Taxa de órfãos `order_ref` medida por tenant e comparada ao limiar de 1%.
- Fan-out de `order_ref` verificado.
- Cobertura do seed de `product_costs` dimensionada.
- Decisão de design para `product_daily_sales` (Plano 02) desbloqueada.
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-00-SUMMARY.md`
</output>
