---
phase: 08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - supabase/migrations/20260720000003_product_daily_sales_view.sql
  - supabase/migrations/20260720000004_product_sales_ranking_view.sql
autonomous: true
requirements: [D-07, D-08, D-09, D-21, D-23]
must_haves:
  truths:
    - "product_daily_sales expõe uma linha por (tenant_id, product_code, sale_date) com qty_sold, revenue, cogs, profit"
    - "Linhas de sale_items sem sales_records correspondente (órfãs) são excluídas da view"
    - "product_sales_ranking agrega venda por produto E loja, sem vendor_id, com profit NULL quando custo ausente"
    - "Ambas as views são revogadas de anon/authenticated e concedidas a service_role"
  artifacts:
    - path: "supabase/migrations/20260720000003_product_daily_sales_view.sql"
      provides: "view base product_daily_sales (D-08)"
      contains: "CREATE OR REPLACE VIEW product_daily_sales"
    - path: "supabase/migrations/20260720000004_product_sales_ranking_view.sql"
      provides: "view product_sales_ranking (D-23)"
      contains: "CREATE OR REPLACE VIEW product_sales_ranking"
  key_links:
    - from: "product_daily_sales"
      to: "sale_items JOIN sales_records"
      via: "sale_items.order_id = sales_records.order_ref (mesmo tenant_id)"
      pattern: "order_ref = si.order_id"
---

<objective>
Criar as duas views de VENDA da fase: `product_daily_sales` (D-08 — série diária por produto, insumo de todas as métricas de velocidade) e `product_sales_ranking` (D-23 — ranking de vendas por produto E loja, a ÚNICA view da fase com `store`). Ambas nascem da junção canônica D-07 `sale_items.order_id = sales_records.order_ref`.

Purpose: `sale_items` tem `period_id` mas NÃO tem `sale_date` (D-07) — a única forma de datar uma venda por item é via `sales_records`. `product_daily_sales` materializa logicamente essa junção; `product_sales_ranking` é o recorte por loja para o ranking da Fase 09 (D-17 da Fase 09).
Output: 2 migrations SQL. NÃO aplicadas ao banco.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-CONTEXT.md
@.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-DIAGNOSTICS.md
@supabase/migrations/20260626171308_profit_commission.sql
@supabase/migrations/20260601000019_p0_security_closeout.sql

<interfaces>
Colunas relevantes:
- sale_items: tenant_id, order_id (text), product_code, qty (numeric), unit_price, total_price, unit_cost (nullable), total_profit (nullable)
- sales_records: tenant_id, order_ref (text), sale_date (date), store (text)
- products: tenant_id, period_id, product_code, name — para COALESCE de nome (D-19)
- product_costs: tenant_id, product_code, name — precedência de nome
- inventory_settings: long_window_days (default 90) — janela L do ranking

Contrato D-08: product_daily_sales(tenant_id uuid, product_code text, sale_date date, qty_sold numeric, revenue numeric, cogs numeric, profit numeric).
Contrato D-23: product_sales_ranking(tenant_id, product_code, product_name, store, qty_sold, revenue, profit, window_days). vendor_id NUNCA aparece.

Grants padrão (verbatim de p0_security_closeout.sql):
```sql
REVOKE ALL ON public.<view> FROM anon, authenticated;
GRANT SELECT ON public.<view> TO service_role;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: View product_daily_sales (D-07, D-08, D-09) + grants</name>
  <files>supabase/migrations/20260720000003_product_daily_sales_view.sql</files>
  <read_first>
    - 08-CONTEXT.md D-07, D-08, D-09 e a tabela do contrato para product_daily_sales
    - 08-DIAGNOSTICS.md (decisão sobre órfãos — se BLOCKER foi levantado, aplicar a opção decidida antes de escrever esta view)
    - 08-RESEARCH.md "Anti-Patterns" (JOIN sempre com tenant_id nas duas pontas)
  </read_first>
  <action>
    IMPORTANTE: se 08-DIAGNOSTICS.md registrou `DECISÃO: fallback necessário` (orphan_pct >= 1%), PARE e escale ao dono antes de prosseguir — a view abaixo assume "excluir órfãos" (INNER JOIN). Se `DECISÃO: excluir órfãos`, seguir.

    Criar `supabase/migrations/20260720000003_product_daily_sales_view.sql`. Usar um CTE `order_dates` com `DISTINCT ON (tenant_id, order_ref)` para pegar UMA `sale_date` por order_ref e eliminar fan-out (order_ref com múltiplas linhas em sales_records inflaria a soma). O INNER JOIN exclui órfãos (D-08). `cogs` = `sum(qty*unit_cost)`, `profit` = `sum(total_profit)` — custo congelado da venda, NUNCA de product_costs (D-08). Janelas de data ficam para as views downstream — esta view é a série completa (D-09: datas reais, sem proxy de period_id):

    ```sql
    -- Phase 08: série de venda diária por produto (D-07/D-08/D-09).
    -- Insumo de todas as métricas de velocidade. Sem loja (métricas são tenant-wide).
    CREATE OR REPLACE VIEW product_daily_sales AS
    WITH order_dates AS (
      -- Uma sale_date por order_ref (elimina fan-out de order_ref duplicado).
      SELECT DISTINCT ON (sr.tenant_id, sr.order_ref)
        sr.tenant_id, sr.order_ref, sr.sale_date
      FROM sales_records sr
      ORDER BY sr.tenant_id, sr.order_ref, sr.sale_date
    )
    SELECT
      si.tenant_id,
      si.product_code,
      od.sale_date,
      sum(si.qty)                 AS qty_sold,
      sum(si.total_price)         AS revenue,
      sum(si.qty * si.unit_cost)  AS cogs,     -- NULL nas linhas sem unit_cost
      sum(si.total_profit)        AS profit    -- NULL só se todas as linhas do grupo forem NULL
    FROM sale_items si
    JOIN order_dates od
      ON  od.tenant_id = si.tenant_id
      AND od.order_ref = si.order_id           -- órfãos (sem match) são EXCLUÍDOS (D-08)
    GROUP BY si.tenant_id, si.product_code, od.sale_date;

    REVOKE ALL ON public.product_daily_sales FROM anon, authenticated;
    GRANT SELECT ON public.product_daily_sales TO service_role;
    ```
  </action>
  <acceptance_criteria>
    - Arquivo contém `CREATE OR REPLACE VIEW product_daily_sales` com as 7 colunas do contrato (`tenant_id, product_code, sale_date, qty_sold, revenue, cogs, profit`).
    - JOIN inclui `tenant_id` nas duas pontas (`od.tenant_id = si.tenant_id`).
    - Contém `REVOKE ALL ON public.product_daily_sales FROM anon, authenticated` e `GRANT SELECT ON public.product_daily_sales TO service_role`.
    - NÃO referencia product_costs (cogs vem de sale_items).
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000003_product_daily_sales_view.sql"; grep -q "CREATE OR REPLACE VIEW product_daily_sales" "$f" && grep -q "od.tenant_id = si.tenant_id" "$f" && grep -q "REVOKE ALL ON public.product_daily_sales FROM anon, authenticated" "$f" && grep -q "GRANT SELECT ON public.product_daily_sales TO service_role" "$f" && ! grep -q "product_costs" "$f" && echo OK</automated>
  </verify>
  <done>product_daily_sales existe com shape do contrato, exclui órfãos, cogs/profit de sale_items, grants restritos.</done>
</task>

<task type="auto">
  <name>Task 2: View product_sales_ranking (D-23) + grants</name>
  <files>supabase/migrations/20260720000004_product_sales_ranking_view.sql</files>
  <read_first>
    - 08-CONTEXT.md D-23 e a tabela do contrato para product_sales_ranking (a nota "vendor_id NUNCA aparece")
    - 08-CONTEXT.md D-19 (COALESCE de nome)
  </read_first>
  <action>
    Criar `supabase/migrations/20260720000004_product_sales_ranking_view.sql`. Esta é a ÚNICA view da fase com `store` — vem de `sales_records.store`. `vendor_id` NÃO existe na view (fronteira estrutural D-13 da Fase 09). Nome resolvido por `COALESCE(product_costs.name, products.name mais recente, product_code)` (D-19). Janela = `long_window_days` (default 90) via LEFT JOIN em inventory_settings (D-06):

    ```sql
    -- Phase 08: ranking de VENDAS por produto e loja (D-23).
    -- ÚNICA view da fase com store. Sem vendor_id. Puramente venda — nenhuma
    -- métrica de estoque cruza aqui.
    CREATE OR REPLACE VIEW product_sales_ranking AS
    WITH settings AS (
      SELECT t.id AS tenant_id, COALESCE(s.long_window_days, 90) AS l
      FROM tenants t
      LEFT JOIN inventory_settings s ON s.tenant_id = t.id
    ),
    order_meta AS (
      SELECT DISTINCT ON (sr.tenant_id, sr.order_ref)
        sr.tenant_id, sr.order_ref, sr.sale_date, sr.store
      FROM sales_records sr
      ORDER BY sr.tenant_id, sr.order_ref, sr.sale_date
    ),
    prod_name AS (
      SELECT DISTINCT ON (p.tenant_id, p.product_code)
        p.tenant_id, p.product_code, p.name
      FROM products p
      ORDER BY p.tenant_id, p.product_code, p.period_id DESC
    )
    SELECT
      si.tenant_id,
      si.product_code,
      COALESCE(pc.name, pn.name, si.product_code) AS product_name,
      om.store,
      sum(si.qty)          AS qty_sold,
      sum(si.total_price)  AS revenue,
      sum(si.total_profit) AS profit,
      st.l                 AS window_days
    FROM sale_items si
    JOIN order_meta om ON om.tenant_id = si.tenant_id AND om.order_ref = si.order_id
    JOIN settings st   ON st.tenant_id = si.tenant_id
    LEFT JOIN product_costs pc ON pc.tenant_id = si.tenant_id AND pc.product_code = si.product_code
    LEFT JOIN prod_name pn     ON pn.tenant_id = si.tenant_id AND pn.product_code = si.product_code
    WHERE om.sale_date >= CURRENT_DATE - st.l
    GROUP BY si.tenant_id, si.product_code,
             COALESCE(pc.name, pn.name, si.product_code), om.store, st.l;

    REVOKE ALL ON public.product_sales_ranking FROM anon, authenticated;
    GRANT SELECT ON public.product_sales_ranking TO service_role;
    ```
  </action>
  <acceptance_criteria>
    - Arquivo contém `CREATE OR REPLACE VIEW product_sales_ranking` com colunas `store` e `window_days`.
    - NÃO contém a string `vendor_id` em lugar nenhum do arquivo.
    - Contém os grants REVOKE/GRANT para product_sales_ranking.
    - Contém `COALESCE(pc.name, pn.name, si.product_code)` (D-19).
  </acceptance_criteria>
  <verify>
    <automated>f="supabase/migrations/20260720000004_product_sales_ranking_view.sql"; grep -q "CREATE OR REPLACE VIEW product_sales_ranking" "$f" && grep -q "om.store" "$f" && ! grep -q "vendor_id" "$f" && grep -q "REVOKE ALL ON public.product_sales_ranking FROM anon, authenticated" "$f" && grep -q "COALESCE(pc.name, pn.name, si.product_code)" "$f" && echo OK</automated>
  </verify>
  <done>product_sales_ranking existe com store, sem vendor_id, nome via COALESCE, janela L, grants restritos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente authenticated → views de venda | Views expõem receita/lucro por produto — devem ser inacessíveis a authenticated direto |
| tenant A → dados de venda do tenant B | Todo JOIN carrega tenant_id nas duas pontas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | product_daily_sales / product_sales_ranking (receita/lucro) | mitigate | REVOKE ALL FROM anon, authenticated + GRANT SELECT TO service_role em cada view (D-21). Leitura só via createAdminClient() + .eq('tenant_id',...). |
| T-08-07 | Information Disclosure | vendor_id vazando no ranking de compras | mitigate | Coluna vendor_id não existe na view (fronteira estrutural D-13 da Fase 09). |
| T-08-08 | Tampering | cross-tenant via order_ref colidente | mitigate | JOIN inclui tenant_id nas duas pontas (`od.tenant_id = si.tenant_id`) — order_ref sozinho nunca casa cross-tenant. |
</threat_model>

<verification>
- Duas views criadas com grants D-21.
- product_daily_sales: shape do contrato, exclui órfãos, sem fan-out, cogs/profit de sale_items.
- product_sales_ranking: com store, sem vendor_id, nome via COALESCE, janela L configurável.
</verification>

<success_criteria>
- Insumo de velocidade (product_daily_sales) disponível para as views de métrica (wave 3) e para product_stock_history (wave 4).
- Ranking de venda por loja disponível para a Fase 09.
</success_criteria>

<output>
After completion, create `.planning/phases/08-schema-de-invent-rio-m-tricas-dos-wos-giro-e-quebra-de-estoq/08-02-SUMMARY.md`
</output>
