---
phase: "06"
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/lib/export-xlsx.ts
  - src/lib/export-pdf.ts
  - src/app/api/admin/relatorio-excel/route.ts
  - src/app/dashboard/ExportButton.tsx
  - src/app/dashboard/relatorios/page.tsx
  - src/__tests__/export.test.ts
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "ExportButton mostra seletor de formato CSV / XLSX / PDF com tab ativo destacado"
    - "Download de CSV funciona como antes (sem regressão)"
    - "Download de XLSX gera arquivo .xlsx com headers em negrito e larguras de coluna configuradas"
    - "Download de PDF gera arquivo .pdf com header 'GDS Frame — Relatório de Comissões', período e tabela de dados"
    - "Rota /api/admin/relatorio-excel aceita ?format=csv|xlsx|pdf e retorna o arquivo binário correto"
    - "Testes unitários de export-xlsx e export-pdf passam"
  artifacts:
    - path: "src/lib/export-xlsx.ts"
      provides: "generateCommissionXlsx(vendors, periodLabel) → Buffer"
      exports: ["generateCommissionXlsx"]
    - path: "src/lib/export-pdf.ts"
      provides: "generateCommissionPdf(vendors, periodLabel) → Promise<Buffer>"
      exports: ["generateCommissionPdf"]
    - path: "src/app/api/admin/relatorio-excel/route.ts"
      provides: "GET com ?format=csv|xlsx|pdf retornando arquivo binário"
    - path: "src/app/dashboard/ExportButton.tsx"
      provides: "Seletor de formato segmentado + download do formato escolhido"
  key_links:
    - from: "src/app/dashboard/ExportButton.tsx"
      to: "/api/admin/relatorio-excel?period=N&format=xlsx"
      via: "fetch com ?format= dinâmico"
      pattern: "format=\\$\\{selectedFormat\\}"
    - from: "src/app/api/admin/relatorio-excel/route.ts"
      to: "src/lib/export-xlsx.ts"
      via: "import generateCommissionXlsx"
      pattern: "generateCommissionXlsx"
    - from: "src/app/api/admin/relatorio-excel/route.ts"
      to: "src/lib/export-pdf.ts"
      via: "import generateCommissionPdf"
      pattern: "generateCommissionPdf"
---

<objective>
Implementar geração de relatórios em XLSX (formatado, com bold nos headers) e PDF (tabela simples com header GDS incluindo logo se disponível), e atualizar o ExportButton para permitir seleção de formato antes do download.

Purpose: Gerentes precisam de relatórios no formato que seus sistemas aceitam — XLSX para Excel/planilhas, PDF para compartilhamento por email. O CSV atual continua disponível sem regressão.

Output: Duas novas libs de export (`export-xlsx.ts`, `export-pdf.ts`), rota atualizada aceitando `?format=`, ExportButton com seletor de formato segmentado, e testes unitários cobrindo os dois novos formatos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-CONTEXT.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-RESEARCH.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-UI-SPEC.md

<interfaces>
<!-- Contrato da rota existente (fonte de dados) -->
De src/app/api/admin/relatorio-excel/route.ts (resposta atual — JSON):
```typescript
// GET retorna JSON com:
{
  period: string,       // label do período ex: "Maio 2026"
  vendors: Array<{
    nome: string,
    loja: string,
    total_vendido: number,
    comissao_pct: number,
    comissao: number,
    meta_atingida: string,
    bonus: number,
    total_ganhos: number,
    status_aprovacao: string
  }>
}
// NOVO: quando ?format=xlsx ou ?format=pdf, retornar Response binário em vez de JSON
```

<!-- Interface a criar para export-xlsx.ts e export-pdf.ts -->
Tipo VendorReport a definir em export-xlsx.ts e export-pdf.ts:
```typescript
export interface VendorReport {
  nome: string
  loja: string
  total_vendido: number
  comissao_pct: number
  comissao: number
  meta_atingida: string
  bonus: number
  total_ganhos: number
  status_aprovacao: string
}
```

<!-- ExportButton atual -->
De src/app/dashboard/ExportButton.tsx:
```typescript
// Estado atual: apenas CSV, sem seletor de formato
// NOVO: adicionar useState<'csv' | 'xlsx' | 'pdf'>('csv') para selectedFormat
// NOVO: passar &format=${selectedFormat} no fetch
// NOVO: ajustar blob type e filename conforme formato
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar export-xlsx.ts e export-pdf.ts com testes unitários</name>
  <files>
    src/lib/export-xlsx.ts
    src/lib/export-pdf.ts
    src/__tests__/export.test.ts
  </files>
  <read_first>
    - src/__tests__/export.test.ts (stubs it.todo criados no plano 01 — substituir pelos testes reais)
    - src/lib/server-parser.ts (observar o import de 'xlsx' para NÃO usar o mesmo módulo em export-xlsx.ts — usar 'xlsx-js-style')
    - package.json (verificar se xlsx-js-style e pdfkit já foram instalados)
  </read_first>
  <behavior>
    - Test export-xlsx-buffer: generateCommissionXlsx(vendors, 'Maio 2026') retorna Buffer com length > 0
    - Test export-xlsx-headers-bold: fazer roundtrip Buffer→XLSX: célula A1 tem .s.font.bold === true (via xlsx-js-style.read com cellStyles:true)
    - Test export-xlsx-sheet-name: nome da sheet = 'Maio 2026' (truncado a 31 chars se necessário)
    - Test export-xlsx-col-count: primeira linha tem 9 colunas (Vendedor, Loja, Total Vendido, Meta Atingida, Comissão %, Comissão Base, Bônus, Total Ganhos, Status)
    - Test export-pdf-buffer: generateCommissionPdf(vendors, 'Maio 2026') retorna Promise<Buffer> com length > 0
    - Test export-pdf-has-pdf-header: Buffer começa com `%PDF` (magic bytes do formato PDF)
  </behavior>
  <action>
1. Instalar dependências (se ainda não instaladas):
   ```bash
   npm install xlsx-js-style pdfkit
   npm install --save-dev @types/pdfkit
   ```

2. Criar src/lib/export-xlsx.ts:
   ```typescript
   // IMPORTANT: importar de 'xlsx-js-style', NUNCA de 'xlsx' neste arquivo
   // xlsx (community 0.18.5) não persiste cell styles — xlsx-js-style é o fork correto
   import XLSXStyle from 'xlsx-js-style'

   export interface VendorReport {
     nome: string
     loja: string
     total_vendido: number
     comissao_pct: number
     comissao: number
     meta_atingida: string
     bonus: number
     total_ganhos: number
     status_aprovacao: string
   }

   const HEADER_STYLE = {
     font: { bold: true },
     fill: { fgColor: { rgb: 'F5F5F5' } },
   }

   const HEADERS = [
     'Vendedor', 'Loja', 'Total Vendido ($)', 'Meta Atingida',
     'Comissão %', 'Comissão ($)', 'Bônus ($)', 'Total Ganhos ($)', 'Status',
   ]

   const COL_WIDTHS = [
     { wch: 24 }, { wch: 16 }, { wch: 18 }, { wch: 18 },
     { wch: 14 }, { wch: 16 }, { wch: 14 }, { wch: 18 }, { wch: 14 },
   ]

   function fmt(n: number): string {
     return n.toFixed(2).replace('.', ',')
   }

   function fmtPct(n: number): string {
     return (n * 100).toFixed(1).replace('.', ',') + '%'
   }

   function vendorToRow(v: VendorReport): (string | number)[] {
     return [
       v.nome, v.loja,
       v.total_vendido,
       v.meta_atingida,
       fmtPct(v.comissao_pct),
       v.comissao,
       v.bonus,
       v.total_ganhos,
       v.status_aprovacao,
     ]
   }

   export function generateCommissionXlsx(vendors: VendorReport[], periodLabel: string): Buffer {
     const rows: (string | number)[][] = [HEADERS, ...vendors.map(vendorToRow)]
     const ws = XLSXStyle.utils.aoa_to_sheet(rows)

     // Aplicar bold em todos os headers (linha 0)
     HEADERS.forEach((_, i) => {
       const cellRef = XLSXStyle.utils.encode_cell({ r: 0, c: i })
       if (ws[cellRef]) ws[cellRef].s = HEADER_STYLE
     })

     ws['!cols'] = COL_WIDTHS

     // Linha de totais
     const totalRow = [
       'TOTAL', '',
       vendors.reduce((s, v) => s + v.total_vendido, 0),
       '', '',
       vendors.reduce((s, v) => s + v.comissao, 0),
       vendors.reduce((s, v) => s + v.bonus, 0),
       vendors.reduce((s, v) => s + v.total_ganhos, 0),
       '',
     ]
     XLSXStyle.utils.sheet_add_aoa(ws, [totalRow], { origin: -1 })

     const wb = XLSXStyle.utils.book_new()
     const sheetName = periodLabel.slice(0, 31)
     XLSXStyle.utils.book_append_sheet(wb, ws, sheetName)

     return XLSXStyle.write(wb, { type: 'buffer', bookType: 'xlsx' }) as Buffer
   }
   ```

3. Criar src/lib/export-pdf.ts:
   ```typescript
   import PDFDocument from 'pdfkit'
   import fs from 'fs'
   import path from 'path'
   import type { VendorReport } from './export-xlsx'

   // Re-exportar VendorReport para que importadores não precisem de dois imports
   export type { VendorReport }

   interface ColDef {
     label: string
     width: number
     align: 'left' | 'right'
     value: (v: VendorReport) => string
   }

   const fmt = (n: number) => n.toFixed(2).replace('.', ',')
   const fmtPct = (n: number) => (n * 100).toFixed(1) + '%'

   const COLS: ColDef[] = [
     { label: 'Vendedor',      width: 130, align: 'left',  value: v => v.nome },
     { label: 'Loja',          width: 80,  align: 'left',  value: v => v.loja },
     { label: 'Total ($)',     width: 80,  align: 'right', value: v => fmt(v.total_vendido) },
     { label: 'Meta',          width: 80,  align: 'left',  value: v => v.meta_atingida },
     { label: 'Com. %',        width: 55,  align: 'right', value: v => fmtPct(v.comissao_pct) },
     { label: 'Comissão ($)',  width: 80,  align: 'right', value: v => fmt(v.comissao) },
     { label: 'Bônus ($)',     width: 70,  align: 'right', value: v => fmt(v.bonus) },
     { label: 'Total ($)',     width: 80,  align: 'right', value: v => fmt(v.total_ganhos) },
     { label: 'Status',        width: 70,  align: 'left',  value: v => v.status_aprovacao },
   ]

   const PAGE_MARGIN = 40
   const ROW_HEIGHT = 18
   const HEADER_HEIGHT = 22
   const LOGO_PATH = path.join(process.cwd(), 'public', 'logo.png')

   export async function generateCommissionPdf(
     vendors: VendorReport[],
     periodLabel: string
   ): Promise<Buffer> {
     return new Promise((resolve, reject) => {
       const doc = new PDFDocument({ margin: PAGE_MARGIN, size: 'A4', layout: 'landscape' })
       const chunks: Buffer[] = []
       doc.on('data', (chunk: Buffer) => chunks.push(chunk))
       doc.on('end', () => resolve(Buffer.concat(chunks)))
       doc.on('error', reject)

       // ── Header (D-12: logo GDS + título + período) ────
       // Logo: incluído se public/logo.png existir — fallback gracioso se ausente
       const logoExists = fs.existsSync(LOGO_PATH)
       if (logoExists) {
         doc.image(LOGO_PATH, PAGE_MARGIN, PAGE_MARGIN, { height: 36 })
         doc.moveDown(0.5)
       }
       doc
         .fontSize(16)
         .font('Helvetica-Bold')
         .text('GDS Frame — Relatório de Comissões', { align: 'center' })
       doc
         .fontSize(11)
         .font('Helvetica')
         .text(periodLabel, { align: 'center' })
       doc.moveDown(1.2)

       // ── Cabeçalho da tabela ────
       let x = PAGE_MARGIN
       const tableTop = doc.y

       doc.fontSize(9).font('Helvetica-Bold')
       doc.rect(x, tableTop, COLS.reduce((s, c) => s + c.width, 0), HEADER_HEIGHT).fill('#E8E8E8')
       doc.fillColor('black')

       COLS.forEach(col => {
         doc.text(col.label, x + 3, tableTop + 5, {
           width: col.width - 6,
           align: col.align,
           lineBreak: false,
         })
         x += col.width
       })

       // ── Linhas de dados ────
       doc.font('Helvetica').fontSize(8)
       vendors.forEach((vendor, idx) => {
         const rowY = tableTop + HEADER_HEIGHT + idx * ROW_HEIGHT
         // Zebra alternado suave
         if (idx % 2 === 0) {
           doc.rect(PAGE_MARGIN, rowY, COLS.reduce((s, c) => s + c.width, 0), ROW_HEIGHT).fill('#F9F9F9')
           doc.fillColor('black')
         }

         let cx = PAGE_MARGIN
         COLS.forEach(col => {
           doc.text(col.value(vendor), cx + 3, rowY + 4, {
             width: col.width - 6,
             align: col.align,
             lineBreak: false,
           })
           cx += col.width
         })
       })

       // ── Rodapé ────
       const footerY = tableTop + HEADER_HEIGHT + vendors.length * ROW_HEIGHT + 10
       doc
         .fontSize(8)
         .font('Helvetica')
         .fillColor('#888888')
         .text(`Gerado em ${new Date().toLocaleDateString('pt-BR')} — GDS Frame Dashboard`, PAGE_MARGIN, footerY)

       doc.end()
     })
   }
   ```

4. Atualizar src/__tests__/export.test.ts — substituir os it.todo pelos testes reais:
   ```typescript
   import { describe, it, expect } from 'vitest'
   import { generateCommissionXlsx, type VendorReport } from '@/lib/export-xlsx'
   import { generateCommissionPdf } from '@/lib/export-pdf'
   import XLSXStyle from 'xlsx-js-style'

   const SAMPLE_VENDORS: VendorReport[] = [
     {
       nome: 'João Silva', loja: 'Jebai',
       total_vendido: 50000, comissao_pct: 0.03, comissao: 1500,
       meta_atingida: '2ª meta', bonus: 200, total_ganhos: 1700,
       status_aprovacao: 'Aprovada',
     },
     {
       nome: 'Maria Santos', loja: 'Pajé 1',
       total_vendido: 30000, comissao_pct: 0.02, comissao: 600,
       meta_atingida: '1ª meta', bonus: 0, total_ganhos: 600,
       status_aprovacao: 'Pendente',
     },
   ]

   describe('export-xlsx', () => {
     it('retorna Buffer não-vazio', () => {
       const buf = generateCommissionXlsx(SAMPLE_VENDORS, 'Maio 2026')
       expect(buf).toBeInstanceOf(Buffer)
       expect(buf.length).toBeGreaterThan(1000)
     })

     it('headers têm bold via roundtrip', () => {
       const buf = generateCommissionXlsx(SAMPLE_VENDORS, 'Maio 2026')
       const wb = XLSXStyle.read(buf, { type: 'buffer', cellStyles: true })
       const ws = wb.Sheets[wb.SheetNames[0]]
       const a1 = ws['A1']
       expect(a1?.s?.font?.bold).toBe(true)
     })

     it('nome da sheet = periodLabel (truncado a 31 chars)', () => {
       const buf = generateCommissionXlsx(SAMPLE_VENDORS, 'Maio 2026')
       const wb = XLSXStyle.read(buf, { type: 'buffer' })
       expect(wb.SheetNames[0]).toBe('Maio 2026')
     })

     it('primeira linha tem 9 colunas', () => {
       const buf = generateCommissionXlsx(SAMPLE_VENDORS, 'Maio 2026')
       const wb = XLSXStyle.read(buf, { type: 'buffer' })
       const ws = wb.Sheets[wb.SheetNames[0]]
       const data = XLSXStyle.utils.sheet_to_json(ws, { header: 1 }) as unknown[][]
       expect(data[0]).toHaveLength(9)
     })
   })

   describe('export-pdf', () => {
     it('retorna Promise<Buffer> não-vazio', async () => {
       const buf = await generateCommissionPdf(SAMPLE_VENDORS, 'Maio 2026')
       expect(buf).toBeInstanceOf(Buffer)
       expect(buf.length).toBeGreaterThan(1000)
     })

     it('começa com magic bytes %PDF', async () => {
       const buf = await generateCommissionPdf(SAMPLE_VENDORS, 'Maio 2026')
       expect(buf.slice(0, 4).toString('ascii')).toBe('%PDF')
     })
   })
   ```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/export.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - `npm install xlsx-js-style pdfkit` conclui sem erros
    - Todos os 6 testes de export.test.ts passam (GREEN)
    - `grep -n "export function generateCommissionXlsx" src/lib/export-xlsx.ts` encontra a função
    - `grep -n "export async function generateCommissionPdf" src/lib/export-pdf.ts` encontra a função
    - `grep -n "import XLSXStyle from 'xlsx-js-style'" src/lib/export-xlsx.ts` confirma que NÃO usa 'xlsx' para geração
    - `grep -n "fs.existsSync" src/lib/export-pdf.ts` confirma fallback gracioso de logo (D-12)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar rota relatorio-excel e ExportButton com seleção de formato</name>
  <files>
    src/app/api/admin/relatorio-excel/route.ts
    src/app/dashboard/ExportButton.tsx
    src/app/dashboard/relatorios/page.tsx
  </files>
  <read_first>
    - src/app/api/admin/relatorio-excel/route.ts (OBRIGATÓRIO ler antes — lógica de busca de dados será reusada)
    - src/app/dashboard/ExportButton.tsx (OBRIGATÓRIO ler antes — estado atual e estilos a preservar)
    - src/app/dashboard/relatorios/page.tsx (OBRIGATÓRIO ler antes — grid atual a atualizar para 3 colunas)
    - src/lib/export-xlsx.ts (recém criada na Tarefa 1 — ler para entender o contrato VendorReport)
    - src/lib/export-pdf.ts (recém criada na Tarefa 1)
    - .planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-UI-SPEC.md (contratos de UI para ExportButton e grid da página)
  </read_first>
  <action>
**Modificação 1 — src/app/api/admin/relatorio-excel/route.ts:**

A rota GET atual retorna JSON. Para XLSX e PDF, deve retornar Response binário com headers corretos.

Adicionar leitura do parâmetro `format`:
```typescript
const format = (searchParams.get('format') ?? 'csv') as 'csv' | 'xlsx' | 'pdf'
```

Após a construção do array `vendors` (antes do return atual), adicionar branch por formato:

```typescript
// Importar no topo do arquivo (adicionar aos imports existentes):
import { generateCommissionXlsx } from '@/lib/export-xlsx'
import { generateCommissionPdf } from '@/lib/export-pdf'
import type { VendorReport } from '@/lib/export-xlsx'
```

No final do handler, substituir o `return NextResponse.json({...})` atual por:

```typescript
const vendorReports: VendorReport[] = vendors.map(v => ({
  nome: v.nome,
  loja: v.loja,
  total_vendido: v.total_vendido,
  comissao_pct: v.comissao_pct,
  comissao: v.comissao,
  meta_atingida: v.meta_atingida,
  bonus: v.bonus,
  total_ganhos: v.total_ganhos,
  status_aprovacao: v.status_aprovacao,
}))

const periodLabelSafe = (period?.label ?? `Período ${periodId}`)
  .toLowerCase().replace(/\s+/g, '-')

if (format === 'xlsx') {
  const buffer = generateCommissionXlsx(vendorReports, period?.label ?? `Período ${periodId}`)
  return new Response(buffer, {
    headers: {
      'Content-Type': 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
      'Content-Disposition': `attachment; filename="comissoes-${periodLabelSafe}.xlsx"`,
    },
  })
}

if (format === 'pdf') {
  const buffer = await generateCommissionPdf(vendorReports, period?.label ?? `Período ${periodId}`)
  return new Response(buffer, {
    headers: {
      'Content-Type': 'application/pdf',
      'Content-Disposition': `attachment; filename="comissoes-${periodLabelSafe}.pdf"`,
    },
  })
}

// format === 'csv' — comportamento original (JSON para o ExportButton montar o CSV)
return NextResponse.json({
  period: period?.label ?? `Período ${periodId}`,
  vendors,
})
```

**Modificação 2 — src/app/dashboard/ExportButton.tsx:**

Adicionar estado `selectedFormat` e seletor segmentado de 3 opções. Preservar toda a lógica CSV existente.

```typescript
'use client'

import { useState } from 'react'
import { toast } from 'sonner'

type ExportFormat = 'csv' | 'xlsx' | 'pdf'

const FORMAT_LABELS: Record<ExportFormat, string> = {
  csv: 'Relatório CSV',
  xlsx: 'Relatório XLSX',
  pdf: 'Relatório PDF',
}

export default function ExportButton({ activePeriod }: { activePeriod?: number }) {
  const [loading, setLoading]             = useState(false)
  const [selectedFormat, setSelectedFormat] = useState<ExportFormat>('csv')

  async function handleExport() {
    if (!activePeriod) return
    setLoading(true)
    try {
      const res = await fetch(`/api/admin/relatorio-excel?period=${activePeriod}&format=${selectedFormat}`)

      if (selectedFormat === 'xlsx' || selectedFormat === 'pdf') {
        if (!res.ok) {
          const err = await res.json().catch(() => ({ error: 'Falha ao gerar relatório' }))
          toast.error('Falha ao gerar relatório', { description: err.error })
          return
        }
        const blob = await res.blob()
        const ext = selectedFormat
        const url = URL.createObjectURL(blob)
        const a = document.createElement('a')
        a.href = url
        a.download = `comissoes-periodo-${activePeriod}.${ext}`
        document.body.appendChild(a)
        a.click()
        document.body.removeChild(a)
        URL.revokeObjectURL(url)
        return
      }

      // CSV — lógica original preservada
      const data = await res.json()
      if (data.error) { toast.error('Falha ao gerar relatório', { description: data.error }); return }

      const { period: periodLabel, vendors } = data as {
        period: string
        vendors: {
          nome: string; loja: string; total_vendido: number
          comissao_pct: number; comissao: number
          meta_atingida: string; bonus: number; total_ganhos: number; status_aprovacao: string
        }[]
      }

      const BOM = '﻿'
      const SEP = ';'
      const fmt    = (n: number) => n.toFixed(2).replace('.', ',')
      const fmtPct = (n: number) => (n * 100).toFixed(1).replace('.', ',') + '%'

      const lines: string[] = [
        `Relatório de Comissões${SEP}${periodLabel}`,
        '',
        ['#','Vendedor','Canal de Venda','Total Vendido ($)','Comissão % Vendas','Comissão ($)','Meta Atingida','Bônus ($)','Total Ganhos ($)','Status'].join(SEP),
        ...vendors.map((v, i) => [
          i + 1, v.nome, v.loja,
          fmt(v.total_vendido), fmtPct(v.comissao_pct), fmt(v.comissao),
          v.meta_atingida, fmt(v.bonus), fmt(v.total_ganhos), v.status_aprovacao
        ].join(SEP)),
        '',
        ['TOTAL','','',
          fmt(vendors.reduce((s,v) => s + v.total_vendido, 0)),'',
          fmt(vendors.reduce((s,v) => s + v.comissao, 0)),'',
          fmt(vendors.reduce((s,v) => s + v.bonus, 0)),
          fmt(vendors.reduce((s,v) => s + v.total_ganhos, 0)),
          ''
        ].join(SEP),
      ]

      const blob = new Blob([BOM + lines.join('\r\n')], { type: 'text/csv;charset=utf-8;' })
      const url  = URL.createObjectURL(blob)
      const a    = document.createElement('a')
      a.href = url
      a.download = `comissoes-${periodLabel.toLowerCase().replace(/\s+/g, '-')}.csv`
      document.body.appendChild(a)
      a.click()
      document.body.removeChild(a)
      URL.revokeObjectURL(url)
    } catch (err: unknown) {
      const msg = err instanceof Error ? err.message : 'Erro desconhecido'
      toast.error('Falha ao gerar relatório', { description: msg })
    } finally {
      setLoading(false)
    }
  }

  return (
    <div className="flex flex-col gap-4">
      {/* Seletor de formato segmentado — per UI-SPEC D-10 */}
      <div className="flex rounded-xl overflow-hidden border border-white/5 h-[44px]">
        {(['csv', 'xlsx', 'pdf'] as ExportFormat[]).map(fmt => (
          <button
            key={fmt}
            onClick={() => setSelectedFormat(fmt)}
            className={[
              'flex-1 text-xs font-mono font-bold uppercase transition-colors',
              selectedFormat === fmt
                ? 'neon-border-active bg-surface-container text-primary'
                : 'bg-surface-container-high text-muted-foreground hover:bg-surface-container',
            ].join(' ')}
          >
            {fmt.toUpperCase()}
          </button>
        ))}
      </div>

      {/* Botão de download */}
      <button
        onClick={handleExport}
        disabled={loading}
        className="bg-surface-container-high hover:bg-surface-container-highest border border-white/5 rounded-xl text-primary font-mono font-bold text-xs px-4 py-[0.6rem] cursor-pointer whitespace-nowrap transition-colors flex items-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed h-[44px] w-full justify-center"
      >
        <span className="material-symbols-outlined text-sm">{loading ? 'hourglass_empty' : 'download'}</span>
        {loading ? 'Gerando...' : FORMAT_LABELS[selectedFormat]}
      </button>
    </div>
  )
}
```

**Modificação 3 — src/app/dashboard/relatorios/page.tsx:**

Atualizar apenas o grid e o título do card de export. O card de conectores será adicionado no plano 06-03.

- Linha `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-4xl` → `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl`
- Título h2 no Export Card: `Exportar Relatório CSV` → `Exportar Relatório`
- Subtexto do Export Card: `Selecione o período abaixo para gerar um relatório consolidado de todas as comissões, vendas e bônus da equipe.` → `Selecione o período e o formato para baixar o relatório consolidado de comissões e vendas.`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `npm run build` passa sem erros TypeScript
    - `grep -n "format=.*xlsx.*pdf" src/app/api/admin/relatorio-excel/route.ts` encontra o parâmetro format ou: `grep -n "generateCommissionXlsx" src/app/api/admin/relatorio-excel/route.ts` encontra o import
    - `grep -n "selectedFormat" src/app/dashboard/ExportButton.tsx` encontra o estado
    - `grep -n "neon-border-active" src/app/dashboard/ExportButton.tsx` encontra o seletor ativo
    - `grep -n "lg:grid-cols-3" src/app/dashboard/relatorios/page.tsx` encontra o grid atualizado
    - `grep -n "fs.existsSync" src/lib/export-pdf.ts` confirma logo com fallback gracioso (per D-12)
    - `npx vitest run` finaliza sem falhas
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| client → /api/admin/relatorio-excel | Parâmetros `period` e `format` não confiáveis |
| export-xlsx.ts / export-pdf.ts | Dados vindos do banco, potencialmente com chars especiais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | relatorio-excel route — parâmetro ?format= | mitigate | Cast explícito para `'csv' \| 'xlsx' \| 'pdf'`; qualquer outro valor cai no branch CSV (comportamento seguro por default) |
| T-06-02-02 | Tampering | XLSX gerado — CSV/Formula injection em células | mitigate | A função `sanitizeCSV()` já existente em relatorio-excel/route.ts é aplicada a campos `nome` e `loja` antes da construção de `vendorReports`; numeric fields (total_vendido, comissao, etc.) são tipados como `number` — sem risco de injection |
| T-06-02-03 | Elevation of Privilege | relatorio-excel route — acesso não autorizado | accept | Auth guard existente: `getTenantContext()` + check de roles `['adm', 'gerente', 'super_admin']` (linhas 6-11 da rota atual) — não alterado |
| T-06-02-04 | Denial of Service | export-pdf — geração de PDF com muitos vendors | accept | Conjunto de vendors já filtrado por period_id + tenant_id na query; máximo realista ~50 vendedores por período (negócio familiar CDE) — sem necessidade de paginação |
| T-06-02-05 | Information Disclosure | Content-Disposition header | accept | filename construído de periodLabel (dado do banco, não user input direto) com sanitização via `.replace(/\s+/g, '-')` — sem path traversal possível |
</threat_model>

<verification>
## Checagem Final da Fase 06 Plano 02

```bash
# 1. Testes de export passam
npx vitest run src/__tests__/export.test.ts --reporter=verbose

# 2. generateCommissionXlsx usa xlsx-js-style (não xlsx)
grep -n "from 'xlsx-js-style'" src/lib/export-xlsx.ts

# 3. Rota aceita format=xlsx e format=pdf
grep -n "format" src/app/api/admin/relatorio-excel/route.ts

# 4. ExportButton tem seletor segmentado
grep -n "selectedFormat" src/app/dashboard/ExportButton.tsx
grep -n "neon-border-active" src/app/dashboard/ExportButton.tsx

# 5. Grid atualizado para 3 colunas
grep -n "lg:grid-cols-3" src/app/dashboard/relatorios/page.tsx

# 6. Logo com fallback gracioso (D-12)
grep -n "fs.existsSync" src/lib/export-pdf.ts

# 7. Build limpo
npm run build 2>&1 | grep -E "^.*error" | head -10
```
</verification>

<success_criteria>
- 6 testes de export.test.ts passam: 4 de XLSX + 2 de PDF
- `src/lib/export-xlsx.ts` usa `xlsx-js-style` para geração (bold headers confirmado por roundtrip test)
- `src/lib/export-pdf.ts` gera Buffer com magic bytes `%PDF`
- `src/lib/export-pdf.ts` inclui `doc.image()` com verificação `fs.existsSync` para logo GDS (D-12 completo)
- Rota `/api/admin/relatorio-excel?format=xlsx` retorna `Content-Type: application/vnd.openxmlformats...`
- Rota `/api/admin/relatorio-excel?format=pdf` retorna `Content-Type: application/pdf`
- Rota `/api/admin/relatorio-excel` (sem format ou format=csv) mantém comportamento JSON original
- ExportButton mostra 3 tabs CSV/XLSX/PDF com estado ativo destacado
- `npm run build` passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-02-SUMMARY.md`
</output>
