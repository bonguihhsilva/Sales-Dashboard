---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/__tests__/fingerprint.test.ts
  - src/__tests__/export.test.ts
  - src/__tests__/connectors.test.ts
  - src/lib/fingerprint.ts
  - src/app/api/admin/parse-upload/route.ts
  - src/app/dashboard/UploadModal.tsx
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "Arquivo HTML CEC com 19+ colunas é detectado automaticamente como sistema 'cec'"
    - "Arquivo CSV genérico retorna null (não-detectado) e UploadModal exibe dropdown de seleção manual"
    - "API parse-upload retorna campo detected_system na resposta"
    - "UploadModal exibe badge 'Formato detectado: CEC' quando sistema detectado"
    - "UploadModal exibe dropdown com CEC/Pegasus/ISRP/Genérico quando detected_system é null"
    - "Suite vitest executa sem erros (testes unitários passam)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Configuração do framework de testes unitários"
    - path: "src/__tests__/fingerprint.test.ts"
      provides: "Testes D-05 e D-06 (fingerprint CEC e fallback null)"
    - path: "src/__tests__/export.test.ts"
      provides: "Stubs de testes para XLSX e PDF (cobertos no plano 02)"
    - path: "src/__tests__/connectors.test.ts"
      provides: "Stubs de testes para prefixo de sistema (cobertos no plano 03)"
    - path: "src/lib/fingerprint.ts"
      provides: "detectFileSystem(buffer, filename) → DetectedSystem"
    - path: "src/app/api/admin/parse-upload/route.ts"
      provides: "Campo detected_system adicionado à resposta"
    - path: "src/app/dashboard/UploadModal.tsx"
      provides: "Badge de sistema detectado + dropdown fallback"
  key_links:
    - from: "src/app/api/admin/parse-upload/route.ts"
      to: "src/lib/fingerprint.ts"
      via: "import detectFileSystem"
      pattern: "detectFileSystem\\(buffer"
    - from: "src/app/dashboard/UploadModal.tsx"
      to: "detected_system campo da resposta"
      via: "data.detected_system"
      pattern: "detected_system"
---

<objective>
Configurar infraestrutura de testes (Wave 0) e implementar detecção automática de formato por fingerprint, com fallback de dropdown manual no UploadModal quando o formato não for identificado.

Purpose: Habilitar o sistema a identificar o sistema de origem de um arquivo antes do parse, sem depender do nome do arquivo. Provê o alicerce de testes para toda a fase 6.

Output: `fingerprint.ts` com `detectFileSystem()`, extensão da rota `parse-upload` retornando `detected_system`, e UploadModal atualizado com badge de detecção e dropdown fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-CONTEXT.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-RESEARCH.md
@.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-UI-SPEC.md

<interfaces>
<!-- Contratos existentes que esta tarefa consome e estende -->

De src/lib/parser.ts:
```typescript
export interface SaleTransaction {
  vendor_id: string
  client_id: string
  client_name: string
  sale_date: string   // DD/MM/YY
  sale_time: string   // HH:MM:SS
  order_ref: string
  valor: number
  quantity: number
}
export function parseSalesHtml(html: string): SaleTransaction[]
export interface DetectedPeriod {
  year: number; month: number; label: string; start_date: string; end_date: string
}
export function detectPeriodFromTransactions(transactions: SaleTransaction[]): DetectedPeriod | null
```

De src/app/api/admin/parse-upload/route.ts (resposta atual):
```typescript
return NextResponse.json({
  transactions,
  detected       // DetectedPeriod | null
})
// NOVO: adicionar detected_system: DetectedSystem | null
```

De src/app/dashboard/UploadModal.tsx (estado atual):
```typescript
const [detected, setDetected] = useState<DetectedInfo | null>(null)
// NOVO: adicionar
// const [detectedSystem, setDetectedSystem] = useState<string | null>(null)
// const [selectedSystem, setSelectedSystem] = useState<string>('generic')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Wave 0 — Infraestrutura de testes e fingerprint.ts</name>
  <files>
    vitest.config.ts
    src/__tests__/fingerprint.test.ts
    src/__tests__/export.test.ts
    src/__tests__/connectors.test.ts
    src/lib/fingerprint.ts
  </files>
  <read_first>
    - src/lib/parser.ts (interface SaleTransaction e lógica looksLikeCec existente para referência)
    - src/lib/server-parser.ts (para entender ponto de extensão e importação do xlsx)
    - package.json (verificar se vitest já está listado como devDependency)
    - tsconfig.json (verificar configurações strict e paths)
  </read_first>
  <behavior>
    - Test fingerprint-CEC: buffer com HTML de 3+ linhas de tabela com 19 colunas onde col[0] tem 4-6 dígitos → retorna 'cec'
    - Test fingerprint-not-cec-html: HTML com menos de 19 colunas ou col[0] não numérica → retorna null
    - Test fingerprint-csv-fallback: buffer de texto CSV qualquer → retorna null
    - Test fingerprint-xlsx-magic: buffer começando com bytes PK (0x50 0x4B) → retorna null (stub Pegasus/ISRP não implementado)
    - Test fingerprint-html-extension: arquivo .html com conteúdo CEC → retorna 'cec'
    - Test fingerprint-unknown: buffer aleatório → retorna null
    - export.test.ts e connectors.test.ts: apenas stubs describe/it.todo para Wave 2 e Wave 3
  </behavior>
  <action>
1. Instalar vitest se ausente:
   ```bash
   npm install --save-dev vitest
   ```

2. Criar vitest.config.ts na raiz do projeto:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import path from 'path'
   
   export default defineConfig({
     test: {
       environment: 'node',
       globals: false,
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

3. Adicionar script em package.json (apenas se não existir):
   `"test": "vitest run"` em `scripts`

4. Criar src/lib/fingerprint.ts:
   ```typescript
   export type DetectedSystem = 'cec' | 'pegasus' | 'isrp' | 'generic' | null
   
   export function detectFileSystem(buffer: Buffer, filename: string): DetectedSystem {
     const ext = filename.split('.').pop()?.toLowerCase()
   
     // Magic bytes: XLSX/ZIP começa com PK (0x50 0x4B 0x03 0x04)
     const isZip = buffer.length >= 4 && buffer[0] === 0x50 && buffer[1] === 0x4B
   
     // HTML check via primeiros 512 bytes
     const head = buffer.slice(0, 512).toString('utf-8').trimStart().toLowerCase()
     const isHtml = head.startsWith('<!doctype') || head.startsWith('<html') || head.includes('<table')
   
     if (isHtml || ext === 'html' || ext === 'htm') {
       const html = buffer.toString('utf-8')
       if (looksLikeCecHtml(html)) return 'cec'
       return null
     }
   
     if (isZip || ext === 'xlsx' || ext === 'xls') {
       // TODO: quando docs Pegasus chegarem, verificar headers XLSX da planilha
       // Assinatura Pegasus: primeira linha com colunas 'COD_VENDEDOR', 'COD_CLIENTE', 'VALOR' (a confirmar)
       // Por ora retorna null para forçar fallback dropdown (D-06)
       return null
     }
   
     if (ext === 'csv') {
       // TODO: quando docs Pegasus/ISRP chegarem, verificar headers CSV
       // Assinatura Pegasus CSV: header linha 1 com 'vendor_code', 'client_code', 'amount' (a confirmar)
       // Por ora retorna null para forçar fallback dropdown (D-06)
       return null
     }
   
     return null
   }
   
   function looksLikeCecHtml(html: string): boolean {
     // CEC: tabela HTML com 19+ colunas por linha, col[0] = código numérico de 4-6 dígitos
     const rowRegex = /<tr[^>]*>([\s\S]*?)<\/tr>/gi
     let dataRowsFound = 0
     let match
     while ((match = rowRegex.exec(html)) !== null) {
       const cells = match[1].match(/<td[^>]*>([\s\S]*?)<\/td>/gi)
       if (cells && cells.length >= 19) {
         const firstCell = cells[0].replace(/<[^>]+>/g, '').replace(/&nbsp;/g, ' ').trim()
         if (/^\d{4,6}$/.test(firstCell)) {
           dataRowsFound++
           if (dataRowsFound >= 3) return true
         }
       }
     }
     return false
   }
   ```

5. Criar src/__tests__/fingerprint.test.ts com testes RED → GREEN para as 6 behaviors listadas acima.

   Fixtures necessárias inline (sem arquivos externos):
   - HTML CEC: string com 3 rows de `<tr><td>12345</td><td>...</td></tr>` com 19 cells cada
   - HTML curto: string com 2 colunas por row
   - CSV: string `"vendedor;cliente;valor\nA;B;100"`
   - XLSX magic: `Buffer.from([0x50, 0x4B, 0x03, 0x04, ...zeros])`

6. Criar src/__tests__/export.test.ts:
   ```typescript
   import { describe, it } from 'vitest'
   describe('export-xlsx', () => {
     it.todo('XLSX gerado tem bold nos headers (roundtrip) — implementado no plano 06-02')
   })
   describe('export-pdf', () => {
     it.todo('PDF gerado é Buffer não-vazio com conteúdo do período — implementado no plano 06-02')
   })
   ```

7. Criar src/__tests__/connectors.test.ts:
   ```typescript
   import { describe, it } from 'vitest'
   describe('connectors', () => {
     it.todo('vendor_id recebe prefixo PEG_ para sistema Pegasus — implementado no plano 06-03')
     it.todo('vendor_id recebe prefixo ISRP_ para sistema ISRP — implementado no plano 06-03')
   })
   ```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/fingerprint.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - vitest.config.ts existe na raiz com alias @/* configurado
    - src/lib/fingerprint.ts exporta DetectedSystem e detectFileSystem
    - Todos os 6 testes de fingerprint.test.ts passam (GREEN)
    - export.test.ts e connectors.test.ts existem com stubs it.todo
    - `npx vitest run` finaliza sem falhas (todos os testes PASS ou TODO)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Estender parse-upload e UploadModal com detecção de sistema</name>
  <files>
    src/app/api/admin/parse-upload/route.ts
    src/app/dashboard/UploadModal.tsx
  </files>
  <read_first>
    - src/app/api/admin/parse-upload/route.ts (estado atual completo — OBRIGATÓRIO ler antes de editar)
    - src/app/dashboard/UploadModal.tsx (estado atual completo — OBRIGATÓRIO ler antes de editar)
    - src/lib/fingerprint.ts (recém criada na Tarefa 1 — ler para entender o contrato)
    - .planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-UI-SPEC.md (contratos visuais de badge e dropdown)
  </read_first>
  <action>
**Modificação 1 — src/app/api/admin/parse-upload/route.ts:**

Adicionar import de `detectFileSystem` e incluir `detected_system` na resposta:

```typescript
import { detectFileSystem } from '@/lib/fingerprint'
```

Após a linha `const transactions = await parseUploadBuffer(buffer, file.name)`, adicionar:
```typescript
const detectedSystem = detectFileSystem(buffer, file.name)
```

Alterar o return para:
```typescript
return NextResponse.json({
  transactions,
  detected,
  detected_system: detectedSystem,   // null → UploadModal mostra dropdown (D-06)
})
```

**Modificação 2 — src/app/dashboard/UploadModal.tsx:**

1. Adicionar dois novos estados após os existentes:
```typescript
const [detectedSystem, setDetectedSystem] = useState<string | null>(undefined as unknown as null)
const [selectedSystem, setSelectedSystem] = useState<string>('generic')
```
   Usar `useState<string | null>(null)` e `useState<string>('generic')` respectivamente.

2. Em `handleFileChange`, após `setParsedData(data.transactions)`:
```typescript
setDetectedSystem(data.detected_system ?? null)
setSelectedSystem(data.detected_system ?? 'generic')
```

3. Adicionar bloco de badge/dropdown no JSX, entre o file input e o bloco de período (buscar pelo `setDetecting(false)` e inserir JSX logo após o file input, antes do bloco de modo incremental/replace):

```tsx
{/* Sistema detectado (D-05/D-06) */}
{file && !detecting && (
  <div className="mt-4">
    {detectedSystem !== null ? (
      <span className="text-xs font-mono text-muted-foreground uppercase tracking-wider">
        Formato detectado: {detectedSystem === 'cec' ? 'CEC (Relatório HTML)' : detectedSystem.toUpperCase()}
      </span>
    ) : file && (
      <div>
        <label className="block text-xs font-mono text-muted-foreground uppercase tracking-wider mb-2">
          Sistema não identificado — selecione manualmente:
        </label>
        <select
          value={selectedSystem}
          onChange={e => setSelectedSystem(e.target.value)}
          className="bg-surface-container border border-white/5 rounded-xl text-sm text-on-surface px-3 py-2 w-full"
        >
          <option value="cec">CEC (Relatório HTML)</option>
          <option value="pegasus">Pegasus</option>
          <option value="isrp">ISRP</option>
          <option value="generic">Genérico</option>
        </select>
      </div>
    )}
  </div>
)}
```

4. Em `handleUpload`, adicionar validação antes do fetch:
```typescript
if (detectedSystem === null && !selectedSystem) {
  setMessage('⚠ Selecione o formato do arquivo para continuar')
  return
}
```

5. Resetar `detectedSystem` e `selectedSystem` em `handleFileChange` no bloco de reset inicial:
```typescript
setDetectedSystem(null)
setSelectedSystem('generic')
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` passa sem erros TypeScript
    - src/app/api/admin/parse-upload/route.ts retorna `detected_system` na resposta JSON
    - `grep -n "detected_system" src/app/api/admin/parse-upload/route.ts` encontra a linha
    - `grep -n "detectedSystem" src/app/dashboard/UploadModal.tsx` encontra useState e uso no JSX
    - `grep -n "Sistema não identificado" src/app/dashboard/UploadModal.tsx` encontra o label do dropdown
    - `grep -n "Formato detectado" src/app/dashboard/UploadModal.tsx` encontra o badge
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| client → /api/admin/parse-upload | Buffer de arquivo não-confiável, potencialmente malicioso |
| fingerprint interno | Parsing de bytes do buffer — sem acesso externo, mas pode receber lixo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | detectFileSystem — buffer de entrada | mitigate | Análise limitada aos primeiros 512 bytes para detecção; não descompacta XLSX durante fingerprint — previne ZIP bomb no fingerprint |
| T-06-01-02 | Tampering | parse-upload route | accept | Limite de 10MB já existente em `parse-upload/route.ts` (linha 29: `file.size > 10 * 1024 * 1024`); strictRateLimiter já aplicado |
| T-06-01-03 | Spoofing | UploadModal — selectedSystem | mitigate | Valor de selectedSystem é usado apenas como metadado de UI; o parse real em server-parser.ts não depende desse campo para lógica de segurança |
| T-06-01-04 | Information Disclosure | detected_system no response JSON | accept | Campo expõe apenas o sistema detectado ('cec'/null) — sem dados sensíveis; informação já visível ao usuário pelo arquivo enviado |
</threat_model>

<verification>
## Checagem Final da Fase 06 Plano 01

```bash
# 1. Testes unitários fingerprint passam
npx vitest run src/__tests__/fingerprint.test.ts --reporter=verbose

# 2. fingerprint.ts exporta os símbolos corretos
grep -n "export type DetectedSystem" src/lib/fingerprint.ts
grep -n "export function detectFileSystem" src/lib/fingerprint.ts

# 3. parse-upload retorna detected_system
grep -n "detected_system" src/app/api/admin/parse-upload/route.ts

# 4. UploadModal tem badge e dropdown
grep -n "Formato detectado" src/app/dashboard/UploadModal.tsx
grep -n "Sistema não identificado" src/app/dashboard/UploadModal.tsx

# 5. Build limpo
npm run build 2>&1 | grep -E "error|Error" | head -10
```
</verification>

<success_criteria>
- `npx vitest run` finaliza com 0 testes falhando
- `src/lib/fingerprint.ts` exporta `DetectedSystem` e `detectFileSystem`
- `src/app/api/admin/parse-upload/route.ts` inclui `detected_system` na resposta
- `src/app/dashboard/UploadModal.tsx` exibe badge quando `detected_system !== null` e dropdown quando `detected_system === null`
- `npm run build` passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-formatos-de-upload-download-e-conectores-de-sistemas-de-vend/06-01-SUMMARY.md`
</output>
