---
phase: 03-auth-users
plan: 06
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/app/api/admin/disable-user/route.ts
  - src/app/api/admin/update-user/route.ts
  - src/app/dashboard/usuarios/page.tsx
  - src/app/dashboard/usuarios/UsersClient.tsx
autonomous: true
requirements: [AUTH-06]
must_haves:
  truths:
    - "A tabela de usuarios mostra nome, numero vendedor, loja, role, ativo e ultimo acesso"
    - "A tabela tem filtros por role e por loja"
    - "A acao editar abre um Sheet lateral com nome, role, loja e ativo editaveis"
    - "Desativar usuario chama disable-user que aplica ban_duration e profiles.ativo=false"
    - "O botao Convidar usuario abre um Sheet que cria o convite e exibe o link com botao Copiar"
    - "A acao 'Reenviar convite' chama POST /api/admin/invite para gerar um novo link de convite"
    - "A tabela so mostra usuarios do tenant do gerente logado (nao usuarios de outros tenants)"
  artifacts:
    - path: "src/app/api/admin/disable-user/route.ts"
      provides: "Route handler que bane usuario e sincroniza profiles.ativo"
      exports: ["POST"]
    - path: "src/app/dashboard/usuarios/page.tsx"
      provides: "Server component com guard de 4 roles e fetch de profiles + last_sign_in_at"
      contains: "app_metadata"
    - path: "src/app/dashboard/usuarios/UsersClient.tsx"
      provides: "Tabela com DataTable, filtros, Sheet de edicao, Sheet de convite e acao reenviar convite"
      contains: "use client"
  key_links:
    - from: "src/app/dashboard/usuarios/page.tsx"
      to: "admin.auth.admin.listUsers"
      via: "merge de last_sign_in_at com profiles do tenant"
      pattern: "listUsers"
    - from: "src/app/dashboard/usuarios/UsersClient.tsx"
      to: "/api/admin/invite"
      via: "fetch no submit do Sheet de convite E na acao reenviar convite"
      pattern: "api/admin/invite"
    - from: "src/app/dashboard/usuarios/UsersClient.tsx"
      to: "/api/admin/disable-user"
      via: "fetch na confirmacao do Dialog"
      pattern: "disable-user"
---

<objective>
Reescrever a tela `/dashboard/usuarios`: criar o route handler `POST /api/admin/disable-user` (ban + `ativo=false`), atualizar `update-user` para os 4 roles, expandir o guard do server component e substituir o `<table>` inline atual por um `DataTable` shadcn com filtros, Sheet de edicao (D-08), Sheet de convite que consome o backend dos Plans 03/05, e a acao "Reenviar convite" (AUTH-06).

Purpose: AUTH-06 — tabela completa com filtros e as 3 acoes exigidas (editar role, desativar, reenviar convite). D-08 (editar via Sheet), D-09 (desativar via ban_duration). Este e o ultimo plano da fase e tambem resolve a falha de build deixada pelo Plan 01 (UsersClient.tsx usa `p.active`).

Posicao na fase: Wave 3. Importa `canInvite`, `isValidRole`, `setUserRole`, `ASSIGNABLE_ROLES` de `src/lib/auth/roles.ts` (criado no Plan 03 — Wave 2). `depends_on: [01, 03]` e `wave: 3` garantem execucao apos o Plan 03.
Output: disable-user route, update-user atualizado, page.tsx e UsersClient.tsx reescritos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-auth-users/03-CONTEXT.md
@.planning/phases/03-auth-users/03-RESEARCH.md
@.planning/phases/03-auth-users/03-UI-SPEC.md

<interfaces>
<!-- Contratos para o executor. Usar diretamente — sem exploracao de codebase. -->

Profile (de src/types/index.ts apos Plan 01):
```typescript
export type UserRole = 'vendedor' | 'adm' | 'gerente' | 'super_admin'
export interface Profile {
  id: string; name: string; role: UserRole
  vendor_id: string | null; numero_vendedor: string | null
  store: string | null; tenant_id: string | null
  data_admissao: string | null; ativo: boolean; created_at: string
}
```

STORE_LABELS (de src/types/index.ts) — mapeia valor do banco -> rotulo:
```typescript
// 'Jebai' -> 'Jebai' | 'Paje-MKT' -> 'Paje 1' | 'Paje-Caixa' -> 'Paje 2'
```
Os 3 valores de loja sao: 'Jebai', 'Paje-MKT', 'Paje-Caixa'.

ASSIGNABLE_ROLES (de src/lib/auth/roles.ts, Plan 03): ['vendedor', 'gerente', 'adm']
setUserRole(admin, userId, role) (de src/lib/auth/roles.ts): atualiza app_metadata + profiles.
canInvite(role) (de src/lib/auth/roles.ts): true para adm|gerente|super_admin.

Contrato de /api/admin/invite (criado no Plan 03 — Wave 2):
```typescript
// POST /api/admin/invite
// body: { email?: string, role: string, loja: string }
//   - email e OPCIONAL: omitir gera convite link-only (sem envio de email)
// resposta 200: { link: string, email_sent: boolean, token: string }
//   - link: URL completa /convite/[token] para copiar/compartilhar
//   - email_sent: true se email foi preenchido E o envio Resend teve sucesso
```

Buscar last_sign_in_at no server component (RESEARCH "Pattern 6"):
```typescript
const admin = createClient(url, serviceRoleKey, { auth: { autoRefreshToken:false, persistSession:false } })
const { data: { users } } = await admin.auth.admin.listUsers({ perPage: 1000 })
// merge: profiles.map(p => ({ ...p, last_sign_in_at: users.find(u => u.id===p.id)?.last_sign_in_at ?? null }))
// IMPORTANTE: listUsers retorna todos os tenants — filtrar por profiles do tenant atual (Pitfall 4)
```

DataTable existente (src/components/ui/data-table.tsx) — confirmar API no read_first.
A API tipica do data-table shadcn recebe `columns` (ColumnDef[]) e `data`.
Se a API local divergir, ADAPTAR o uso a assinatura real do arquivo.

Componentes do barrel @/components/ui: Button, Card, Input, Label, Select,
Sheet, Dialog, Badge, Avatar, Skeleton, EmptyState, PageHeader, LojaBadge,
DropdownMenu, Tooltip, Separator, Switch. Confirmar nomes exatos em ui/index.tsx.

Padrao update-user atual (src/app/api/admin/update-user/route.ts — JA EXISTE):
Ler o arquivo no read_first. Ele provavelmente verifica role 'adm' apenas e
atualiza profiles direto. Precisa: (1) aceitar caller adm|gerente|super_admin
via app_metadata; (2) usar setUserRole quando o role muda.

Contrato de copy e colunas — UI-SPEC secao "Screen 2: /dashboard/usuarios".
Colunas da tabela: Nome | N Vendedor | Loja | Role | Ativo | Ultimo acesso | Acoes.

Convencao do projeto: server component faz fetch + guard; client component
recebe dados via props. Filtros via URL searchParams. Erros de API como
NextResponse.json({ error }, { status }).

Next.js 15: searchParams do page e Promise:
```typescript
export default async function Page({ searchParams }: { searchParams: Promise<{ role?: string; loja?: string }> })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar disable-user e atualizar update-user (route handlers)</name>
  <read_first>
    - src/app/api/admin/update-user/route.ts (arquivo inteiro — sera modificado)
    - src/app/api/admin/create-user/route.ts (padrao de admin client de referencia)
    - src/lib/auth/roles.ts (canInvite, setUserRole, isValidRole — criados no Plan 03)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Code Examples" > "Desativar usuario (ban + profiles.ativo)" — codigo de referencia; nota: usar ban_duration, NAO banned:true)
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-09 — ban_duration via service role)
  </read_first>
  <action>
    Dois arquivos.

    --- ARQUIVO 1: criar `src/app/api/admin/disable-user/route.ts` ---

    Route handler `POST` que bane/reativa um usuario. Conteudo EXATO:

    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import { NextRequest, NextResponse } from 'next/server'
    import { createClient as createServerClient } from '@/lib/supabase/server'
    import { canInvite } from '@/lib/auth/roles'

    export async function POST(req: NextRequest) {
      // Caller: mesmos roles que podem gerir usuarios (adm|gerente|super_admin)
      const caller = await createServerClient()
      const { data: { user } } = await caller.auth.getUser()
      if (!user) {
        return NextResponse.json({ error: 'Nao autorizado' }, { status: 401 })
      }
      const callerRole = user.app_metadata?.role as string | undefined
      if (!canInvite(callerRole)) {
        return NextResponse.json({ error: 'Acesso negado' }, { status: 403 })
      }

      const { userId, ativo } = await req.json()
      if (!userId || typeof ativo !== 'boolean') {
        return NextResponse.json({ error: 'userId e ativo (boolean) obrigatorios' }, { status: 400 })
      }

      // Caller nao pode desativar a si mesmo
      if (userId === user.id) {
        return NextResponse.json({ error: 'Voce nao pode desativar a si mesmo' }, { status: 400 })
      }

      const admin = createClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!,
        { auth: { autoRefreshToken: false, persistSession: false } }
      )

      // ativo=false -> banir (invalida refresh tokens). ativo=true -> remover ban.
      // O parametro correto do SDK JS e ban_duration string (D-09 / RESEARCH).
      const banDuration = ativo ? 'none' : '876600h' // 'none' remove o ban; 876600h ~= 100 anos
      const { error: banError } = await admin.auth.admin.updateUserById(userId, {
        ban_duration: banDuration,
      })
      if (banError) {
        return NextResponse.json({ error: banError.message }, { status: 400 })
      }

      // Sincronizar profiles.ativo
      const { error: profError } = await admin
        .from('profiles')
        .update({ ativo })
        .eq('id', userId)
      if (profError) {
        return NextResponse.json({ error: profError.message }, { status: 400 })
      }

      return NextResponse.json({ success: true, ativo })
    }
    ```

    --- ARQUIVO 2: editar `src/app/api/admin/update-user/route.ts` ---

    Ler o arquivo atual no read_first. Reescrever para o seguinte comportamento (conteudo EXATO):

    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import { NextRequest, NextResponse } from 'next/server'
    import { createClient as createServerClient } from '@/lib/supabase/server'
    import { canInvite, isValidRole, setUserRole } from '@/lib/auth/roles'
    import type { UserRole } from '@/types'

    export async function POST(req: NextRequest) {
      const caller = await createServerClient()
      const { data: { user } } = await caller.auth.getUser()
      if (!user) {
        return NextResponse.json({ error: 'Nao autorizado' }, { status: 401 })
      }
      const callerRole = user.app_metadata?.role as string | undefined
      if (!canInvite(callerRole)) {
        return NextResponse.json({ error: 'Acesso negado' }, { status: 403 })
      }

      const { userId, name, role, store, ativo } = await req.json()
      if (!userId) {
        return NextResponse.json({ error: 'userId obrigatorio' }, { status: 400 })
      }
      if (role !== undefined && !isValidRole(role)) {
        return NextResponse.json({ error: 'Role invalido' }, { status: 400 })
      }

      const admin = createClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!,
        { auth: { autoRefreshToken: false, persistSession: false } }
      )

      // Se o role mudou, usar setUserRole (atualiza app_metadata + profiles juntos)
      if (role !== undefined) {
        const roleResult = await setUserRole(admin, userId, role as UserRole)
        if (roleResult.error) {
          return NextResponse.json({ error: roleResult.error }, { status: 400 })
        }
      }

      // Demais campos (name, store, ativo) — update direto no profile
      const patch: Record<string, unknown> = {}
      if (name !== undefined) patch.name = name
      if (store !== undefined) patch.store = store
      if (ativo !== undefined) patch.ativo = ativo
      if (Object.keys(patch).length > 0) {
        const { error: profError } = await admin
          .from('profiles')
          .update(patch)
          .eq('id', userId)
        if (profError) {
          return NextResponse.json({ error: profError.message }, { status: 400 })
        }
      }

      return NextResponse.json({ success: true })
    }
    ```

    Notas: (1) o Sheet de edicao tem um Switch de `ativo`, mas a invalidacao de sessao por ban so acontece via disable-user — se o Sheet alterar `ativo` para false, ele deve chamar disable-user, nao update-user, para o ban acontecer (a UI da Task 5 trata isso); update-user aceita `ativo` apenas como conveniencia para reativacao de profile; (2) `canInvite` e reutilizado como "pode gerir usuarios" — os roles sao os mesmos (adm/gerente/super_admin).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/admin/disable-user/route.ts` existe e exporta `POST`
    - disable-user contem `ban_duration` e o valor `'876600h'`
    - disable-user contem checagem `userId === user.id` (nao pode auto-desativar)
    - disable-user contem `.from('profiles').update({ ativo })`
    - `src/app/api/admin/update-user/route.ts` contem `setUserRole` e `canInvite`
    - update-user NAO contem mais `profile?.role !== 'adm'` (guard antigo de 1 role removido)
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>disable-user banido via ban_duration + ativo sincronizado; update-user aceita 4 roles e usa setUserRole; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever o server component src/app/dashboard/usuarios/page.tsx</name>
  <read_first>
    - src/app/dashboard/usuarios/page.tsx (arquivo inteiro — guard e fetch atuais)
    - src/lib/supabase/server.ts (createClient SSR)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Code Examples" > "Buscar usuarios com last_sign_in_at" e "Pitfall 4" — filtrar por tenant)
    - .planning/phases/03-auth-users/03-UI-SPEC.md (secao "Screen 2" — layout com PageHeader)
  </read_first>
  <action>
    Reescrever TODO o conteudo de `src/app/dashboard/usuarios/page.tsx`. Server component (sem `'use client'`). Responsabilidades: guard de role (4 roles), fetch dos profiles do tenant + merge com `last_sign_in_at`, passar tudo para `UsersClient`.

    Estrutura alvo:
    - Imports: `createClient` de `@/lib/supabase/server`, `createClient as createAdminClient` de `@supabase/supabase-js`, `redirect` de `next/navigation`, `UsersClient` de `./UsersClient`, e tipo `Profile` de `@/types`.
    - Assinatura Next.js 15: `export default async function UsersPage({ searchParams }: { searchParams: Promise<{ role?: string; loja?: string }> })`.
    - Logica:
      ```typescript
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/login')

      // Guard: somente adm/gerente/super_admin (D-01). Via app_metadata.
      const callerRole = user.app_metadata?.role as string | undefined
      if (!callerRole || !['adm', 'gerente', 'super_admin'].includes(callerRole)) {
        redirect('/meu-resultado')
      }

      // tenant_id do caller — para isolar a listagem (Pitfall 4)
      const { data: callerProfile } = await supabase
        .from('profiles')
        .select('tenant_id')
        .eq('id', user.id)
        .single()
      const tenantId = callerProfile?.tenant_id

      // Profiles do tenant + last_sign_in_at via admin API
      const admin = createAdminClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!,
        { auth: { autoRefreshToken: false, persistSession: false } }
      )
      const [{ data: profiles }, listResult] = await Promise.all([
        supabase.from('profiles').select('*').eq('tenant_id', tenantId).order('name'),
        admin.auth.admin.listUsers({ perPage: 1000 }),
      ])
      const authUsers = listResult.data?.users ?? []

      const users = (profiles ?? []).map((p) => ({
        ...p,
        last_sign_in_at: authUsers.find((u) => u.id === p.id)?.last_sign_in_at ?? null,
      }))
      ```
    - Resolver os filtros: `const { role, loja } = await searchParams`. Aplicar filtro em memoria sobre `users` (a lista e pequena — 3 lojas): se `role` definido, manter so `u.role === role`; se `loja` definida, manter so `u.store === loja`.
    - Renderizar: `<UsersClient users={filteredUsers} activeRole={role ?? null} activeLoja={loja ?? null} />`. O `UsersClient` cuida de PageHeader, filtros e tabela.
    - Definir um tipo exportado para a linha da tabela e usa-lo aqui e no UsersClient — declarar no topo do page.tsx:
      ```typescript
      export type UserRow = Profile & { last_sign_in_at: string | null }
      ```

    NAO buscar `goals` nem `vendorOptions` (a tela antiga vinculava vendedor a goals — fora do escopo desta fase). NAO usar inline styles. O layout visual (sidebar/topbar) vem do layout da fase 1; este arquivo so renderiza o conteudo da pagina via UsersClient.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/usuarios/page.tsx` NAO contem `'use client'`
    - O arquivo contem guard com `['adm', 'gerente', 'super_admin'].includes(callerRole)`
    - O arquivo le `callerRole` de `user.app_metadata?.role` (nao via query a profiles.role)
    - O arquivo contem `admin.auth.admin.listUsers`
    - O arquivo contem `.eq('tenant_id', tenantId)` (isolamento por tenant — Pitfall 4)
    - O arquivo exporta o tipo `UserRow`
    - O arquivo NAO contem `from('goals')`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>page.tsx faz guard de 4 roles, busca profiles do tenant com last_sign_in_at e passa para UsersClient; build verde.</done>
</task>

<task type="auto">
  <name>Task 3: UsersClient.tsx — estrutura base, DataTable, filtros e skeleton</name>
  <read_first>
    - src/app/dashboard/usuarios/UsersClient.tsx (arquivo inteiro — sera totalmente substituido; este arquivo usa p.active e quebra o build desde o Plan 01)
    - src/app/dashboard/usuarios/page.tsx (apos Task 2 — props que UsersClient recebe e o tipo UserRow)
    - src/components/ui/data-table.tsx (API real do DataTable — colunas e data)
    - src/components/ui/dropdown-menu.tsx (API do DropdownMenu — usado na coluna Acoes)
    - src/components/ui/select.tsx (API do Select — usado nos filtros)
    - src/components/ui/index.tsx (nomes exportados pelo barrel)
    - src/components/ui/loja-badge.tsx (props do LojaBadge)
    - src/types/index.ts (STORE_LABELS)
    - .planning/phases/03-auth-users/03-UI-SPEC.md (secao "Screen 2" — tabela, colunas, filtros, EmptyState, copy)
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-05, D-08, D-09 — contexto das acoes)
  </read_first>
  <action>
    Esta task cria a ESTRUTURA BASE do `src/app/dashboard/usuarios/UsersClient.tsx` — a tabela, filtros e o esqueleto dos handlers de Sheet/Dialog (que as Tasks 4 e 5 preenchem). Substituir TODO o conteudo do arquivo. Client component (`'use client'` na primeira linha).

    Antes de codar, ler `data-table.tsx`, `select.tsx`, `dropdown-menu.tsx` no read_first para usar as APIs reais — adaptar a estrutura abaixo a assinatura concreta de cada componente.

    Props: `{ users, activeRole, activeLoja }: { users: UserRow[]; activeRole: string | null; activeLoja: string | null }` — importar `UserRow` de `./page`.

    Estado (declarar TODO o estado da tela aqui — as Tasks 4 e 5 consomem):
    `inviteOpen` (bool), `editingUser` (UserRow|null), `disablingUser` (UserRow|null),
    e estados auxiliares: `loading` (bool), `error` (string|null). Os estados especificos
    de cada Sheet (campos de form, inviteLink, copied) sao adicionados nas Tasks 4/5.

    Composicao desta task (todos os componentes do barrel `@/components/ui`):

    1. **PageHeader**: titulo "Usuarios", subtitulo "Gerencie acessos e convites da equipe". No slot de acoes, um `Button` "Convidar usuario" que faz `setInviteOpen(true)`.

    2. **Linha de filtros**: dois `Select` lado a lado — filtro de Role (opcoes: todos os 4 roles) e filtro de Loja (opcoes: Jebai/Paje-MKT/Paje-Caixa com rotulos de STORE_LABELS). Ao mudar, navegar via `useRouter().push` atualizando searchParams (`?role=...&loja=...`). Quando `activeRole` ou `activeLoja` estiver setado, mostrar um link "Limpar filtros" com `href="/dashboard/usuarios"`.

    3. **Tabela** via `DataTable`. Colunas (UI-SPEC):
       - Nome — `users[].name`, com `Avatar` de iniciais.
       - N Vendedor — `numero_vendedor`, exibir `—` se null.
       - Loja — `LojaBadge` recebendo `store` (usar STORE_LABELS para o rotulo).
       - Role — `Badge` colorido por role.
       - Ativo — `Badge`: "ativo" / "inativo".
       - Ultimo acesso — `last_sign_in_at` formatado: "Nunca" se null, senao "Ha X dias" (calculo simples com `Date`).
       - Acoes — `DropdownMenu` com 3 itens: "Editar" -> `setEditingUser(row)`; "Desativar"/"Reativar" -> `setDisablingUser(row)`; "Reenviar convite" -> `handleReenviarConvite(row)` (a funcao `handleReenviarConvite` e implementada na Task 5 — nesta task deixar como `function handleReenviarConvite(_row: UserRow) {}` stub vazio para o build passar).
       Se a API do `DataTable` local nao usar `ColumnDef`, adaptar — montar a tabela com a estrutura que o arquivo expoe.

    4. **EmptyState**: se `users` estiver vazio apos filtro, renderizar `EmptyState` (icone Users, "Nenhum usuario encontrado", "Tente ajustar os filtros ou convide um novo membro.").

    5. **Skeleton**: o page.tsx ja faz o fetch server-side, entao a tabela chega populada — nao ha estado de loading da tabela em si. Se o UI-SPEC pedir skeleton, usar `Suspense` boundary OU simplesmente nao renderizar skeleton (a lista vem pronta do server). Documentar a escolha no SUMMARY.

    Regras: sem inline styles (zero `style={{`); usar classes Tailwind. Importar icones de `lucide-react` (`Users`, `UserPlus`, `Pencil`). `'use client'` na primeira linha. Os Sheets de convite/edicao e o Dialog de desativacao NAO sao renderizados nesta task — apenas os estados que os controlam. As Tasks 4 e 5 adicionam os componentes JSX de Sheet/Dialog ao final do return.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/usuarios/UsersClient.tsx` primeira linha e `'use client'`
    - O arquivo NAO contem a string `.active` nem `p.active` (campo renomeado para ativo)
    - O arquivo contem `DataTable` importado (ou o arquivo direto correspondente)
    - O arquivo contem os filtros via `Select` e `useRouter` para searchParams
    - O arquivo declara os estados `inviteOpen`, `editingUser`, `disablingUser`
    - O arquivo contem `EmptyState` para lista vazia
    - O arquivo NAO contem nenhum `style={{`
    - Note: este componente shadcn novo usa classes Tailwind; o padrao legado de inline `style={{}}` descrito no CLAUDE.md aplica-se apenas a arquivos pre-existentes nao reescritos nesta fase — `UsersClient.tsx` esta sendo totalmente reescrito e segue o design system v2
    - `npm run build` finaliza com exit code 0 (o stub de `handleReenviarConvite` evita erro de referencia)
  </acceptance_criteria>
  <done>UsersClient renderiza DataTable com filtros e DropdownMenu de acoes; estados de Sheet/Dialog declarados; EmptyState para lista vazia; build verde.</done>
</task>

<task type="auto">
  <name>Task 4: UsersClient.tsx — Sheet de convite (criar convite, link, copiar)</name>
  <read_first>
    - src/app/dashboard/usuarios/UsersClient.tsx (apos Task 3 — estrutura base ja existe)
    - src/components/ui/sheet.tsx (API do Sheet)
    - src/components/ui/select.tsx (API do Select)
    - src/components/ui/index.tsx (nomes exportados pelo barrel)
    - src/lib/auth/roles.ts (ASSIGNABLE_ROLES)
    - .planning/phases/03-auth-users/03-UI-SPEC.md (secao "Screen 2" — Invite Sheet e "Interaction Contracts" — copiar link)
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-05 — link sempre mostrado; "Specific Ideas" — botao copiar com feedback)
  </read_first>
  <action>
    Adicionar ao `src/app/dashboard/usuarios/UsersClient.tsx` (criado na Task 3) o **Sheet de convite**. NAO reescrever a tabela/filtros da Task 3 — apenas adicionar o Sheet ao final do return e os estados/handlers correspondentes.

    Estado adicional a declarar: campos do form de convite (`inviteEmail`, `inviteRole`, `inviteLoja`), `inviteLink` (string|null), `inviteEmailSent` (bool), `copied` (bool). Reaproveitar `loading` e `error` da Task 3.

    **Sheet de convite** (side="right"), aberto por `inviteOpen` (estado da Task 3). Titulo "Convidar usuario". Campos:
    - Email: `Input` opcional, placeholder "email@dasilva.com — opcional".
    - Role: `Select` com opcoes de `ASSIGNABLE_ROLES` (vendedor/gerente/adm).
    - Loja: `Select` com Jebai/Paje-MKT/Paje-Caixa.
    - Botao "Gerar convite": `handleGerarConvite` que faz
      `fetch('/api/admin/invite', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ email: inviteEmail || undefined, role: inviteRole, loja: inviteLoja }) })`.
      IMPORTANTE: enviar `email: inviteEmail || undefined` — se o campo estiver vazio, omitir a chave `email` (a rota /api/admin/invite trata email omitido como convite link-only).
    - Apos sucesso: setar `inviteLink` com o `link` retornado e `inviteEmailSent` com `email_sent`. Exibir o link num bloco monospace (`bg-card border border-border rounded-md p-3 font-mono text-[12px] break-all`) + botao "Copiar link". Se `inviteEmailSent === true`, mostrar "Email enviado para {inviteEmail}". Mostrar nota "Link valido por 7 dias".
    - Em erro: `setError(data.error ?? 'Falha ao gerar convite.')`.

    **Interacao "Copiar link"** (UI-SPEC "Interaction Contracts"): botao `variant="outline"` com icone `Copy` (lucide). Ao clicar: `navigator.clipboard.writeText(inviteLink)`, setar `copied=true`, trocar o icone para `Check` e o texto para "Link copiado" por 1500ms (`setTimeout` -> `setCopied(false)`). Em falha do clipboard (catch), manter o link visivel para copia manual.

    Ao fechar o Sheet (onOpenChange false): resetar `inviteLink`, `inviteEmailSent`, `copied`, `error` e os campos do form.

    Regras: sem inline styles (zero `style={{`); classes Tailwind. Importar icones `Copy`, `Check` de `lucide-react`. Confirmar nomes exatos do `Sheet` no barrel.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `UsersClient.tsx` contem `api/admin/invite` no fetch do Sheet de convite
    - `UsersClient.tsx` contem `email: inviteEmail || undefined` (email opcional — omitido se vazio)
    - `UsersClient.tsx` contem `navigator.clipboard.writeText` (botao copiar link)
    - `UsersClient.tsx` contem `ASSIGNABLE_ROLES` importado de `@/lib/auth/roles`
    - `UsersClient.tsx` importa `Sheet` (ou o arquivo direto correspondente)
    - `UsersClient.tsx` contem os icones `Copy` e `Check` de `lucide-react`
    - O arquivo NAO contem nenhum `style={{`
    - Note: o Sheet usa componentes shadcn com Tailwind; o padrao legado de inline `style={{}}` do CLAUDE.md nao se aplica a este arquivo reescrito
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Sheet de convite cria convite via /api/admin/invite (email opcional), exibe link com botao copiar (feedback Check/1500ms); build verde.</done>
</task>

<task type="auto">
  <name>Task 5: UsersClient.tsx — Sheet de edicao, Dialog de desativacao e acao Reenviar convite</name>
  <read_first>
    - src/app/dashboard/usuarios/UsersClient.tsx (apos Tasks 3 e 4 — estrutura base + Sheet de convite ja existem)
    - src/components/ui/sheet.tsx (API do Sheet)
    - src/components/ui/dialog.tsx (API do Dialog)
    - src/components/ui/select.tsx (API do Select)
    - src/components/ui/switch.tsx (API do Switch)
    - src/components/ui/index.tsx (nomes exportados pelo barrel)
    - src/lib/auth/roles.ts (ASSIGNABLE_ROLES)
    - .planning/phases/03-auth-users/03-UI-SPEC.md (secao "Screen 2" — Edit Sheet, Disable Dialog, copy, interaction contracts)
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-08, D-09)
    - .planning/REQUIREMENTS.md (AUTH-06 — acao "reenviar convite")
  </read_first>
  <action>
    Adicionar ao `src/app/dashboard/usuarios/UsersClient.tsx` (apos Tasks 3 e 4) o **Sheet de edicao**, o **Dialog de desativacao** e a implementacao da funcao **handleReenviarConvite** (que na Task 3 ficou como stub vazio). NAO reescrever a tabela/filtros nem o Sheet de convite — apenas adicionar estes 3 elementos.

    Estado adicional: campos do form de edicao (`editName`, `editRole`, `editStore`, `editAtivo`) — inicializados a partir de `editingUser` quando ele muda (via `useEffect` ou ao abrir o Sheet). Reaproveitar `loading` e `error`.

    --- 1. Sheet de edicao (side="right"), aberto por `editingUser` (estado da Task 3) ---

    Titulo "Editar usuario" (D-08). Campos pre-preenchidos a partir de `editingUser`: Nome (`Input`), Role (`Select` de `ASSIGNABLE_ROLES`), Loja (`Select` de Jebai/Paje-MKT/Paje-Caixa), Ativo (`Switch`).

    Botao "Salvar alteracoes" chama `handleSave`. Logica EXATA de `handleSave`:
    1. SEMPRE chama `PATCH` (usar method 'POST' — a rota update-user e POST; "PATCH" aqui e conceitual) para `/api/admin/update-user` com body `{ userId: editingUser.id, name: editName, role: editRole, store: editStore }`. Aguardar a resposta. Se `!res.ok` -> `setError(data.error)`, parar a sequencia (NAO prosseguir para o passo 2).
    2. Se o `Switch` de `ativo` mudou de `true` para `false` (comparar `editAtivo` com `editingUser.ativo`), chamar adicionalmente `POST /api/admin/disable-user` com body `{ userId: editingUser.id, ativo: false }`. Se `ativo` mudou de `false` para `true`, chamar `POST /api/admin/disable-user` com `{ userId: editingUser.id, ativo: true }` (remove o ban). Se `ativo` nao mudou, NAO chamar disable-user.
    3. Os dois fetch rodam SEQUENCIALMENTE (update-user primeiro, depois disable-user). Os erros sao tratados independentemente: o PRIMEIRO erro interrompe a sequencia — se update-user falhar, disable-user nao e chamado; se update-user ok mas disable-user falhar, mostrar o erro do disable-user.
    4. Em sucesso de toda a sequencia: `router.refresh()` para recarregar a lista do servidor e fechar o Sheet.

    Motivo da sequencia (D-09): a invalidacao de sessao por ban so acontece via disable-user (`ban_duration`); por isso a mudanca de `ativo` NAO vai no body do update-user — vai numa chamada separada a disable-user. update-user cuida de name/role/store; disable-user cuida do ban.

    Botao "Descartar alteracoes" (variant="outline") fecha o Sheet sem salvar.

    --- 2. Dialog de desativacao, aberto por `disablingUser` (estado da Task 3) ---

    Titulo "Desativar usuario". Body: "Isso encerrara a sessao ativa de {nome} imediatamente. Esta acao pode ser revertida reativando o usuario." Botoes: "Manter usuario ativo" (outline, fecha) e "Desativar" (variant="destructive") -> `fetch('/api/admin/disable-user', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ userId: disablingUser.id, ativo: false }) })`. Apos sucesso: `router.refresh()` e fecha o Dialog. Se o usuario ja esta inativo (`disablingUser.ativo === false`), o item do menu vira "Reativar", o Dialog ajusta o texto ("Isso reativara o acesso de {nome}.") e chama disable-user com `ativo: true`.

    --- 3. Implementar handleReenviarConvite (substituir o stub vazio da Task 3) ---

    Funcao `handleReenviarConvite(row: UserRow)` — acao do item "Reenviar convite" do DropdownMenu (AUTH-06 exige esta acao; NAO e placeholder desabilitado). Comportamento:
    - Faz `fetch('/api/admin/invite', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ role: row.role, loja: row.store, email: row.email ?? undefined }) })`.
      - O `UserRow` vem de `profiles` e nao tem coluna `email` confiavel para todos os usuarios (alguns foram criados com placeholder `@dasilva.local`). Por isso `email: row.email ?? undefined` — se nao houver email real, a chave e omitida e a rota /api/admin/invite gera um convite **link-only** (modo ja suportado pela rota, ver interfaces). Se `UserRow` nao tiver campo `email`, omitir a chave email por completo (sempre gerar link-only).
    - Em sucesso: a resposta traz `link`. Reaproveitar o mesmo bloco visual do Sheet de convite — abrir o Sheet de convite (`setInviteOpen(true)`) ja com `inviteLink` setado, OU mostrar um toast/inline com o link e botao copiar. Escolha mais simples: setar `inviteLink` com o link retornado e `setInviteOpen(true)` — o Sheet de convite (Task 4) ja renderiza o bloco de link + copiar quando `inviteLink` esta setado.
    - Em erro: `setError(data.error ?? 'Falha ao reenviar convite.')`.
    - O item de menu "Reenviar convite" fica SEMPRE habilitado (sem `disabled`, sem tooltip "Em breve").

    Regras: sem inline styles (zero `style={{`); classes Tailwind. Confirmar nomes exatos de `Sheet`, `Dialog`, `Switch` no barrel — se algum nao estiver no barrel, importar do arquivo direto. Importar icone `Pencil` de `lucide-react` se necessario.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `UsersClient.tsx` contem `api/admin/update-user` no fetch do `handleSave`
    - `UsersClient.tsx` contem `api/admin/disable-user` no fetch (Dialog E handleSave quando ativo muda)
    - `UsersClient.tsx` contem a string `Reenviar convite` (item de menu, nao placeholder)
    - `handleReenviarConvite` contem `fetch('/api/admin/invite'` (chamada real, NAO `disabled`/"Em breve")
    - `handleSave` chama update-user primeiro e disable-user depois, sequencialmente, so quando `ativo` muda
    - O item de menu "Reenviar convite" NAO contem `disabled` nem o texto "Em breve"
    - O arquivo NAO contem `create-user` (fluxo antigo de criacao direta por senha removido)
    - O arquivo NAO contem nenhum `style={{`
    - Note: Sheet/Dialog/Switch sao componentes shadcn novos com Tailwind; o padrao legado de inline `style={{}}` do CLAUDE.md aplica-se apenas a arquivos pre-existentes nao reescritos nesta fase
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Sheet de edicao salva via update-user+disable-user sequencial; Dialog de desativacao chama disable-user; handleReenviarConvite chama /api/admin/invite e exibe o link; build verde — falha do Plan 01 resolvida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/admin/disable-user, update-user, invite | Cliente envia mutacoes de usuario; o handler nao confia em role/identidade do body |
| Server component -> admin.listUsers | listUsers retorna usuarios de TODOS os tenants do projeto |
| Gerente -> usuarios de outros tenants | A tabela deve mostrar apenas o tenant do gerente logado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Information Disclosure | listUsers vaza usuarios de outros tenants (incl. PetHub no banco compartilhado) | mitigate | page.tsx faz `.eq('tenant_id', tenantId)` no fetch de profiles e SO faz merge de `last_sign_in_at` para esses ids. authUsers de outros tenants nunca chegam a `users` (Pitfall 4) |
| T-3-26 | Elevation of Privilege | disable-user/update-user chamados por vendedor | mitigate | Ambos os handlers checam `canInvite(callerRole)` via `app_metadata.role` e retornam 403. Role do caller nunca vem do body |
| T-3-27 | Denial of Service | Gerente desativa a si mesmo e perde acesso | mitigate | disable-user rejeita `userId === user.id` com 400 |
| T-3-28 | Elevation of Privilege | Cliente edita role para super_admin via update-user | mitigate | `update-user` valida `isValidRole`; a UI so oferece `ASSIGNABLE_ROLES` (sem super_admin). Risco residual de chamada direta a API com role=adm: aceitavel — caller ja e role de alto privilegio (D-01) |
| T-3-29 | Denial of Service (reverse) | Sessao ativa persiste apos desativacao | mitigate | disable-user aplica `ban_duration` que invalida refresh tokens; janela maxima ~1h ate expirar o JWT (RESEARCH Pitfall 6 — aceitavel para caso de demissao) |
| T-3-30 | Spoofing | RLS de profiles permitiria gerente ler profiles de outro tenant | accept | RLS da migration 0010 (`profiles_tenant_select`) ja restringe SELECT ao proprio tenant; o `.eq('tenant_id', ...)` e defesa em profundidade redundante |
| T-3-31 | Elevation of Privilege | Reenviar convite gera convite com role superior ao do caller | mitigate | handleReenviarConvite usa `row.role` (o role atual do usuario, ja persistido); /api/admin/invite valida o caller via `canInvite` e o role solicitado contra `ASSIGNABLE_ROLES`. Nenhum privilegio novo e criado pela acao de reenvio |

ASVS L1: V4.1 (autorizacao server-side por role do JWT), V4.2 (isolamento multi-tenant no fetch), V5.1 (validacao de role no update), V3.3 (invalidacao de sessao via ban_duration).
</threat_model>

<verification>
- `npm run build` exit code 0 (resolve definitivamente a falha do Plan 01 — UsersClient nao usa mais `.active`)
- `npm run lint` sem erros criticos
- Teste manual AUTH-06: tabela mostra as 6 colunas; filtros de role e loja funcionam; Sheet de edicao abre com campos preenchidos; Dialog de desativacao chama disable-user; Sheet de convite gera link com botao copiar; acao "Reenviar convite" gera um novo link de convite
- Teste manual Pitfall 4: confirmar que a tabela NAO mostra emails do PetHub (so usuarios do tenant gds-interno)
</verification>

<success_criteria>
- Tabela de usuarios mostra nome, numero vendedor, loja, role, ativo, ultimo acesso com filtros por role e loja
- Editar abre Sheet lateral com nome/role/loja/ativo (D-08); save sequencia update-user + disable-user
- Desativar usa ban_duration via disable-user (D-09)
- Convidar usuario abre Sheet que cria convite e exibe link com botao copiar (D-05)
- Reenviar convite chama /api/admin/invite e gera novo link (AUTH-06 — acao funcional, nao placeholder)
- Tabela isolada por tenant (Pitfall 4)
- `npm run build` verde
</success_criteria>

<output>
Apos completar, criar `.planning/phases/03-auth-users/03-06-SUMMARY.md`.
</output>
