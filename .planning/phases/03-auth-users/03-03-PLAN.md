---
phase: 03-auth-users
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/auth/roles.ts
  - src/app/api/admin/invite/route.ts
autonomous: true
requirements: [AUTH-03]
must_haves:
  truths:
    - "POST /api/admin/invite chamado por um vendedor retorna 403"
    - "POST /api/admin/invite chamado por adm/gerente/super_admin cria uma linha em convites"
    - "A resposta de sucesso contem um link no formato {APP_URL}/convite/{token}"
    - "O convite criado tem token UUID, role, loja, tenant_id e criado_por preenchidos"
    - "Existe utilitario setUserRole reutilizavel que atualiza app_metadata.role e profiles.role juntos"
  artifacts:
    - path: "src/lib/auth/roles.ts"
      provides: "Constantes de roles e funcao setUserRole(admin, userId, role)"
      exports: ["INVITE_ALLOWED_ROLES", "setUserRole", "ASSIGNABLE_ROLES"]
    - path: "src/app/api/admin/invite/route.ts"
      provides: "Route handler POST que cria convite e retorna link"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/admin/invite/route.ts"
      to: "convites table"
      via: "admin.from('convites').insert"
      pattern: "from\\('convites'\\)"
    - from: "src/app/api/admin/invite/route.ts"
      to: "caller app_metadata.role"
      via: "getUser() + checagem de INVITE_ALLOWED_ROLES"
      pattern: "app_metadata"
---

<objective>
Construir o backend de criacao de convites: um utilitario de roles reutilizavel (`src/lib/auth/roles.ts`) e o route handler `POST /api/admin/invite` que valida o caller, insere uma linha na tabela `convites` e retorna o link `/convite/[token]` para a UI exibir.

Purpose: AUTH-03 e D-02/D-05 — gerente/adm/super_admin criam convites; o sistema gera o link. Este plano entrega a logica server-side; a UI do Sheet de convite e wired no Plan 06. O utilitario `setUserRole` centraliza a regra D-04 (app_metadata + profiles juntos) e sera consumido tambem pelos Plans 04 e 06.
Output: `src/lib/auth/roles.ts` e `src/app/api/admin/invite/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-auth-users/03-CONTEXT.md
@.planning/phases/03-auth-users/03-RESEARCH.md

<interfaces>
<!-- Contratos para o executor. Usar diretamente. -->

Schema da tabela convites (migration 0007 + coluna `loja` adicionada pela 0014):
```sql
-- id uuid PK DEFAULT gen_random_uuid()
-- tenant_id uuid NOT NULL REFERENCES tenants(id)
-- email text (nullable)
-- role text NOT NULL  -- aceita vendedor|adm|gerente|super_admin
-- loja_id uuid (nullable, IGNORAR — usar coluna loja)
-- loja text (nullable) -- ADICIONADA pela migration 0014; usar esta
-- token text UNIQUE DEFAULT gen_random_uuid()::text  -- gerado pelo Postgres
-- usado boolean DEFAULT false
-- expira_em timestamptz DEFAULT now() + interval '7 days'
-- criado_por uuid NOT NULL REFERENCES profiles(id)
-- criado_em timestamptz DEFAULT now()
```

Padrao de admin client (de src/app/api/admin/create-user/route.ts — ja existe no codebase):
```typescript
import { createClient } from '@supabase/supabase-js'
const admin = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
  { auth: { autoRefreshToken: false, persistSession: false } }
)
```

Padrao de verificacao do caller (de create-user/route.ts) — ADAPTAR para usar app_metadata:
```typescript
import { createClient as createServerClient } from '@/lib/supabase/server'
const caller = await createServerClient()
const { data: { user } } = await caller.auth.getUser()
if (!user) return NextResponse.json({ error: 'Nao autorizado' }, { status: 401 })
// NOVO: role vem de app_metadata, nao de query a profiles
const role = user.app_metadata?.role as string | undefined
```

Convencao do projeto (CONVENTIONS.md): route handlers retornam `NextResponse.json(...)`; auth check primeiro (verify user -> verify role -> proceed); erros como `NextResponse.json({ error: string }, { status: N })`.

UserRole (de src/types/index.ts apos Plan 01): `'vendedor' | 'adm' | 'gerente' | 'super_admin'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar utilitario src/lib/auth/roles.ts</name>
  <read_first>
    - src/types/index.ts (tipo UserRole — apos Plan 01 tem 4 valores)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Pattern 2: Atualizar app_metadata + profiles em conjunto" e "Pitfall 2")
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-02 — quem cria convites; D-04 — app_metadata + profiles juntos)
  </read_first>
  <action>
    Criar o diretorio `src/lib/auth/` e o arquivo `src/lib/auth/roles.ts`. Este modulo centraliza a regra D-04 (mutacao de role toca dois lugares) — sera reutilizado pelos Plans 04 e 06. Conteudo EXATO:

    ```typescript
    import type { SupabaseClient } from '@supabase/supabase-js'
    import type { UserRole } from '@/types'

    // Roles que podem criar convites (D-02)
    export const INVITE_ALLOWED_ROLES: UserRole[] = ['adm', 'gerente', 'super_admin']

    // Roles que um gerente/adm pode atribuir a um novo usuario via convite ou edicao.
    // super_admin nao e atribuivel pela UI — so existe via configuracao direta.
    export const ASSIGNABLE_ROLES: UserRole[] = ['vendedor', 'gerente', 'adm']

    // Verifica se um valor de role e um dos 4 roles validos
    export function isValidRole(value: unknown): value is UserRole {
      return value === 'vendedor' || value === 'adm'
        || value === 'gerente' || value === 'super_admin'
    }

    // Verifica se o role do caller pode criar convites
    export function canInvite(role: string | undefined): boolean {
      return !!role && (INVITE_ALLOWED_ROLES as string[]).includes(role)
    }

    // D-04: toda mutacao de role deve atualizar app_metadata (fonte de verdade
    // do middleware) E profiles.role (fonte de verdade das queries de UI).
    // `admin` deve ser um SupabaseClient criado com a service role key.
    // A mudanca de app_metadata so reflete no middleware apos logout/login.
    export async function setUserRole(
      admin: SupabaseClient,
      userId: string,
      role: UserRole,
    ): Promise<{ error: string | null }> {
      const { error: metaError } = await admin.auth.admin.updateUserById(userId, {
        app_metadata: { role },
      })
      if (metaError) return { error: metaError.message }

      const { error: profileError } = await admin
        .from('profiles')
        .update({ role })
        .eq('id', userId)
      if (profileError) return { error: profileError.message }

      return { error: null }
    }
    ```

    NAO criar barrel para `src/lib/` (CONVENTIONS.md diz que `src/lib/` nao tem barrel — imports diretos). NAO usar `any` (TypeScript strict).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/auth/roles.ts` existe
    - O arquivo exporta `INVITE_ALLOWED_ROLES`, `ASSIGNABLE_ROLES`, `setUserRole`, `canInvite`, `isValidRole`
    - `INVITE_ALLOWED_ROLES` contem exatamente `'adm'`, `'gerente'`, `'super_admin'`
    - A funcao `setUserRole` contem chamada a `updateUserById` E a `.from('profiles').update`
    - O arquivo NAO contem a string `any` como tipo
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Utilitario de roles criado; setUserRole atualiza app_metadata e profiles juntos; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar route handler POST /api/admin/invite</name>
  <read_first>
    - src/app/api/admin/create-user/route.ts (padrao de route handler admin existente — copiar estrutura de auth check e admin client)
    - src/lib/auth/roles.ts (criado na Task 1 — usar canInvite e ASSIGNABLE_ROLES)
    - supabase/migrations/0007_convites.sql (schema da tabela convites)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Pattern 3: Criacao de convite")
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-02, D-05 — quem cria, link sempre retornado)
  </read_first>
  <action>
    Criar `src/app/api/admin/invite/route.ts`. Route handler `POST` que valida o caller via `app_metadata.role`, insere em `convites` e retorna o link. Conteudo EXATO:

    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import { NextRequest, NextResponse } from 'next/server'
    import { createClient as createServerClient } from '@/lib/supabase/server'
    import { canInvite, isValidRole } from '@/lib/auth/roles'

    export async function POST(req: NextRequest) {
      // 1. Verificar caller autenticado
      const caller = await createServerClient()
      const { data: { user } } = await caller.auth.getUser()
      if (!user) {
        return NextResponse.json({ error: 'Nao autorizado' }, { status: 401 })
      }

      // 2. Verificar caller pode criar convites (D-02) — via app_metadata
      const callerRole = user.app_metadata?.role as string | undefined
      if (!canInvite(callerRole)) {
        return NextResponse.json({ error: 'Acesso negado' }, { status: 403 })
      }

      // 3. Validar payload
      const body = await req.json()
      const email: string | null = body.email?.trim() || null
      const role: unknown = body.role
      const loja: string | null = body.loja?.trim() || null

      if (!isValidRole(role)) {
        return NextResponse.json({ error: 'Role invalido' }, { status: 400 })
      }
      if (!loja) {
        return NextResponse.json({ error: 'Loja e obrigatoria' }, { status: 400 })
      }

      // 4. Descobrir tenant_id do caller (convite herda o tenant de quem cria)
      const admin = createClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.SUPABASE_SERVICE_ROLE_KEY!,
        { auth: { autoRefreshToken: false, persistSession: false } }
      )

      const { data: callerProfile, error: profErr } = await admin
        .from('profiles')
        .select('tenant_id')
        .eq('id', user.id)
        .single()
      if (profErr || !callerProfile?.tenant_id) {
        return NextResponse.json({ error: 'Tenant do criador nao encontrado' }, { status: 400 })
      }

      // 5. Inserir convite — token e expira_em vem dos DEFAULTs do Postgres
      const { data: convite, error: insertErr } = await admin
        .from('convites')
        .insert({
          tenant_id: callerProfile.tenant_id,
          email,
          role,
          loja,
          criado_por: user.id,
        })
        .select('token, expira_em')
        .single()
      if (insertErr || !convite) {
        return NextResponse.json(
          { error: insertErr?.message ?? 'Falha ao criar convite' },
          { status: 400 }
        )
      }

      // 6. Construir link e retornar (D-05: link sempre retornado)
      const appUrl = process.env.NEXT_PUBLIC_APP_URL
        ?? req.nextUrl.origin
      const link = `${appUrl}/convite/${convite.token}`

      return NextResponse.json({
        link,
        token: convite.token,
        expira_em: convite.expira_em,
        email_requested: !!email,
      })
    }
    ```

    Notas: (1) o token NAO e gerado no JS — vem do DEFAULT `gen_random_uuid()::text` do schema (criptograficamente seguro, RESEARCH "Don't Hand-Roll"); (2) o envio de email NAO acontece aqui — sera adicionado no Plan 05; este handler so retorna `email_requested` para a UI saber; (3) `NEXT_PUBLIC_APP_URL` pode nao estar configurado — fallback para `req.nextUrl.origin` cobre dev e prod.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/admin/invite/route.ts` existe e exporta `POST`
    - O arquivo contem `from('convites')` com `.insert(`
    - O arquivo contem `canInvite(callerRole)` e retorna status 403 quando falso
    - O arquivo contem `user.app_metadata?.role` (caller verificado via JWT, nao via query de role)
    - O arquivo contem a string `/convite/` na construcao do link
    - O arquivo NAO contem `Math.random` nem geracao manual de token
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>POST /api/admin/invite valida caller, cria convite com token do Postgres e retorna link; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> /api/admin/invite | Cliente envia payload de convite; o handler nao confia no role enviado pelo cliente |
| Route handler -> service role | O handler usa a service role key (bypass de RLS) para inserir em convites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Elevation of Privilege | POST /api/admin/invite chamado por vendedor | mitigate | `canInvite(callerRole)` le `user.app_metadata.role` (validado pelo `getUser()`) e retorna 403 se nao for adm/gerente/super_admin. O role do caller nunca vem do body |
| T-3-04 | Tampering | Token de convite previsivel/brute-forcavel | mitigate | Token gerado pelo DEFAULT `gen_random_uuid()::text` do Postgres (122 bits de entropia) — nunca `Math.random()`. RESEARCH "Don't Hand-Roll" |
| T-3-14 | Elevation of Privilege | Caller pede role super_admin para o convidado | accept | `isValidRole` aceita os 4 roles; a UI (Plan 06) restringe a `ASSIGNABLE_ROLES`. Risco residual: um caller adm com chamada direta a API poderia convidar outro adm — aceitavel, pois adm ja e role de alto privilegio por D-01 |
| T-3-15 | Spoofing | Caller forja tenant_id de outro tenant | mitigate | `tenant_id` do convite NAO vem do body — e lido de `profiles.tenant_id` do proprio caller. Convite sempre herda o tenant de quem cria |
| T-3-16 | Information Disclosure | Service role key exposta ao cliente | mitigate | `SUPABASE_SERVICE_ROLE_KEY` lido apenas em route handler server-side; nunca prefixado com `NEXT_PUBLIC_` |

ASVS L1: V4.1 (autorizacao server-side, role do JWT), V5.1 (validacao de input — role e loja), V6.2 (token via gerador criptografico do Postgres).
</threat_model>

<verification>
- `npm run build` exit code 0
- `npm run lint` sem erros criticos
- Teste manual AUTH-03: POST /api/admin/invite com sessao de vendedor -> 403; com sessao de gerente -> 200 com `link`; verificar nova linha em `convites` com token preenchido
</verification>

<success_criteria>
- `src/lib/auth/roles.ts` exporta `setUserRole`, `canInvite`, `INVITE_ALLOWED_ROLES`, `ASSIGNABLE_ROLES`
- `POST /api/admin/invite` retorna 403 para vendedor, cria convite para adm/gerente/super_admin
- Resposta de sucesso inclui link `/convite/[token]`
</success_criteria>

<output>
Apos completar, criar `.planning/phases/03-auth-users/03-03-SUMMARY.md`.
</output>
