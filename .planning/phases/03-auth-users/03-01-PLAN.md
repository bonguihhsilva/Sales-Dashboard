---
phase: 03-auth-users
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0014_auth_roles.sql
  - src/types/index.ts
  - src/middleware.ts
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Schema profiles aceita os 4 roles (vendedor, adm, gerente, super_admin) sem violar CHECK constraint"
    - "Campo de status do usuário chama-se profiles.ativo (renomeado de active)"
    - "Middleware redireciona vendedor para /meu-resultado ao tentar acessar /dashboard"
    - "Middleware redireciona adm/gerente para /dashboard ao tentar acessar /admin"
    - "Middleware permite /convite/[token] sem sessão (rota pública)"
    - "Middleware le role via user.app_metadata.role sem nenhuma query ao banco"
  artifacts:
    - path: "supabase/migrations/0014_auth_roles.sql"
      provides: "Rename active->ativo, CHECK de 4 roles, coluna convites.loja"
      contains: "ALTER TABLE profiles"
    - path: "src/types/index.ts"
      provides: "UserRole com 4 valores e Profile atualizado"
      contains: "super_admin"
    - path: "src/middleware.ts"
      provides: "RBAC por app_metadata.role com 5 grupos de rotas"
      contains: "ROLE_RULES"
  key_links:
    - from: "src/middleware.ts"
      to: "user.app_metadata.role"
      via: "leitura do JWT retornado por getUser()"
      pattern: "app_metadata"
    - from: "src/middleware.ts"
      to: "PUBLIC_PREFIXES"
      via: "checagem de prefixo antes do auth guard"
      pattern: "/convite"
---

<objective>
Estabelecer a fundação de autenticação da fase: corrigir o schema do banco (que ainda restringe roles a `adm | vendedor` e usa `active`), expandir os tipos TypeScript para 4 roles, e reescrever o middleware para proteger 5 grupos de rotas lendo o role do JWT `app_metadata` — eliminando a query DB feita hoje a cada request.

Purpose: Nenhuma outra feature de auth (convite, login, tabela de usuários) pode ser testada antes do middleware reconhecer os 4 roles e antes do schema aceitar `gerente`/`super_admin`. Este plano é Wave 1 — bloqueia todo o resto.
Output: Migration 0014 aplicável, `src/types/index.ts` com `UserRole` de 4 valores, `src/middleware.ts` reescrito sem query ao banco.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-users/03-CONTEXT.md
@.planning/phases/03-auth-users/03-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraidos do codebase. Usar diretamente — sem exploracao. -->

Estado ATUAL de src/types/index.ts (linhas 1-10):
```typescript
export type UserRole = 'adm' | 'vendedor'

export interface Profile {
  id: string
  name: string
  role: UserRole
  vendor_id: string | null
  store: string | null
  active: boolean
}
```

Schema ATUAL de profiles (migration 0001 + 0002):
```sql
-- 0001: CHECK (role IN ('adm', 'vendedor')), coluna 'active boolean'
-- 0002: ADD COLUMN tenant_id uuid, numero_vendedor text, data_admissao date
-- NAO existe coluna loja_id. As 3 lojas vivem em profiles.store (text).
```

Schema ATUAL de convites (migration 0007):
```sql
-- token text UNIQUE DEFAULT gen_random_uuid()::text
-- role text NOT NULL, loja_id uuid REFERENCES tenants(id)
-- usado boolean DEFAULT false, expira_em timestamptz DEFAULT now() + interval '7 days'
-- criado_por uuid REFERENCES profiles(id)
```

Valores de loja existentes (de STORE_LABELS em src/types/index.ts):
```typescript
// 'Jebai' -> 'Jebai' | 'Paje-MKT' -> 'Paje 1' | 'Paje-Caixa' -> 'Paje 2'
```

Trigger handle_new_user (migration 0010) — insere profile com role do user_metadata:
```sql
-- INSERT INTO profiles (id, name, role) VALUES (NEW.id, ..., COALESCE(NEW.raw_user_meta_data->>'role', 'vendedor'))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Migration de correcao de schema (roles + ativo + convites.loja)</name>
  <read_first>
    - supabase/migrations/0001_schema_inicial.sql (linhas 11-19 — CHECK constraint de role e coluna `active`)
    - supabase/migrations/0002_tenants.sql (confirma que loja_id NAO foi adicionado a profiles)
    - supabase/migrations/0007_convites.sql (schema completo de convites)
    - supabase/migrations/0010_rls_policies.sql (linhas 211-234 — funcao handle_new_user atual)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Schema Notes")
  </read_first>
  <action>
    Criar `supabase/migrations/0014_auth_roles.sql`. Este arquivo resolve 3 bloqueadores de schema identificados no RESEARCH.md. Conteudo EXATO:

    ```sql
    -- =============================================================
    -- Migration 0014 — Auth: 4 roles, rename active->ativo, convites.loja
    -- Phase 3 — Auth & Users
    -- =============================================================

    -- 1. Expandir CHECK constraint de role para 4 roles
    -- O CHECK da 0001 so aceita 'adm'|'vendedor' — bloqueia gerente/super_admin
    ALTER TABLE profiles DROP CONSTRAINT IF EXISTS profiles_role_check;
    ALTER TABLE profiles
      ADD CONSTRAINT profiles_role_check
      CHECK (role IN ('vendedor', 'adm', 'gerente', 'super_admin'));

    -- 2. Renomear coluna active -> ativo (alinha com CONTEXT D-09 e UI-SPEC)
    ALTER TABLE profiles RENAME COLUMN active TO ativo;

    -- 3. Adicionar coluna loja (text) em convites
    -- convites.loja_id referencia tenants(id), mas as 3 lojas vivem em
    -- profiles.store (text). Usar campo text mantem compatibilidade.
    ALTER TABLE convites ADD COLUMN IF NOT EXISTS loja text;

    -- 4. Atualizar trigger handle_new_user para registrar o ban como nao-ativo
    -- (mantem logica existente; apenas garante coluna correta)
    -- Nenhuma mudanca necessaria no corpo do trigger — ele insere apenas
    -- id/name/role; ativo usa o DEFAULT true da coluna.
    ```

    NAO criar tabela `lojas`. NAO criar 3 tenants separados. NAO modificar `convites.loja_id` existente. As lojas sao valores text — decisao de escopo minimo do planner confirmada no RESEARCH.md secao "Schema Notes" opcao 2.

    Aplicar a migration no banco via Supabase MCP (`apply_migration` com name `0014_auth_roles`) OU via `supabase db push` se o ambiente tiver CLI conectada. Se nenhum acesso ao banco estiver disponivel no momento da execucao, deixar a migration criada no arquivo e registrar no SUMMARY que ela precisa ser aplicada manualmente — ela e idempotente exceto pelo RENAME (que falha se ja renomeado; isso e aceitavel e sinaliza que ja foi aplicada).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/0014_auth_roles.sql','utf8');if(!s.includes('super_admin'))process.exit(1);if(!s.includes('RENAME COLUMN active TO ativo'))process.exit(1);if(!s.includes('convites ADD COLUMN'))process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0014_auth_roles.sql` existe
    - O arquivo contem a string `CHECK (role IN ('vendedor', 'adm', 'gerente', 'super_admin'))`
    - O arquivo contem a string `RENAME COLUMN active TO ativo`
    - O arquivo contem a string `ALTER TABLE convites ADD COLUMN IF NOT EXISTS loja text`
    - O arquivo NAO contem `CREATE TABLE lojas`
    - O arquivo NAO contem `INSERT INTO tenants`
  </acceptance_criteria>
  <done>Migration 0014 criada com os 3 fixes de schema; aplicada ao banco ou marcada para aplicacao manual no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Expandir UserRole e Profile em src/types/index.ts</name>
  <read_first>
    - src/types/index.ts (linhas 1-10 — definicao atual de UserRole e Profile)
    - supabase/migrations/0002_tenants.sql (campos adicionados a profiles: tenant_id, numero_vendedor, data_admissao)
  </read_first>
  <action>
    Editar `src/types/index.ts`. Substituir as linhas 1-10 (definicao de `UserRole` e `interface Profile`) por:

    ```typescript
    export type UserRole = 'vendedor' | 'adm' | 'gerente' | 'super_admin'

    export interface Profile {
      id: string
      name: string
      role: UserRole
      vendor_id: string | null
      numero_vendedor: string | null
      store: string | null
      tenant_id: string | null
      data_admissao: string | null
      ativo: boolean
      created_at: string
    }
    ```

    Mudancas: (1) `UserRole` passa de 2 para 4 valores; (2) `Profile.active` renomeado para `ativo` (coerente com migration 0014); (3) adicionados os campos que a migration 0002 ja criou no banco mas faltavam no tipo: `numero_vendedor`, `tenant_id`, `data_admissao`, `created_at`.

    NAO remover nem alterar nenhuma outra interface ou constante do arquivo (Period, Goal, VendorSummary, STORE_COLORS, STORE_LABELS etc. permanecem intactas).

    Apos editar, rodar `npm run build` — esperado que falhe em `src/app/dashboard/usuarios/UsersClient.tsx` linha 49 (usa `p.active`). NAO corrigir esse arquivo aqui — ele sera totalmente reescrito no Plan 06. Se o build falhar APENAS por causa de `.active` em UsersClient.tsx, isso e esperado e aceitavel para este plano; registrar no SUMMARY. Se falhar por outro motivo, corrigir.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/types/index.ts','utf8');if(!s.includes(\"'vendedor' | 'adm' | 'gerente' | 'super_admin'\"))process.exit(1);if(!s.includes('numero_vendedor'))process.exit(1);if(s.includes('active: boolean'))process.exit(1);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` contem `export type UserRole = 'vendedor' | 'adm' | 'gerente' | 'super_admin'`
    - `src/types/index.ts` interface Profile contem campo `ativo: boolean`
    - `src/types/index.ts` interface Profile contem campo `numero_vendedor: string | null`
    - `src/types/index.ts` interface Profile contem campo `tenant_id: string | null`
    - `src/types/index.ts` NAO contem mais a string `active: boolean`
    - `src/types/index.ts` ainda contem `export const STORE_LABELS` e `export interface VendorSummary`
  </acceptance_criteria>
  <done>UserRole tem 4 valores; Profile reflete o schema real do banco; STORE_LABELS e demais tipos intactos.</done>
</task>

<task type="auto">
  <name>Task 3: Reescrever src/middleware.ts — RBAC via app_metadata, 5 grupos de rotas</name>
  <read_first>
    - src/middleware.ts (arquivo inteiro — implementacao atual com query DB a profiles)
    - .planning/phases/03-auth-users/03-RESEARCH.md (secao "Code Examples" > "Middleware expandido para 4 roles" — codigo de referencia completo)
    - .planning/phases/03-auth-users/03-CONTEXT.md (D-03, D-04 — route mapping e regra app_metadata)
  </read_first>
  <action>
    Substituir TODO o conteudo de `src/middleware.ts` pelo seguinte (conteudo EXATO — TypeScript strict, anotacoes de tipo explicitas nos cookies conforme exige o CLAUDE.md):

    ```typescript
    import { createServerClient } from '@supabase/ssr'
    import { NextResponse, type NextRequest } from 'next/server'

    // Ordem importa: prefixos mais especificos primeiro.
    // /dashboard/config deve ser avaliado antes de /dashboard.
    const ROLE_RULES: Array<{ prefix: string; allowed: string[] }> = [
      { prefix: '/admin', allowed: ['super_admin'] },
      { prefix: '/dashboard/config', allowed: ['adm', 'super_admin'] },
      { prefix: '/dashboard', allowed: ['adm', 'gerente', 'super_admin'] },
      { prefix: '/meu-resultado', allowed: ['vendedor'] },
      { prefix: '/treinamento', allowed: ['vendedor', 'adm', 'gerente', 'super_admin'] },
    ]

    // Rotas publicas — sem auth check. /convite e novo (link de convite).
    const PUBLIC_PREFIXES = ['/login', '/convite', '/api']

    export async function middleware(request: NextRequest) {
      let supabaseResponse = NextResponse.next({ request })

      const supabase = createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() { return request.cookies.getAll() },
            setAll(cookiesToSet: { name: string; value: string; options?: Record<string, unknown> }[]) {
              cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
              supabaseResponse = NextResponse.next({ request })
              cookiesToSet.forEach(({ name, value, options }) =>
                supabaseResponse.cookies.set(name, value, options)
              )
            },
          },
        }
      )

      const { pathname } = request.nextUrl

      // Rotas publicas: deixa passar (apos refresh de cookie acima)
      if (PUBLIC_PREFIXES.some(p => pathname.startsWith(p))) {
        return supabaseResponse
      }

      // getUser() valida o token no servidor Supabase — NUNCA usar getSession() aqui
      const { data: { user } } = await supabase.auth.getUser()

      if (!user) {
        const url = request.nextUrl.clone()
        url.pathname = '/login'
        return NextResponse.redirect(url)
      }

      // Role vem do JWT app_metadata — sem query ao banco (D-04)
      const role = (user.app_metadata?.role as string | undefined) ?? 'vendedor'

      const rule = ROLE_RULES.find(r => pathname.startsWith(r.prefix))
      if (rule && !rule.allowed.includes(role)) {
        const url = request.nextUrl.clone()
        url.pathname = role === 'vendedor' ? '/meu-resultado' : '/dashboard'
        return NextResponse.redirect(url)
      }

      return supabaseResponse
    }

    export const config = {
      matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)'],
    }
    ```

    Pontos criticos: (1) NAO deve sobrar nenhuma chamada `supabase.from('profiles')` — o role vem 100% de `app_metadata`; (2) `/convite` esta em `PUBLIC_PREFIXES` — usuario sem sessao precisa abrir o link; (3) a ordem de `ROLE_RULES` coloca `/dashboard/config` antes de `/dashboard` para que `.find()` case o mais especifico primeiro.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` contem `const ROLE_RULES` com exatamente 5 entradas de objeto `{ prefix, allowed }`
    - `src/middleware.ts` contem `const PUBLIC_PREFIXES` incluindo `'/convite'`
    - `src/middleware.ts` NAO contem a string `profiles` em nenhum lugar
    - `src/middleware.ts` NAO contem a string `getSession`
    - `src/middleware.ts` contem `user.app_metadata?.role`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Middleware protege 5 grupos de rotas via app_metadata.role sem query DB; /convite e publico; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Middleware | Toda navegacao do usuario passa pelo middleware antes de chegar a uma rota protegida |
| JWT -> Middleware | O role no `app_metadata` e emitido e assinado pelo Supabase Auth; o middleware confia no token apos `getUser()` valida-lo |
| Migration -> Banco | DDL aplicada altera constraints de seguranca (CHECK de role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Elevation of Privilege | Middleware role check | mitigate | `ROLE_RULES.find()` avalia prefixo; role nao-permitido -> redirect. Role invalido/ausente cai no default `'vendedor'` (menor privilegio) |
| T-3-02 | Spoofing | Acesso direto a /dashboard sem sessao | mitigate | `getUser()` valida token no servidor Supabase (nao `getSession()`); `!user` -> redirect /login |
| T-3-08 | Tampering | Role forjado no cookie do cliente | accept | `getUser()` revalida o JWT no servidor Supabase a cada request — cookie adulterado e rejeitado pela assinatura. Risco residual nulo |
| T-3-09 | Information Disclosure | Rota /convite exposta sem auth | accept | Intencional (D-07): /convite precisa ser publica. O token UUID na URL e o segredo; sem token valido a pagina nao revela nada (validacao no Plan 04) |
| T-3-10 | Elevation of Privilege | CHECK constraint permite role arbitrario | mitigate | Migration 0014 restringe `role` aos 4 valores via `CHECK`; INSERT/UPDATE com role fora da lista falha no banco |

ASVS L1: V4.1 (acesso negado por padrao — default `vendedor`), V4.2 (enforcement centralizado no middleware), V3.2 (token validado server-side via `getUser()`).
</threat_model>

<verification>
- `npm run build` exit code 0 (apos Task 3 — exceto a falha esperada e isolada de `UsersClient.tsx` apos Task 2, que e resolvida no Plan 06)
- `npm run lint` sem erros criticos
- Migration 0014 aplicada ou registrada para aplicacao manual no SUMMARY
- Teste manual AUTH-01 (apos toda a fase): vendedor em /dashboard -> redirect /meu-resultado; adm em /admin -> redirect /dashboard
</verification>

<success_criteria>
- `src/types/index.ts` exporta `UserRole` com 4 valores e `Profile` com `ativo`
- `src/middleware.ts` faz RBAC por `app_metadata.role` sem nenhuma query a `profiles`
- `/convite` listado como rota publica no middleware
- Migration 0014 criada com os 3 fixes de schema
</success_criteria>

<output>
Apos completar, criar `.planning/phases/03-auth-users/03-01-SUMMARY.md` registrando: estado de aplicacao da migration 0014, e a falha esperada de build em UsersClient.tsx (a ser resolvida no Plan 06).
</output>
