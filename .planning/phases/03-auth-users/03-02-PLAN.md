---
phase: 03-auth-users
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/login/page.tsx
autonomous: true
requirements: [AUTH-02]
must_haves:
  truths:
    - "Pagina /login renderiza usando componentes shadcn (Card, Input, Button, Label)"
    - "Pagina /login NAO tem link de auto-cadastro nem de recuperacao de senha"
    - "Login com credenciais validas redireciona para /"
    - "Login com credenciais invalidas mostra mensagem de erro sem expor detalhes tecnicos"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Tela de login redesenhada com design system v2"
      contains: "from '@/components/ui'"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "handleLogin no submit do form"
      pattern: "signInWithPassword"
    - from: "src/app/login/page.tsx"
      to: "/components/ui Card Input Button Label"
      via: "import do barrel de UI"
      pattern: "Card|Button"
---

<objective>
Reescrever a pagina `/login`, hoje 100% inline styles, usando os componentes shadcn ja instalados (Card, Input, Button, Label) com o tema dark GDS. Manter a identidade visual "DA SILVA / Painel de Vendas" e a logica de autenticacao Supabase existente, sem oferecer auto-cadastro.

Purpose: AUTH-02 exige que /login adote o design system v2. A logica de `signInWithPassword` ja funciona — o trabalho e substituir os `style={{}}` por componentes shadcn seguindo o contrato visual do UI-SPEC.
Output: `src/app/login/page.tsx` reescrito com componentes shadcn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-auth-users/03-CONTEXT.md
@.planning/phases/03-auth-users/03-UI-SPEC.md

<interfaces>
<!-- Contratos do UI-SPEC e dos componentes. Usar diretamente. -->

Componentes disponiveis no barrel `@/components/ui` (todos ja instalados):
```typescript
// Card, CardContent — ui/card.tsx
// Input — ui/input.tsx
// Label — ui/label.tsx
// Button — ui/button.tsx (variant="default" = bg-primary text-primary-foreground)
```

Logica de auth ATUAL (src/app/login/page.tsx) — preservar comportamento:
```typescript
const { error } = await supabase.auth.signInWithPassword({ email, password })
// sucesso: router.push('/'); router.refresh()
// erro: setError('Email ou senha incorretos.')
```

Contrato de copy do UI-SPEC para /login:
```
Wordmark chip: "DA SILVA"
Heading: "Painel de Vendas"
Subtext: "Entre com suas credenciais"
Email label: "Email"  | Password label: "Senha"
Primary CTA: "Entrar" | Loading CTA: "Entrando..."
Auth error: "Email ou senha incorretos. Verifique e tente novamente."
```

Contrato visual do UI-SPEC para /login:
- Container: `min-h-screen flex items-center justify-center bg-background px-4`
- Card: max-width 380px, shadcn Card + CardContent
- Logo chip: span com `bg-primary text-primary-foreground rounded-lg px-4 py-2 font-bold text-base`
- Email Input: type="email", autoComplete="email"
- Password Input: type="password", autoComplete="current-password"
- Submit: Button variant="default", full width
- Erro: div com `bg-destructive/10 border border-destructive/30 text-destructive text-[12px]`, `role="alert"`
- SEM link "Esqueci minha senha", SEM link de auto-cadastro
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever src/app/login/page.tsx com componentes shadcn</name>
  <read_first>
    - src/app/login/page.tsx (arquivo inteiro — logica de auth atual a preservar)
    - src/components/ui/card.tsx (API de Card e CardContent)
    - src/components/ui/button.tsx (variants disponiveis)
    - src/components/ui/input.tsx (props de Input)
    - src/components/ui/label.tsx (props de Label)
    - src/components/ui/index.tsx (confirmar quais nomes sao exportados pelo barrel)
    - .planning/phases/03-auth-users/03-UI-SPEC.md (secao "Screen 1: /login" — contrato visual e de copy)
  </read_first>
  <action>
    Substituir TODO o conteudo de `src/app/login/page.tsx`. Manter `'use client'` na primeira linha. Preservar a logica de `handleLogin` (signInWithPassword + router.push/refresh). Trocar todos os `style={{}}` por componentes shadcn + classes Tailwind, seguindo o contrato visual do UI-SPEC.

    Estrutura alvo:
    - Primeira linha: `'use client'`
    - Imports: `useState` de react, `createClient` de `@/lib/supabase/client`, `useRouter` de `next/navigation`, e os componentes do barrel `@/components/ui` (`Card`, `CardContent`, `Input`, `Label`, `Button`). Confirmar no `read_first` os nomes exatos exportados — se o barrel nao exportar algum, importar do arquivo direto (ex: `@/components/ui/card`).
    - Estado: `email`, `password`, `loading`, `error` (igual ao atual).
    - `handleLogin`: identico ao atual, exceto a mensagem de erro que passa a ser `'Email ou senha incorretos. Verifique e tente novamente.'`
    - JSX:
      - Container raiz: `<div className="min-h-screen flex items-center justify-center bg-background px-4">`
      - `<Card className="w-full max-w-[380px]">` com `<CardContent className="p-8">`
      - Topo centralizado: chip `<span className="inline-block bg-primary text-primary-foreground rounded-lg px-4 py-2 font-bold text-base">DA SILVA</span>`
      - `<h1>` "Painel de Vendas" (classes: `text-xl font-bold mt-4`)
      - `<p>` "Entre com suas credenciais" (classes: `text-[12px] text-muted-foreground font-mono mt-1`)
      - `<form onSubmit={handleLogin}>` com:
        - Campo email: `<Label htmlFor="email">Email</Label>` + `<Input id="email" type="email" autoComplete="email" required value={email} onChange={...} />`
        - Campo senha: `<Label htmlFor="password">Senha</Label>` + `<Input id="password" type="password" autoComplete="current-password" required value={password} onChange={...} />`
        - Bloco de erro (renderizado so se `error`): `<div role="alert" className="bg-destructive/10 border border-destructive/30 text-destructive text-[12px] rounded-md px-3 py-2">{error}</div>`
        - `<Button type="submit" disabled={loading} className="w-full">{loading ? 'Entrando...' : 'Entrar'}</Button>`
    - NAO incluir nenhum link de "criar conta", "cadastre-se" ou "esqueci minha senha".

    Espacamento entre campos: usar `space-y-4` no form ou `mb-4` nos wrappers de campo. O chip + heading + subtext ficam num bloco com `text-center mb-8`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/login/page.tsx` primeira linha e `'use client'`
    - O arquivo contem import de componentes UI (`Card` e `Button` presentes em alguma linha de import)
    - O arquivo contem a string `signInWithPassword` (logica preservada)
    - O arquivo contem `role="alert"` no bloco de erro
    - O arquivo NAO contem nenhuma das strings: `cadastr`, `criar conta`, `Esqueci`, `signUp`
    - O arquivo NAO contem nenhum `style={{` (zero inline styles)
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>/login renderizada com Card/Input/Button/Label shadcn, sem inline styles, sem auto-cadastro; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Supabase Auth | Credenciais email/senha enviadas para `signInWithPassword` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-11 | Information Disclosure | Mensagem de erro de login | mitigate | Erro generico `'Email ou senha incorretos'` — nao revela se o email existe (previne user enumeration). Nunca exibir `error.message` cru do Supabase |
| T-3-12 | Spoofing | Auto-cadastro permitiria criar conta sem convite | mitigate | /login nao tem `signUp` nem link de cadastro; unico caminho de criacao de conta e o fluxo de convite (Plans 03/04) |
| T-3-13 | Tampering | Forca bruta de senha | accept | Supabase Auth aplica rate limiting nativo em `signInWithPassword`; nao hand-roll de mitigacao adicional nesta fase |

ASVS L1: V2.1 (autenticacao via Supabase, nao hand-roll), V2.2 (mensagem de erro nao enumera usuarios).
</threat_model>

<verification>
- `npm run build` exit code 0
- `npm run lint` sem erros criticos
- Teste manual: abrir /login, conferir chip "DA SILVA", heading "Painel de Vendas", ausencia de link de cadastro; login com credenciais erradas mostra erro generico
</verification>

<success_criteria>
- `/login` usa componentes shadcn (Card, Input, Button, Label) com tema dark
- Zero inline styles na pagina
- Sem auto-cadastro nem recuperacao de senha
- Logica `signInWithPassword` preservada
</success_criteria>

<output>
Apos completar, criar `.planning/phases/03-auth-users/03-02-SUMMARY.md`.
</output>
