---
phase: 07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/dashboard/super-admin/api-key-actions.ts
  - src/app/dashboard/super-admin/ApiKeysManager.tsx
  - src/app/dashboard/super-admin/page.tsx
autonomous: false
requirements: [D-01, D-03, D-04, D-16]
must_haves:
  truths:
    - "Super_admin cria uma API key associada a um tenant e a vê UMA única vez com aviso de que não será exibida de novo"
    - "Super_admin revoga uma key (revoked_at setado) e ela deixa de autenticar"
    - "A lista mostra tenant associado, key_prefix, last_used_at e status (ativa/revogada) — nunca a key crua"
    - "Somente super_admin acessa as actions (guard de role em toda action)"
  artifacts:
    - path: "src/app/dashboard/super-admin/api-key-actions.ts"
      provides: "createApiKey, revokeApiKey, listApiKeys server actions"
      exports: ["createApiKey", "revokeApiKey", "listApiKeys"]
    - path: "src/app/dashboard/super-admin/ApiKeysManager.tsx"
      provides: "UI de criação/listagem/revogação de keys"
  key_links:
    - from: "src/app/dashboard/super-admin/api-key-actions.ts"
      to: "src/lib/auth/apiKey.ts"
      via: "generateApiKey"
      pattern: "generateApiKey"
    - from: "src/app/dashboard/super-admin/page.tsx"
      to: "ApiKeysManager"
      via: "render no painel super-admin"
      pattern: "ApiKeysManager"
---

<objective>
Adicionar ao painel super-admin (`/dashboard/super-admin`) uma seção de gestão de API keys: criar key associada a um tenant (exibida UMA vez com aviso), listar keys com tenant/prefix/last_used_at/status, e revogar. Somente super_admin. A key crua nunca é persistida nem re-exibida — só o hash SHA-256 e o prefixo (D-01).

Purpose: Fecha D-16 (UI) e D-03 (só super_admin) — o operador humano precisa emitir e revogar keys para os integradores.
Output: server actions + componente client + wiring na página.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-CONTEXT.md
@src/app/dashboard/super-admin/actions.ts
@src/app/dashboard/super-admin/page.tsx
@src/app/dashboard/super-admin/SuperAdminClient.tsx
@src/lib/auth/apiKey.ts
@src/lib/supabase/admin.ts

<interfaces>
<!-- Contratos consumidos + padrões de UI existentes a replicar. -->

```typescript
import { generateApiKey } from '@/lib/auth/apiKey'  // → { rawKey, keyHash, keyPrefix }
import { createAdminClient } from '@/lib/supabase/admin'
```

Padrão de server action com guard de super_admin (de actions.ts):
```typescript
'use server'
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) return { error: 'Não autorizado.' }
const { data: profile } = await supabase.from('profiles').select('role').eq('id', user.id).single()
if (profile?.role !== 'super_admin') return { error: 'Permissão negada. Apenas Super Admin.' }
const adminDb = createAdminClient()
// ... revalidatePath('/dashboard/super-admin')
```

api_keys (Plan 01): { id, tenant_id, name, key_prefix, key_hash, scopes, last_used_at, revoked_at, created_by, created_at }
tenants: { id, nome, slug, ativo } — já carregado em page.tsx via adminDb.from('tenants')
UI: componentes de @/components/ui (Button, Input, Label, Badge, DataTable) — ver SuperAdminClient.tsx para uso concreto.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions de API keys (create/revoke/list)</name>
  <files>src/app/dashboard/super-admin/api-key-actions.ts</files>
  <read_first>
    - src/app/dashboard/super-admin/actions.ts (padrão EXATO de guard super_admin + createAdminClient + revalidatePath)
    - src/lib/auth/apiKey.ts (generateApiKey — o que persistir e o que retornar uma vez)
    - .planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-CONTEXT.md (D-01/D-03/D-04)
  </read_first>
  <action>
Criar `src/app/dashboard/super-admin/api-key-actions.ts` (`'use server'`) com 3 actions, cada uma com o guard de super_admin do bloco `<interfaces>` (copiar verbatim):

1. `createApiKey(formData: FormData)`: ler `tenant_id` e `name` do form. Guard super_admin. Validar `tenant_id` presente (senão `{ error:'Selecione uma empresa.' }`). `const { rawKey, keyHash, keyPrefix } = generateApiKey()`. Inserir em `api_keys` `{ tenant_id, name: name ?? '', key_prefix: keyPrefix, key_hash: keyHash, created_by: user.id }` — NÃO gravar rawKey. Em colisão de `key_prefix` (UNIQUE violation), regenerar e tentar novamente (até 3 tentativas). `revalidatePath('/dashboard/super-admin')`. Retornar `{ success: true, rawKey }` — rawKey retornado UMA vez para exibição e nunca mais (D-01).

2. `revokeApiKey(id: string)`: guard super_admin. `adminDb.from('api_keys').update({ revoked_at: new Date().toISOString() }).eq('id', id)`. revalidatePath. Retornar `{ success: true }` ou `{ error }`.

3. `listApiKeys()`: guard super_admin. `adminDb.from('api_keys').select('id, tenant_id, name, key_prefix, last_used_at, revoked_at, created_at').order('created_at', { ascending: false })`. Retornar as rows — NUNCA `key_hash`, NUNCA a key crua.

SEGURANÇA: nunca retornar `key_hash` ao client; nunca logar rawKey; rawKey só no retorno de `createApiKey`, uma vez.
  </action>
  <verify>
    <automated>grep -q "generateApiKey" src/app/dashboard/super-admin/api-key-actions.ts && grep -q "super_admin" src/app/dashboard/super-admin/api-key-actions.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/super-admin/api-key-actions.ts` exporta `createApiKey`, `revokeApiKey`, `listApiKeys`
    - `grep -q "'use server'" src/app/dashboard/super-admin/api-key-actions.ts`
    - Cada action tem o guard `profile?.role !== 'super_admin'` (grep conta 3 ocorrências)
    - O `.select(` de `listApiKeys` NÃO inclui `key_hash`
    - `createApiKey` retorna `rawKey`; nenhum insert grava `rawKey`
    - `npm run build` passa
  </acceptance_criteria>
  <done>Actions de create/revoke/list implementadas com guard super_admin, persistindo só hash+prefixo e retornando a rawKey uma única vez.</done>
</task>

<task type="auto">
  <name>Task 2: Componente ApiKeysManager + wiring na página super-admin</name>
  <files>src/app/dashboard/super-admin/ApiKeysManager.tsx, src/app/dashboard/super-admin/page.tsx</files>
  <read_first>
    - src/app/dashboard/super-admin/SuperAdminClient.tsx (padrão de form com server action, DataTable, Badge, useState de loading/error)
    - src/app/dashboard/super-admin/page.tsx (onde carregar tenants + keys e renderizar o novo componente)
    - src/app/dashboard/super-admin/api-key-actions.ts (actions a consumir)
  </read_first>
  <action>
1. Criar `src/app/dashboard/super-admin/ApiKeysManager.tsx` (`'use client'`) recebendo props `{ tenants: {id,nome}[]; keys: ApiKeyRow[] }`. Conteúdo:
   - Form de criação: `<select name="tenant_id">` com as empresas, `<Input name="name" placeholder="Rótulo (ex: ERP Loja Jebai)">`, botão "Gerar API Key". `action={handleCreate}` que chama `createApiKey(formData)`.
   - Ao receber `{ rawKey }`: exibir um bloco de destaque (Badge/box com `var(--accent)` ou classe de alerta) mostrando a rawKey em fonte mono, com o aviso literal: "Copie agora — esta chave não será exibida novamente." e um botão "Copiar" (navigator.clipboard.writeText). Guardar rawKey em useState só na memória do client (nunca re-fetch).
   - `DataTable` das keys: colunas Empresa (resolver nome pelo tenant_id via mapa dos tenants), Prefixo (`key_prefix` + "…"), Último uso (`last_used_at` formatado ou "Nunca"), Status (Badge "Ativa" se `revoked_at==null`, senão "Revogada"), Ações (botão "Revogar" chamando `revokeApiKey(id)`, desabilitado se já revogada).
   - Estados de loading/error via useState, espelhando SuperAdminClient.tsx.

2. Editar `src/app/dashboard/super-admin/page.tsx`: após carregar tenants, chamar `listApiKeys()` (ou `adminDb.from('api_keys').select(...)`) para obter as keys, e renderizar `<ApiKeysManager tenants={tenants} keys={keys} />` abaixo do `<SuperAdminClient .../>` existente. Passar apenas `{ id, nome }` dos tenants para o componente.

SEGURANÇA: a rawKey só vive no state do client após criação; a lista nunca contém key_hash nem rawKey.
  </action>
  <verify>
    <automated>grep -q "ApiKeysManager" src/app/dashboard/super-admin/page.tsx && grep -q "não será exibida novamente" src/app/dashboard/super-admin/ApiKeysManager.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/super-admin/ApiKeysManager.tsx` existe, é `'use client'`, consome `createApiKey` e `revokeApiKey`
    - Contém o aviso literal "não será exibida novamente" e um botão de copiar
    - `page.tsx` importa e renderiza `<ApiKeysManager` passando tenants e keys
    - A tabela mostra Prefixo, Último uso e Status; nunca renderiza a key crua fora do bloco pós-criação
    - `npm run build` passa
  </acceptance_criteria>
  <done>UI de gestão de keys integrada ao painel super-admin: criar (com exibição única + copiar), listar com status/prefixo/último uso, e revogar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana do fluxo de keys end-to-end</name>
  <read_first>
    - src/middleware.ts (confirmar que `/api/v1` está em PUBLIC_PREFIXES — é o que faz o passo 5 retornar 401 JSON em vez de redirect 307 para /login)
    - .planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-CONTEXT.md (D-01: exibição única; D-03: só super_admin; D-16: UI)
    - src/app/dashboard/super-admin/ApiKeysManager.tsx (o fluxo de UI a validar visualmente)
  </read_first>
  <action>Pausar para o operador humano validar o fluxo completo de emissão/uso/revogação de API key seguindo os passos de how-to-verify. Nada aqui é automatizável: exige inspeção visual da exibição única da key crua e teste real de autenticação HTTP via curl contra os endpoints /api/v1/*.</action>
  <what-built>
Seção de API keys no painel super-admin: criação com exibição única, listagem e revogação. Endpoints `/api/v1/sales` e `/api/v1/stock` (Plans 03/04) prontos para autenticar com a key gerada.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, logar como super_admin, ir a `/dashboard/super-admin`.
    2. Na seção API Keys, selecionar uma empresa, dar um rótulo, clicar "Gerar API Key". Confirmar que a chave `gds_live_...` aparece UMA vez com o aviso e o botão copiar funciona. Copiar a chave.
    3. Recarregar a página — a chave crua NÃO deve reaparecer; só o prefixo na tabela.
    4. Testar autenticação real:
       `curl -i -X POST http://localhost:3000/api/v1/stock -H "Authorization: Bearer <chave copiada>" -H "Content-Type: application/json" -d '{"items":[{"product_code":"P1","quantity":10}]}'`
       → deve retornar 200 com envelope `{ success:true, accepted:1, ... }`.
    5. `curl -i -X POST http://localhost:3000/api/v1/sales` SEM header Authorization → deve retornar 401 JSON (NÃO redirect 307 para /login — valida Pitfall 1 do middleware).
    6. Revogar a key na UI (status vira "Revogada"). Repetir o curl do passo 4 com a mesma chave → agora deve retornar 401.
  </how-to-verify>
  <acceptance_criteria>
    - Passo 2: a rawKey `gds_live_...` é exibida exatamente UMA vez com o aviso e botão copiar (D-01/D-16)
    - Passo 3: após reload, a rawKey NÃO reaparece — só `key_prefix` na tabela (verificável: nenhum endpoint/state re-expõe a key crua)
    - Passo 4: `curl` com key válida em /api/v1/stock retorna HTTP 200 e envelope `success:true, accepted:1`
    - Passo 5: `curl` sem Authorization em /api/v1/sales retorna HTTP 401 JSON (NÃO 307 para /login) — confirma `/api/v1` liberado no middleware
    - Passo 6: após revogar, o mesmo `curl` com a key revogada retorna HTTP 401 (revoked_at invalida imediatamente)
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" se todos os passos funcionarem, ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| super_admin UI → api-key-actions | Ações privilegiadas de emissão/revogação de credenciais |
| server action → client | rawKey retornada uma vez; hash nunca sai do servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-07 | Elevation of Privilege | api-key-actions | mitigate | Guard `role === 'super_admin'` em TODA action (D-03); adm/vendedor não emitem keys (Task 1) |
| T-7-03 | Information Disclosure | rawKey / key_hash | mitigate | rawKey exibida uma vez e só vive no state do client; `key_hash` nunca retornado ao client; banco guarda só hash+prefixo (Task 1/2) |
| T-7-05 | Elevation of Privilege | revogação | mitigate | `revoked_at` setado invalida a key imediatamente (getApiKeyContext checa a cada request); verificado no checkpoint passo 6 (Task 3) |
</threat_model>

<verification>
- `npm run build` passa
- Checkpoint humano: criação (exibição única), persistência só de prefixo, auth real via curl, 401 sem key (não redirect), revogação invalida
</verification>

<success_criteria>
Super_admin emite e revoga API keys pela UI; a key crua aparece só uma vez; endpoints autenticam com a key emitida e rejeitam a revogada.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-05-SUMMARY.md`
</output>
</content>
