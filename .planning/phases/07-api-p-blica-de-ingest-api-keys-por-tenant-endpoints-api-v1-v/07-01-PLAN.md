---
phase: 07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql
  - src/types/supabase.ts
autonomous: false
requirements: [D-02, D-04, D-09, D-10, D-11, D-12, D-15]
must_haves:
  truths:
    - "Tabela api_keys existe com hash SHA-256, key_prefix único, scopes default ['*'], last_used_at e revoked_at"
    - "Tabela stock_snapshots existe agregada por tenant, com UNIQUE(tenant_id, product_code, snapshot_date)"
    - "connector_id_mappings aceita source_system='api' e entity_type='product'"
    - "Schema aplicado ao banco remoto (supabase db push) e src/types/supabase.ts regenerado com as novas tabelas"
  artifacts:
    - path: "supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql"
      provides: "Schema api_keys + stock_snapshots + ALTER connector_id_mappings"
      contains: "CREATE TABLE IF NOT EXISTS api_keys"
    - path: "src/types/supabase.ts"
      provides: "Tipos TS das novas tabelas"
      contains: "api_keys"
  key_links:
    - from: "supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql"
      to: "banco remoto zsczxblhtdhpdqvkpuwz"
      via: "supabase db push"
      pattern: "api_keys"
---

<objective>
Criar o schema de banco desta fase: tabela `api_keys` (autenticação por key por tenant), tabela `stock_snapshots` (snapshot diário de estoque agregado por tenant) e estender `connector_id_mappings` para aceitar dados vindos da API pública (`source_system='api'`, `entity_type='product'`). Aplicar ao banco remoto e regenerar os tipos TypeScript.

Purpose: Todas as demais peças da fase (helper de auth, endpoints, UI de keys) dependem destas tabelas existirem no banco em runtime. É a fundação.
Output: 1 migration nova aplicada + `src/types/supabase.ts` atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-CONTEXT.md
@.planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-RESEARCH.md
@supabase/migrations/20260617000001_connectors.sql
@supabase/migrations/20260626171308_profit_commission.sql

<interfaces>
<!-- Padrões de schema existentes que ESTE plano deve replicar. Extraído do código. -->

connector_id_mappings (migration 20260617000001) — CHECKs atuais a estender:
```sql
source_system TEXT NOT NULL CHECK (source_system IN ('pegasus', 'isrp', 'cec'))
entity_type   TEXT NOT NULL CHECK (entity_type IN ('vendor', 'client'))
UNIQUE(tenant_id, source_system, entity_type, source_id)
```

Padrão de RLS por tenant usado no projeto (profiles join, NÃO auth.jwt tenant_id):
```sql
CREATE POLICY "tenant_isolation" ON <tabela>
  USING (tenant_id IN (SELECT tenant_id FROM profiles WHERE id = (SELECT auth.uid())));
```

tenants.id e profiles.id são UUID. periods.id é integer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration api_keys + stock_snapshots + ALTER connector_id_mappings</name>
  <read_first>
    - supabase/migrations/20260617000001_connectors.sql (padrão de CREATE TABLE, RLS, trigger updated_at, e os CHECKs de connector_id_mappings a alterar)
    - supabase/migrations/20260626171308_profit_commission.sql (padrão exato de RLS "tenant_isolation" e referências a tenants(id))
    - .planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-RESEARCH.md (seção "Code Examples" — schema de referência)
  </read_first>
  <action>
Criar o arquivo `supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql` com EXATAMENTE este conteúdo (valores concretos, não placeholders):

```sql
-- Phase 07: API pública de ingest — api_keys, stock_snapshots, extensão de id mappings

-- ── 1. api_keys (D-01/D-02/D-03/D-04) ──────────────────────────────────────
CREATE TABLE IF NOT EXISTS api_keys (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id     uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  name          text NOT NULL DEFAULT '',            -- rótulo livre p/ o super_admin (ex: "ERP Loja Jebai")
  key_prefix    text NOT NULL,                        -- primeiros chars da key crua, p/ lookup + exibição parcial
  key_hash      text NOT NULL,                        -- SHA-256 hex da key completa — NUNCA a key crua
  scopes        text[] NOT NULL DEFAULT ARRAY['*'],   -- D-02: full-access no MVP, extensível sem migration
  last_used_at  timestamptz,
  revoked_at    timestamptz,                          -- NULL = ativa
  created_by    uuid REFERENCES profiles(id),
  created_at    timestamptz NOT NULL DEFAULT now(),
  UNIQUE(key_prefix)
);

CREATE INDEX IF NOT EXISTS api_keys_tenant ON api_keys (tenant_id);

ALTER TABLE api_keys ENABLE ROW LEVEL SECURITY;

-- D-03: self-service é fase futura. Nega acesso direto a anon/authenticated.
-- A gestão real acontece via createAdminClient() (service-role bypassa RLS) nas server actions do super_admin.
CREATE POLICY api_keys_no_direct_access ON api_keys
  FOR ALL
  USING (false);

-- ── 2. stock_snapshots (D-09/D-10/D-11/D-12) ───────────────────────────────
-- Snapshot diário agregado por tenant (sem granularidade de loja — D-10).
-- Sem coluna de custo (D-12: custo vem do catálogo products).
CREATE TABLE IF NOT EXISTS stock_snapshots (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id     uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  product_code  text NOT NULL,
  snapshot_date date NOT NULL,                        -- dia do snapshot (D-11: 1/dia/produto)
  quantity      numeric(12,2) NOT NULL,
  source        text NOT NULL DEFAULT 'api',
  created_at    timestamptz NOT NULL DEFAULT now(),
  updated_at    timestamptz NOT NULL DEFAULT now(),
  UNIQUE(tenant_id, product_code, snapshot_date)      -- D-11: upsert diário (último POST do dia vence)
);

CREATE INDEX IF NOT EXISTS stock_snapshots_tenant_date
  ON stock_snapshots (tenant_id, snapshot_date);

ALTER TABLE stock_snapshots ENABLE ROW LEVEL SECURITY;

CREATE POLICY "tenant_isolation" ON stock_snapshots
  USING (tenant_id IN (
    SELECT tenant_id FROM profiles WHERE id = (SELECT auth.uid())
  ));

-- ── 3. connector_id_mappings: aceitar 'api' e entity 'product' (D-15) ───────
ALTER TABLE connector_id_mappings DROP CONSTRAINT IF EXISTS connector_id_mappings_source_system_check;
ALTER TABLE connector_id_mappings
  ADD CONSTRAINT connector_id_mappings_source_system_check
  CHECK (source_system IN ('pegasus', 'isrp', 'cec', 'api'));

ALTER TABLE connector_id_mappings DROP CONSTRAINT IF EXISTS connector_id_mappings_entity_type_check;
ALTER TABLE connector_id_mappings
  ADD CONSTRAINT connector_id_mappings_entity_type_check
  CHECK (entity_type IN ('vendor', 'client', 'product'));
```

Notas de design (não alterar sem justificar): `stock_snapshots` NÃO tem coluna de custo (D-12). `api_keys` NÃO armazena a key crua, só `key_hash` (SHA-256 hex) e `key_prefix`. O nome de tabela `stock_snapshots` é a decisão registrada desta fase — a Fase 08 consome esta série (documentar em STATE.md ao fechar a fase).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql && grep -q "CREATE TABLE IF NOT EXISTS api_keys" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql && grep -q "UNIQUE(tenant_id, product_code, snapshot_date)" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql && grep -q "'api'" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql && grep -q "'product'" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql` existe
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql` retorna 2 (api_keys + stock_snapshots)
    - `grep "scopes" supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql` mostra `DEFAULT ARRAY['*']`
    - `grep "connector_id_mappings_source_system_check"` mostra CHECK incluindo `'api'`
    - `grep "connector_id_mappings_entity_type_check"` mostra CHECK incluindo `'product'`
    - NÃO existe coluna de custo em stock_snapshots (grep por `cost` no bloco stock_snapshots retorna vazio)
  </acceptance_criteria>
  <done>Migration criada com api_keys, stock_snapshots e os dois ALTER CHECK, seguindo o padrão de RLS por profiles join do projeto.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar schema ao banco remoto + regenerar tipos</name>
  <read_first>
    - CLAUDE.md do projeto (nota: `apply_migration`/`execute_sql` via MCP falham frequentemente neste projeto — usar Supabase CLI)
    - supabase/migrations/20260714000001_api_keys_and_stock_snapshots.sql (o que será aplicado)
  </read_first>
  <what-built>
Migration `20260714000001_api_keys_and_stock_snapshots.sql` pronta para aplicar ao projeto Supabase `zsczxblhtdhpdqvkpuwz`.
  </what-built>
  <action>
Este é o passo [BLOCKING] obrigatório de schema push. Build e type-check passam SEM ele (tipos vêm do config, não do banco vivo), criando falso-positivo — por isso é mandatório aplicar antes da verificação da fase.

Tentar automaticamente primeiro (não-TTY):
```bash
export SUPABASE_ACCESS_TOKEN=<token do usuário>
supabase db push
```

Se o comando exigir prompt interativo (senha do DB ou confirmação Y/N que não pode ser suprimida), este é o ponto de intervenção manual — o usuário roda `supabase db push` localmente e confirma.

Após aplicar, regenerar os tipos (suprimir stderr conforme decisão de tooling em STATE.md):
```bash
supabase gen types typescript --project-id zsczxblhtdhpdqvkpuwz 2>/dev/null > src/types/supabase.ts
```
  </action>
  <how-to-verify>
    1. Confirmar que as tabelas existem no banco: no painel Supabase → Table Editor, `api_keys` e `stock_snapshots` aparecem.
    2. `grep -q "api_keys" src/types/supabase.ts` retorna sucesso (tipos regenerados incluem a nova tabela).
    3. `grep -q "stock_snapshots" src/types/supabase.ts` retorna sucesso.
    4. `npm run build` passa.
  </how-to-verify>
  <resume-signal>Digite "aplicado" quando as tabelas existirem no banco e os tipos estiverem regenerados, ou descreva o erro.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client authenticated/anon → api_keys | Tenant NÃO deve ler suas próprias keys (self-service é fase futura, D-03) |
| integrador externo → stock_snapshots | Dados escritos só via service-role no endpoint; RLS isola leitura por tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-07 | Information Disclosure / Elevation | tabela `api_keys` | mitigate | RLS `FOR ALL USING (false)` nega leitura direta a anon/authenticated; acesso só via `createAdminClient()` nas server actions do super_admin (Plan 05). `key_hash` armazena SHA-256, nunca a key crua |
| T-7-09 | Tampering | `stock_snapshots` RLS | mitigate | Política `tenant_isolation` via join em profiles — tenant só enxerga seus snapshots. Writes do endpoint usam service-role com `.eq('tenant_id')` obrigatório (Plan 04) |
| T-7-10 | Tampering | CHECK de `connector_id_mappings` | accept | Estender CHECK para `'api'`/`'product'` amplia valores válidos; risco baixo (constraint continua restritiva a lista fechada) |
</threat_model>

<verification>
- Migration existe e passa nos greps de estrutura
- `supabase db push` aplicado (tabelas visíveis no painel)
- `src/types/supabase.ts` contém `api_keys` e `stock_snapshots`
- `npm run build` passa
</verification>

<success_criteria>
As tabelas `api_keys` e `stock_snapshots` existem no banco remoto, `connector_id_mappings` aceita `source_system='api'` e `entity_type='product'`, e os tipos TS refletem o novo schema.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-api-p-blica-de-ingest-api-keys-por-tenant-endpoints-api-v1-v/07-01-SUMMARY.md`
</output>
